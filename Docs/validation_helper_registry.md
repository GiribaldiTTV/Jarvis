# Nexus Validation Helper Registry
<!-- NEXUS-SOURCE-OWNER: schema=source-owner-v1; owner=VALIDATOR-HELPER; ledger=SRCOWN-FIRSTPASS-VALIDATOR-010; surface=validation-helper-registry; status=canonical -->

## Purpose

`Docs/validation_helper_registry.md` is the repo-wide registry for validation helpers, live-validation scripts, audit helpers, harnesses, and shared validation support that live under `dev/`.

Its job is to:

- make helper ownership easy for future developers to discover
- prevent one helper per feature or seam by inertia
- standardize helper names and release-readiness expectations
- require reuse, parameterization, or consolidation before new helper creation
- preserve workstream-specific helper exceptions only when they are explicit and temporary or intentionally scoped

`Docs/phase_governance.md` owns the policy.
This registry owns the helper inventory and naming classification.
Canonical workstream docs own the evidence produced by helpers for a specific branch.

## Validation Output Interpretation Rule

Rule Name: `Validation Output Interpretation Rule`
Owner: `Docs/validation_helper_registry.md` for helper/validator evidence interpretation; `Docs/phase_governance.md` for phase-gate use.
Applies To: every validator, helper, audit, scanner, harness, live-validation script, PR watcher, release helper, and Codex claim that a branch is green, red, blocked, ready, or safe to continue.
Required State: helper output is evidence for review, not final authority. Codex retains responsibility for the phase decision and must trust but verify every validator/helper result against source truth, changed files, phase requirements, USER-approved scope, review bundles, and known drift risks before deciding the branch is green or deciding what to patch.
Allowed Values: `Evidence Supports Green`, `Evidence Supports Repair`, `Evidence Supports Blocked`, `Tool Defect Suspected`, `Tool Defect Confirmed`, `Environment / Configuration Issue`, `Source-Truth Drift`, `USER Decision Required`.
Invalid Values: `Green Because Validator Passed`, `Patch Validator Until Green`, `Ignore Failed Helper`, `Accept Red Without Diagnosis`, `Accept Blocked Without Diagnosis`, `Treat Helper As Source Truth`.
Blocking Condition: `Validation Adequacy Review Missing`, `Validator Green Accepted Without Review`, or `Validator / Helper Patch By Inertia` when Codex treats PASS/GREEN as sufficient by itself, patches a validator/helper before diagnosing the failed condition, or ignores a possible false green/false red.
Repair Owner: current branch owner for source/product defects; helper registry and owning validator/helper for proven tool defects; USER for unclear scope or waiver decisions.
Repair Path: diagnose the result first, classify the failure or green result, inspect the changed files and relevant source-truth owners, decide whether the product/source truth or the tool is wrong, add or update fixture/smoke coverage when the defect is machine-checkable, then rerun validation. A validator/helper code patch is the last repair option unless the tool defect is directly proven by source truth, a minimal reproduction, or a review comment.
USER Decision Required: required when fixing the reported issue would expand scope, alter product/runtime behavior, change source-truth ownership, waive validation evidence, or accept a known helper blind spot.
Validation Owner: the helper/validator that produced the result plus the source-truth owner named by the phase packet.
Final Disposition: every return packet that relies on validation must state that Codex reviewed the validation rather than delegated judgment to it, summarize why the validation is adequate, name what was independently checked, name whether any helper/validator limitation remains, and classify the result as `Green`, `Repair`, `Blocked`, or `USER Decision Required`.

## Plugin / Connector Evidence Interpretation Rule

Rule Name: `Plugin / Connector Evidence Interpretation Rule`
Owner: `Docs/validation_helper_registry.md` for evidence interpretation; `Docs/governance_efficiency_operating_model.md` for repo/external-state split compatibility; `Docs/phase_governance.md` for phase-gate use.
Applies To: Codex app plugins, Codex connectors, GitHub connector reads/writes, Browser / Chrome / Computer Use evidence, OpenAI Docs lookups, OpenAI Developers tooling, Documents / Spreadsheets / Presentations artifacts, and any helper or validator that consumes plugin/connector output.
Required State: plugin and connector output is evidence. It must be reviewed against source truth, live fact owners, changed files, privacy boundaries, USER-approved scope, and phase requirements before Codex claims green, repair, blocked, implementation-ready, PR-ready, release-ready, or safe-to-merge posture.
Allowed Values: `Evidence Supports Green`, `Evidence Supports Repair`, `Evidence Supports Blocked`, `Tool / Connector Unavailable`, `Tool / Connector Drift`, `Sensitive Setup Blocked`, `USER Decision Required`.
Invalid Values: `Green Because Connector Said So`, `Docs Lookup Is Source Truth`, `Plugin Output Is Authority`, `Connector Auth State In Repo`, `API Key State In Repo`, `Live PR Reaction Ledger In Repo`, `Temporary Artifact Treated As Durable`.
Blocking Condition: `Plugin Evidence Treated As Source Truth`, `Plugin Live-State Ledger In Repo`, `Sensitive Connector Setup In Repo`, or `Plugin / Connector Use Plan Missing` blocks when plugin/connector output is used without interpretation, when live state is committed as repo truth, or when a sensitive setup path is smuggled into tracked files.
Repair Owner: current branch owner for branch-local misuse; `Docs/validation_helper_registry.md` and the consuming helper/validator for proven tool interpretation defects; `Docs/governance_efficiency_operating_model.md` for split compatibility drift; USER for sensitive setup or waiver decisions.
Repair Path: classify the plugin/connector use, identify the true live or durable owner, remove live operational ledger text from repo docs, move raw evidence to the Codex digest, USER review packet, helper output, live tool surface, or approved external state, and add machine-checkable fixture coverage only after a concrete false-green or false-red defect is proven.
USER Decision Required: required for plugin/connector execution that mutates source, external state, credentials, provider/API setup, private/Owner/Dev setup, or durable source truth.
Validation Owner: current phase validators plus the helper/validator that consumes the tool result.

Visible Desktop / Tray Target-Acquisition Interpretation:

- `Screenshot / Tooltip Identity Evidence` means the intended target is probably identified. It is not proof that Codex operated it.
- `USER Manual Confirmation` that a tray icon or menu works is supporting evidence only; it cannot clear Codex Live Validation control-surface proof. Validators must report USER manual confirmation as supporting evidence, not PASS.
- `Codex-Operated Tray Proof PASS` requires Codex-owned visible user-level input opening the intended menu/control, followed by photo/video or ordered frame-sequence evidence showing the intended NDAI tray menu or resulting window.
- `Live Validation Control-Surface Acquisition Blocked` is the required classification when multi-monitor coordinate translation, screenshot-origin drift, physical cursor mismatch, shell/taskbar target ambiguity, or hidden-overflow uncertainty prevents a safe user-equivalent click. Helpers and validators must not downgrade that blocker to PASS by using nearby coordinate probing, direct handler calls, internal signal dispatch, synthetic tray/menu activation, or USER-only confirmation.
- `Validation-Only Runtime Control Surface` is invalid proof. Helpers and validators must not clear Live Validation by creating, adding, or depending on a new user-visible runtime control surface, dev-only control window, shortcut side channel, or validation-only product window merely to make existing tray/menu/window controls targetable; actual USER-facing controls must be operated through visible user-equivalent input, or the result must stop on `Live Validation Control-Surface Acquisition Blocked`.
- `Activation-Path Proof Non-Transferable` means each admitted activation path is a separate proof lane: click, keybind, tray/menu, shortcut, and visible window/control activation must be proven separately with photo/video or ordered frame evidence for both the user action and the resulting visible state. A single named USER manual waiver may record confirmation for one unprovable activation path after exhaustive Codex-owned visible attempts, but helpers and validators must not generalize that waiver to other activation paths, controls, windows, or product surfaces.
- `Visible Desktop Coordinate Control` is the allowed old-style real-cursor route for OS-level surfaces such as tray icons, notification-area menus, window borders, resize handles, and desktop chrome. It may be the primary proof route when it uses a visible desktop-control session or the governed human-client helper, moves the real Windows cursor slowly to a screenshot-tied target, sends real OS-level mouse down/up or drag input, preserves before/during/after photo/video or ordered-frame evidence, and verifies the resulting visible menu/window/state. It cannot use shell commands, direct handler calls, hidden activation, synthetic tray/menu activation, blind coordinate probing, or repeated nearby guessing.
- `USER Workspace Preservation` means a Live Validation helper must inventory the visible pre-existing open-window state before interaction and leave every such window unminimized and otherwise undisturbed. `MinimizeAll`, show-desktop, `Win+D`, per-window minimize, hide, restore, close, move, resize, rearrangement, and equivalent shell/Win32/UIAutomation window-state manipulation are invalid setup, target-acquisition, backdrop, evidence, and cleanup actions. Only the in-scope product window may change state, and only when its exact window-state behavior is explicitly admitted by the approved checklist. A helper that cannot acquire its target or proof setup without disturbing another open window must emit `Live Validation USER Workspace Preservation Blocked` rather than continue.
- `Dual-Contrast Window Border Proof` requires every acceptance-critical window border/frame to have a matched focused screenshot against uniform opaque black `#000000` and a matched focused screenshot against uniform opaque white `#FFFFFF`, with identical window geometry, display scale, state, and capture bounds. Helpers must preserve both artifacts and separate border/mask/perimeter/halo/shadow/glow/bleed/clipping/hit-boundary observations in the manifest. White-only, black-only, wallpaper-only, unmatched, or visually ambiguous proof cannot be reported as PASS. Temporary backdrops must be bounded behind the in-scope window and must obey `USER Workspace Preservation`.
- Existing helper rows that describe a white-only or black-only border/corner probe document the helper's current implementation, not an exception to `Dual-Contrast Window Border Proof`. Until the owning worktree extends or composes that helper to produce the matched pair without disturbing pre-existing windows, its border result is supporting evidence only and cannot clear Live Validation, UTS handoff, or PR Readiness.
- `Manual Validation Request Digest` is required before USER handoff when one activation path cannot be proven reliably. Helpers must report the exact control path, intended USER action, expected visible result/state change, captured screenshot/frame evidence, reason Codex proof is unreliable, exact USER action requested, pass/fail criteria, single waiver scope, and still-unproven paths/surfaces; helpers must stop until USER returns `GREEN`, `NOT GREEN`, or repair direction for that named path.
- A product tray/menu defect may be claimed only after target-acquisition proof shows the correct control was operated and runtime/photo/video evidence proves the expected app response did not occur.
Final Disposition: tool evidence can support a recommendation, but Codex must still state what it independently verified and whether the plugin/connector evidence is durable, live, sensitive, or review-only.

## Helper Status Values

Every durable helper or validator under root `dev/` must fit one of these statuses:

- Helper Status: Reusable
- Helper Status: Workstream-scoped
- Helper Status: Temporary probe
- Helper Status: Historical

Definitions:

- `Reusable` means future work should extend or parameterize this helper before creating a new one.
- `Workstream-scoped` means the helper exists for one active workstream because reuse would currently blur proof or behavior boundaries; it must name an owner and consolidation target.
- `Temporary probe` means the file is exploratory only, must stay under an ignored evidence root such as `dev/logs/...`, and must not be committed as closeout-grade tooling.
- `Historical` means the helper is preserved for prior evidence or legacy workflows and is not the default extension point unless explicitly selected.

## Naming Standard

Use these names for new durable validation assets:

- repo-side validator: `dev/orin_<domain>_<capability>_validation.py`
- live desktop helper: `dev/orin_<domain>_<capability>_live_validation.ps1`
- interactive suite: `dev/orin_<domain>_<capability>_interactive_validation.ps1`
- audit helper: `dev/orin_<domain>_<capability>_audit.ps1`
- reusable harness: `dev/orin_<domain>_<capability>_harness.py`
- shared helper module: `dev/orin_<domain>_<capability>_helper.py`

Workstream-scoped exception:

- `dev/orin_<workstream_id>_<bounded_capability>_validation.ps1`
- `dev/orin_<workstream_id>_<bounded_capability>_live_validation.ps1`

Workstream-scoped names must prefer capability words over seam numbers.
Seam-number names are allowed only as a short-lived bridge when the helper was created during a live seam and must carry an explicit consolidation target before PR Readiness.

## Reuse Decision Order

Before creating a new helper, Codex must record or be able to report this decision order:

1. registry lookup by domain and capability
2. use an existing `Reusable` helper unchanged when it already covers the path
3. parameterize or extend the nearest reusable helper in the same helper family
4. extract shared support when two or more helpers need the same watchdog, progress, cleanup, UIAutomation, runtime startup, source snapshot, or artifact-writing behavior
5. create a `Workstream-scoped` helper only when reuse would contaminate proof ownership, blur branch truth, or make validation less reliable
6. use a `Temporary probe` only under ignored evidence roots and delete or promote it before closeout-grade proof

## Required Workstream-Scoped Markers

Every `Workstream-scoped` durable helper must record:

- Helper Status: Workstream-scoped
- Owner Workstream:
- Reason Reusable Helper Was Not Extended:
- Consolidation Target:
- Promotion Decision Point:

Before PR Readiness, each workstream-scoped helper must be resolved in one of three ways:

- folded into an existing `Reusable` helper
- promoted to `Reusable` with this registry updated
- kept as `Workstream-scoped` only with an explicit reason that the helper should remain branch-family-specific after merge

## USER Review Bundle Stale-Output Invariant

The USER review hub helper must treat every local USER hub packet refresh as a clean regeneration. It clears the stable worktree-labeled folder under `C:\Nexus USER`, writes root `START_HERE.md`, writes exactly one primary current-gate decision file under `USER Review`, writes generated supporting digests/checklists under `Review Aids`, copies fresh repo-relative context files under `Source Truth Context`, removes any legacy same-name `C:\Nexus USER\<worktree-label>.zip` upload artifact and previous same-label timestamped upload ZIPs, creates a mandatory timestamped upload ZIP at `C:\Nexus USER\<worktree-label>-YYYYMMDD-HHMMSS.zip`, and validates the timestamped zip filename, zip file list, expected packet identity, copied source-file content, plus applicable `START_HERE.md` / `USER_BRANCH_VISION_REVIEW.md` / `USER_BRANCH_PLAN_REVIEW.md` packet structure before reporting PASS. For PR Readiness Stage 1, the one primary file must instead be `PR_READINESS_STAGE1_REVIEW.md`; BP1 and BP2 remain supporting context and cannot be promoted to the Stage 1 decision surface. The helper must also support a validation-only mode for existing local USER packets so governance/evidence packets assembled outside the build path can prove the same contract: mandatory timestamped ZIP, stable ZIP rejection, stale same-label ZIP absence, ZIP-beside-folder placement, duplicate ZIP entry rejection, folder/ZIP file-list plus content-hash parity, approved root `START_HERE.md` plus `USER Review` / `Review Aids` / `Source Truth Context` layout, exactly one primary USER review file, unresolved-placeholder scan, stale-stage scan, generated USER-facing technical metadata exclusion across root `START_HERE.md`, `USER Review`, and `Review Aids`, and final packet proof reporting. Active branch status, current commit, current baseline, validation status, PR state, worktree status, ZIP SHA256, ZIP hash, packet hash, upload hash, and similar byte-proof metadata belong in helper output, validator output, Codex chat digest, or external governance state instead of USER-facing files. A stale folder file, copied ZIP outside the packet folder parent, duplicate ZIP member names, stale same-name zip, stale same-label timestamped zip, missing timestamp in the upload ZIP filename, stale/tampered packet identity, missing applicable Branch Planning review file, zip/folder file-list or content-hash mismatch, unresolved placeholder, generated USER-facing technical metadata in root, USER Review, or Review Aids files, missing decision path, multiple primary review files, or unapproved packet layout is a helper failure and must block the review packet as `USER Review Packet Stale`.

Final ZIP validation must read the uploaded ZIP text back as the artifact of record, not rely only on the regenerated folder. For copied current Source Truth Context, validation must reject stale `Source Repo HEAD` values, branch-state / branch-plan current metadata disagreement, a copied current `USER Review ZIP` pointer that does not name the final ZIP, and any UDL row claiming closure for a defect still present in the final ZIP.

PR Readiness Stage 1 packet validation must distinguish the current gate from
planning support. When the current decision is Stage 1 analysis, the helper
must generate `USER Review/PR_READINESS_STAGE1_REVIEW.md` as the single primary
decision file, keep `USER_BRANCH_VISION_REVIEW.md` and
`USER_BRANCH_PLAN_REVIEW.md` in supporting context, and reject a structurally
parity-green packet that promotes the BP2 file as the Stage 1 primary. The
Stage 1 primary must expose the outcome, repair posture, transition-safety
proof, adversarial/false-green coverage, USER Gate State, and exact decision
supported. This classification is independent of USER acceptance: Reviewable
does not mean Accepted.

Final ZIP Source Truth Context validation must distinguish packet currentness modes. `active-review` packet validation and `next-gate` packet validation are live-current modes: copied current Source Truth Context must match live repo/external state for the gate being reviewed, including active `Source Repo HEAD` and current `USER Review ZIP` pointer checks. `accepted-historical` packet validation is snapshot-current mode for a packet already accepted by USER as reviewable evidence: the packet is an immutable historical receipt, copied context must be internally consistent and point to the accepted final ZIP, live external state must contain an acceptance receipt for that ZIP, but copied context is not required to byte-match later live external state or later live `HEAD` after acceptance recording or validator repair commits move the branch forward. Accepted historical mode validates the explicitly supplied timestamped ZIP as the artifact of record and must not require current local USER folder parity after a later next-gate packet regenerates that folder. Accepted historical mode must not mask defects present at generation/acceptance time, including pending-regeneration markers, missing concrete packet pointers, branch-state/branch-plan copied `Source Repo HEAD` disagreement, missing copied source heads, invalid artifact contents, or missing image proof. New next-gate packets must copy fresh live Source Truth Context instead of relying on copied context from a historical packet. Same-label ZIP cleanup and live-current validation must not preserve extra physical timestamped ZIPs in the active local USER hub, even when those ZIP names are recorded as accepted historical evidence; historical acceptance may remain in repo/external-state receipts and may be validated only when that exact historical ZIP is explicitly supplied under `accepted-historical` mode. Any extra same-label ZIP beside an active-review or next-gate packet is a stale artifact and must be removed or rejected.

Final ZIP visual-proof validation must also treat the uploaded ZIP as the artifact of record for screenshots and other image proof. When a live proof manifest or proof index references screenshot evidence, the validator must count actual image members in the final ZIP, compare manifest/index expected proof entries against ZIP members, open every ZIP image from ZIP bytes, report local-folder and ZIP image counts separately when relevant, reject manifest-only proof when screenshots are required, reject proof indexes that point only at local image paths, and reject any UDL `CLOSED_WITH_PROOF` status for image-proof defects while the final ZIP lacks the closure screenshots.

Review-bundle hygiene fixtures must cover packet false-green classes after each admitted incident. Current required false-green fixture classes include empty primary review files, stale active Review Aids, wrong primary/current decision-file references, pending copied external-state packet pointers, active-review stale copied Source Truth Context, accepted-historical packet post-acceptance drift pass, accepted-historical packet stale-at-generation failure, next-gate stale copied Source Truth Context failure, wrong packet-validation-mode failure, accepted packet no-regeneration-for-live-byte-match proof, new next-gate fresh copied context pass, active/next-gate rejection of recorded and unrecorded accepted-historical same-label sibling ZIPs, generation cleanup removal of recorded accepted-historical same-label sibling ZIPs, stale final-ZIP Source Truth Context, branch-state/branch-plan copied metadata disagreement, UDL false closure over stale ZIP context, folder-green/ZIP-stale copied context, cropped-only proof, duplicate full-desktop proof, missing required live-proof manifest checks, final ZIP with zero images despite screenshot manifest claims, manifest JSON with no screenshots included, proof index references to images missing from the final ZIP, local proof-folder images omitted from the final ZIP, UDL `CLOSED_WITH_PROOF` while ZIP closure screenshots are absent, and image openability that incorrectly opens local files instead of ZIP bytes.

## Integrated Repair Validation Families

### Active USER-Packet Identity Enforcement

`dev/orin_user_review_bundle.py` (`Helper Status: Reusable`) owns active-review
packet identity enforcement. Active-review validation must fail closed unless
the caller supplies expected branch, expected HEAD, and expected `origin/main`.
The helper must prove those values against the live checkout and against both
the packet folder and the timestamped ZIP independently. Folder/ZIP parity,
layout, or substantive reviewability PASS cannot mask a stale branch, HEAD,
baseline, copied Source Truth Context, or mismatched ZIP identity.

`accepted-historical` remains a separate mode: historical branch and commit
identity are preserved and internally consistent, but later Git advancement is
not a failure. Fixtures must cover positive active identity, wrong branch,
wrong HEAD, wrong baseline, omitted identity arguments, stale folder context,
stale ZIP context, folder/ZIP disagreement, and accepted-historical
preservation.

### Target-Scoped External-State Currentness

`dev/orin_external_state_validation.py` (`Helper Status: Reusable`) owns the
additive `--target-currentness` mode. The mode requires one explicit relative
target plus expected branch, source HEAD, origin/main, worktree path, slot ID,
and target SHA256. It must reject missing/duplicate/ambiguous targets,
absolute/off-root/traversal/alias paths, reparse or symlink escapes, missing or
malformed records, unsupported or historical record classes, identity
mismatches, stale hashes, target changes during validation, non-live `Record Role`
values, and historical boundaries that can redefine live authority.
Modern snapshot proof must fail closed when any child directory cannot be
enumerated, and its manifest, hashes, and physical inventory must retain one
pre/post snapshot-directory identity. Snapshot manifests require canonical UTC
`Last Updated` evidence; the manifest and every copied file must be rehashed after
inventory. Modern and legacy released-lock evidence must retain the parsed digest
through the final decision. Audit discovery must retain the same pre/post confined
directory identity and JSON filename inventory, then rehash every discovered file.
Target `Record Role` evidence must be affirmative and non-negated, while the
historical boundary must be unconditional and must reject retained, remaining,
persistent, continued, inherited, or receipt-sourced live authority.
Malformed-audit transition recovery must preserve an immutable root frame across
mismatched or repeated closers. Confined evidence reads must
reject files above the fixed byte limit before buffering, report memory/decode
resource exhaustion as validation issues, enforce the same fixed limit during
streaming hashes, and scan malformed objects after
invalid leading material for canonical target-set transitions.

Target output must identify the selected target and state explicitly that
`Target PASS Is Root-Wide PASS: NO`. The root manifest is reported as a
structural initialization/index anchor, not treated as a universal current
head. Existing global structural and strict modes retain their existing
semantics.

The target-currentness journal scan must fail closed when the initialized
external-state root has no `audit_log` directory; a removed or renamed audit
namespace is missing transaction evidence rather than an empty inventory.

The only approved current-projection writer is
`dev/orin_external_state_target_reconcile.py` (`Helper Status: Reusable`). It
must require the target pre-write hash, a matching locked write set, a
pre-write snapshot, explicit field assignments, atomic replacement, no-loss
comparison, post-write target validation, and an audit entry. Lock release is
owned by `dev/orin_external_state_lock_release.py` (`Helper Status: Reusable`);
a currentness transition with a remaining lock is incomplete and must fail
closeout. These helpers must not rewrite historical receipts or the structural
root manifest.

`dev/orin_external_state_lock_release.py` (`Helper Status: Reusable`) owns
atomic release of the exact admitted lock, and
`dev/orin_external_state_target_reconcile.py` (`Helper Status: Reusable`) owns
the target-scoped lock/snapshot/atomic-write transition proof, while
`dev/orin_external_state_target_currentness_fixture_validation.py` (`Helper Status: Reusable Fixture Validator`) owns the positive, negative,
malformed, stale, path-security, TOCTOU, writer, audit, lock-release, modern
transaction-journal, legacy completed-audit compatibility, and adversarial
mutation regression suite for this contract.

Legacy completed-audit compatibility is limited by the merge-stable registry
`dev/orin_external_state_legacy_receipt_compatibility.json` to exactly three
immutable pre-`Transaction State` receipts. Admission requires the normalized
relative audit path, raw SHA256, receipt class, immutable purpose, and exact
completion profile to match one registry row before shape, lock, workload,
snapshot, target, and completion evidence can pass. `canonical-complete`, loose
Complete/PASS values, old-looking filenames, age, copied bytes at another path,
or a path match with altered bytes are not production compatibility identities.
Journal discovery parses JSON and selects only an exact
`Transition == "Bounded coherent target-set reconciliation"`; malformed JSON
that explicitly declares that field fails closed, while phrase-only notes or
other fields do not select an unrelated audit. Candidate journals, the
compatibility registry, nested target rows, and supporting lock/snapshot
objects use duplicate-aware JSON decoding. Repeated object names fail closed;
case-ambiguous names for the same security-critical semantic field fail closed;
and no first-value-wins or last-value-wins interpretation is permitted. Journal
selection, validation, and raw SHA256 binding must operate from one byte read so
the parsed document cannot differ from the bytes whose identity is checked.

Required target fixtures include valid live projections; branch/head/baseline,
worktree/slot, hash, missing-target, duplicate/alias, traversal,
absolute/off-root, reparse/symlink, malformed, unsupported-record,
historical-receipt, multiple-source-head, stale-root-manifest,
changed-during-validation, unchanged-global-mode, and scoped-PASS-versus-root-
PASS cases.

## Branch Planning Review Gate Validation Invariant

Branch Planning helpers and validators must distinguish packet reviewability from USER acceptance. `dev/orin_user_review_bundle.py` (`Helper Status: Reusable`) may report a BP1, BP2, or BP3 packet as generated, structurally valid, and reviewable while the USER gate remains pending. That reviewability state is evidence only and must not be converted into `USER Accepted`, `USER Approved`, `USER Waived`, Workstream implementation readiness, or implementation approval.

Reusable helper and fixture coverage must enforce the two-axis state model from `Docs/phase_governance.md`:

- `Packet Reviewability State`: `Missing`, `Generated`, `Validation Failed`, `Reviewable`, `Stale`, or `Superseded`.
- `USER Gate State`: `Pending USER Review`, `USER Revision Requested`, `USER Accepted`, `USER Approved`, `USER Waived`, `USER Rejected`, `USER Blocked`, or `Superseded`.

BP2 preparation requires BP1 `USER Gate State: USER Accepted` or `USER Gate State: USER Waived`. BP3 preparation requires BP2 `USER Gate State: USER Accepted` or `USER Gate State: USER Waived`. A first Workstream implementation recommendation requires BP1 accepted/waived, BP2 accepted/waived, BP3 approved/waived, and a separate USER implementation approval path. Packet validation output must show reviewability status and USER gate status separately, and it must report implementation-readiness separately from implementation approval. If a helper, validator, branch record, branch plan, or Codex digest treats `Packet Reviewability State: Reviewable` as USER acceptance, it must fail on `Packet Validation Treated As USER Acceptance` or `Review Gate Bypass`.

Generated USER-facing BP1/BP2/BP3 files and extra USER review aids must be scanned for stale wrong-family, wrong-phase, placeholder, prior-gate-pending, or implementation-ready wording. Copied source-truth files are context and must be classified separately from generated USER-facing files. Fixture coverage must preserve regressions for stale packet output, wrong-family packet output, BP1-pending output, BP2-placeholder output, BP2-pending output misclassified as BP3, BP3 output misclassified as implementation-ready, and Codex digest text that conflicts with the uploaded USER packet.

FAM-006 Recording BP3 packet generation is a reusable review-bundle guard. When the active branch is `feature/fam-006-dashboard-recording-start-stop-local-file` and USER accepts BP2 Option C, `dev/orin_user_review_bundle.py` (`Helper Status: Reusable`) must classify the packet as BP3 `Workstream Entry / Orchestration Validation`, make `WORKSTREAM_ENTRY_ANALYSIS_DIGEST.md` the primary USER review file, keep `USER_BRANCH_PLAN_REVIEW.md` as accepted BP2 context, validate Dashboard Recording Card / Recording Studio / minimal Log Viewer shell / native-export boundary / open-folder pre-session usability / issue #258 target reliability / deferred carryforward / Element-to-Phase / rollback / validation / H1 / Live Validation / UTS / visual-system inheritance / branch-sprawl / slice-SLC-seam sequencing, and block stale Workstream-final or implementation-approved wording until USER separately approves bounded Workstream implementation. Generated support files and review aids for this BP3 packet must also reject stale BP2-active wording such as `BP2 remains Pending USER Review`, `BP2 must answer whether Option C`, or `BP3 may be prepared only after BP2 is accepted or waived`. `dev/orin_branch_readiness_planning_fixture_validation.py` (`Helper Status: Reusable`) must retain a FAM-006 BP3 packet-generation fixture for this exact guard.

FAM-006 Workstream implementation approval review packet generation is a distinct reusable review-bundle guard. After USER accepts FAM-006 BP3 Option C, `dev/orin_user_review_bundle.py` (`Helper Status: Reusable`) must be able to generate a reviewable but still pending Workstream/runtime implementation approval packet without classifying it as BP3-pending or implementation-ready. The packet must classify as `workstream-implementation-approval-review`, keep `WORKSTREAM_ENTRY_ANALYSIS_DIGEST.md` as the primary USER review file, keep `USER_BRANCH_PLAN_REVIEW.md` as accepted BP2/BP3 context, keep `USER_BRANCH_VISION_REVIEW.md` as accepted BP1 context, name the whole admitted Option C package, preserve the bounded continuation rule that a green first seam is not package completion, and keep Workstream/runtime implementation blocked until USER approves the packet. It must reject FAM-007 fallback wording, first-seam-only authority, stale BP1 Draft / Pending USER Review wording, stale BP2/BP3 pending language, and support-aid text that says BP1 must still be accepted before BP2, BP2 remains pending, BP3 remains pending, or Workstream Entry remains pending. `dev/orin_branch_readiness_planning_fixture_validation.py` (`Helper Status: Reusable`) must retain a FAM-006 Workstream approval-review fixture for this guard and inspect generated support aids, not only the primary decision file.

## External State Transition Validator Hook

The External Operational State Store transition started as source-truth contract work, added helper/bootstrap scaffolding, initialized the local external root after separate USER approval, added Stage 4A report-only repo live-state leakage scanner / migration-map helper support, completed Stage 4B active-state migration planning packet work, landed Stage 4C active-state migration execution planning through PR #229, completed Stage 4 active-state migration execution externally at source repo HEAD `5abdd9c011c80f5b7b57d473b973654a2427d5a8`, landed Stage 5 validator transition through PR #230, landed Stage 6 cleanup planning through PR #231, landed Stage 6A compact pointer-surface cleanup through PR #232, landed Stage 6B branch-authority routing cleanup planning through PR #233, landed Stage 6C branch-authority routing cleanup through PR #234, and landed Stage 6D branch-detail-record / branch-plan cleanup planning through PR #235. It is now in Stage 6E no-loss cleanup closure. Repo validators remain clean-clone safe and must not require `C:\Nexus Governance State`.

Future validator hardening should extend the existing reusable governance validators before adding a new root helper:

- `dev/orin_branch_governance_validation.py` remains `Helper Status: Reusable` and should own marker-first PR Readiness checks for `External State Transition Gate:`, `Transition Stage:`, `Docs Split Target Matrix Status:`, `Active-State Owner Boundary:`, `External Root Approval:`, `External Root Status:`, `Premature Migration Scan:`, `Repo Live-State Leakage Scan:`, `Validator / Helper Transition Status:`, `Source-Truth Agreement:`, and the blockers `External State Transition Gate Missing`, `External State Transition Drift`, `Docs Split Target Matrix Missing`, and `External State Migration Premature`. It must also scan every tracked `Docs/branch_records/*.md` detail file, not only records listed in `Docs/branch_records/index.md`, for non-standing repo live-state leakage such as active branch authority markers, active transition waivers used as current state, active lifecycle `Record State:` or `Status:` rows, live phase markers, active next-gate posture, active `Next Legal Phase Gate:`, `Stage 2 Green-Light Decision Needed:`, `PR Readiness Stage 2 Green-Light Decision Needed:`, `Exact USER Approval Text:`, `Exact USER Decision Needed:`, `Exact Next USER Decision Needed:`, `Pre-PR Live State: No live PR`, `PR Creation Approval: Pending USER approval`, or `Stage 2 PR Creation: Pending USER approval` rows that have not been folded into explicitly historical receipt wording, live PR/open-review state, selected-next posture, worktree assignment, release-window state, or active branch-plan ledger ownership. Unindexed branch records with live-state markers must fail as `Repo Branch Record Live-State Leakage`.
- `dev/orin_governance_efficiency_validation.py` remains `Helper Status: Reusable` and should own contract consistency checks for the External State Transition Drift Gate, Docs Split Target Matrix presence, Stage 0 versus helper/bootstrap/migration boundary, and Main / phase governance / governance efficiency / external plan routing agreement.
- External-state helpers are registered below as no-mutation-by-default scaffolds unless an explicit USER-approved apply path is used. They may report, validate missing-root posture, preview initialization, preview lock/snapshot/promotion/fold-down packets, scan repo live-state leakage, print migration maps, support active-state migration planning packets, support active-state migration execution planning packets, validate the approved Stage 4 migrated record set during local workflows, support Stage 6 cleanup planning evidence, support Stage 6A compact pointer cleanup evidence, support Stage 6B branch-authority routing cleanup planning evidence, support Stage 6C branch-authority routing cleanup evidence for `Docs/branch_records/index.md`, support Stage 6D branch-detail-record / branch-plan cleanup planning evidence, support Stage 6E no-loss cleanup closure evidence, and run smoke checks. They must not create worktree-local staging, move/delete/archive repo files, edit branch detail records or branch plans during Stage 6E except the standing Governance intake record, execute broader cleanup edits, or make `C:\Nexus Governance State` mandatory for clean-clone repo validation without later explicit USER approval and an applied command path.

Backlog taxonomy and AI-native placement validator guidance: extend existing governance validators before adding a new helper. `dev/orin_branch_governance_validation.py` should own marker-first checks for `Backlog Taxonomy And Source-Truth Placement Gate:`, concept classification values, rejected owner classes, existing-owner reuse, `No Existing Owner Fits`, AI Operational Cache Governance placement, the `Docs/ai_runtime_and_trust_architecture.md` owner route, and blockers `Backlog Taxonomy Gate Missing`, `Backlog Addition User Approval Missing`, `AI Runtime Trust Architecture Route Missing`, and `Cache Governance Misclassified As Memory` when those checks become machine-checkable. `dev/orin_branch_readiness_planning_fixture_validation.py` remains `Helper Status: Reusable` and should own fixtures proving valid AI-native taxonomy classification, invalid new-FAM-by-inertia classification, valid cache-as-architecture/policy/runtime placement, invalid cache-as-memory placement, valid existing-owner reuse, and valid AI-runtime/trust architecture citation before any future Workstream can rely on AI-native/cache source-truth placement.

Vision Contract validator guidance: extend existing reusable governance validators and review-bundle helpers before adding a new root helper. `dev/orin_branch_governance_validation.py` should own marker-first checks for `Vision Carrydown Chain:`, `Project Vision Context:`, `NDAI Product Experience Contract Comparison:`, `Reference / Template / Primitive Classification:`, `Accepted Reference Set / Comparative Synthesis:`, `Family Vision Context:`, `Feature Vision Context:`, `Branch Vision Contract Snapshot:`, `Vision Contract Required:`, `Family Feature Vision Required For Selected Feature`, `Feature Vision Sufficiency Check`, `Family Feature Vision Not Applicable`, `Vision Proof Alignment:`, and USER packet `PASS / FAIL / BLOCKED / UNPROVEN` vision evidence disposition when those checks are machine-checkable. `dev/orin_branch_readiness_planning_fixture_validation.py` should keep fixtures for weak FFV, slice-specific FFV, missing FFV, pointer-stale FFV, BP1 branch-local invention, BP2 product-direction drift, BP3 implementation-ready without accepted vision, missing deterministic/intuitive/immersive/predictable/reliable/consistent comparison, template/shared-primitive claims without approved implementation source, reference-derived UI work without accepted reference set comparison, and Live Validation proof that lacks vision-vs-observed-behavior comparison. `dev/orin_user_review_bundle.py` should reject BP1/BP2/BP3/LV/RAR review packets that omit the applied vision layers, selected/deferred FFV elements, user-facing surfaces, the NDAI Product Experience Contract comparison when required, reference/template/primitive classification when UI reuse is claimed, evidence paths, or PASS/FAIL/BLOCKED/UNPROVEN disposition when those artifacts are required by phase governance.

Reference Standard lifecycle validator guidance: future helper/validator coverage should extend existing governance and review-bundle helpers before adding new root tools. Coverage should fail or warn when a packet omits `Reference Standard Review:`, claims a standard was created without USER source-truth promotion, treats a Vision Contract as a Reference Standard, treats a Reference Standard as an Implementation Template, treats a Template as a Shared Primitive, treats screenshot/helper/validator evidence as a standard, enters PR Readiness Stage 1 without reference-effectiveness review, or reaches three same-class repair cycles without a `Reference Effectiveness Note` and `Reference Standard Repair Candidate`. New non-UI reference families should remain planning candidates until repeated evidence, clear owner routing, USER promotion approval, and an adoption path exist.

Vision detail / UI immersion / anti-circular proof validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new root validators. `dev/orin_branch_governance_validation.py` should eventually check for `Vision Contract Product Detail Missing`, `NDAI Window Chrome Missing`, `Default OS Chrome Used Without Exception`, `Nexus-Owned Surface Classification Missing`, `Platform Exception Unclassified`, `FAM-002 Component Grammar Missing`, `Circular Validation Detected`, `Claim Proven By Own Plan`, `Independent Evidence Missing`, `Claim Class Missing`, `Evidence Class Missing`, `Proof Strength Overstated`, `Supporting Evidence Treated As Direct Proof`, `Plan-Only Proof`, `Marker Or Schema Proof Misclassified`, `Screenshot Path Treated As Visual Acceptance`, `Live Validation Comparison Missing`, `USER Validation Escalation Missing`, and `Merged Vision Standard Adoption Missing` when the relevant source-truth markers make those defects machine-checkable. `dev/orin_branch_readiness_planning_fixture_validation.py` should add fixtures for procedural-only vision files, default native title-bar UI accepted without exception, platform exception accepted without reason, BP2/BP3 plans that prove a claim only by restating the plan, BP2/BP3 proof matrices that omit claim class or minimum proof strength, helper-green / marker-green / screenshot-path-only proof misclassified as direct proof, and re-entering branch packets that ignore newly merged vision/UI/proof standards. `dev/orin_user_review_bundle.py` should surface product-detail, window-chrome, surface-classification, platform-exception, claim/evidence-class, proof-strength, and evidence-independence gaps in USER review packets without turning raw screenshots, manifests, or external operational state into repo ledgers.

Pre-Live Visual Purpose Conformance validator guidance: reusable helpers must extend existing governance, branch-planning fixture, review-bundle, and branch-local UI validators before adding a new root validator. `dev/orin_branch_governance_validation.py` and branch-local UI validators should recognize the `Pre-Live Visual Purpose Conformance Gate` from `Docs/phase_governance.md` as a required precondition for visible user-facing UI, window, control, visual-state, layout, or interaction-surface Live Validation. Helpers must fail closed when a Live Validation or UTS handoff lacks visual-purpose rows for each material visible element group; lacks accepted Project Vision / Family Vision / Family Feature Vision / Branch Vision / UIREF-reference comparison; treats screenshot existence, manifest PASS, helper PASS, DOM markers, runtime markers, CSS-marker similarity, or code assertions as visual conformance; runs functional interaction proof before visual-purpose conformance is adjudicated; or leaves any applicable row as `REPAIR`, `BLOCKED`, `UNPROVEN`, missing, or manually elevated without returned USER `PASS` / `WAIVED_WITH_REASON`. The expected proof order is source-truth purpose contract, recorded photo/video element-group visual adjudication, accepted reference/template/primitive comparison, state/affordance visual adjudication, then functional interaction proof. `dev/orin_user_review_bundle.py` should surface the gate in Live Validation / UTS packets when visible UI is in scope.

Visual Acceptance Target validator guidance: reusable helper/fixture coverage must recognize the `Visual Acceptance Target Gate` from `Docs/phase_governance.md` and the branch-plan fields from `Docs/branch_plans/README.md`. `dev/orin_branch_readiness_planning_fixture_validation.py` owns current regression coverage for Visual Acceptance false-green planning packets and RAR-style review packets; branch-local validators may add stricter product-specific checks. Helpers must fail closed when a material visible UI/UX implementation route proceeds without `Visual Acceptance Target Plan:`, render authority classification, reviewable Design Candidate Render or accepted target evidence when USER selection is needed, state/contact-sheet coverage, element legends, visual selection ledger, rejected-pattern ledger, source-truth conflict classification, USER target decision state, implementation authority classification, visual family relation proof, functionality role contract, Pre-Live visual purpose conformance plan, and later Implementation Match Proof. `dev/orin_user_review_bundle.py` should surface visual-target packets and reject packets that use vague `better / closer / improved / looks good enough` language, local-path-only media, missing embedded renders, stale ZIP proof, concept renders treated as accepted targets, or implementation-match claims unsupported by actual runtime screenshots/video. Machine-checkable coverage targets include `Visual Acceptance Target Missing`, `Visual Acceptance Target Not Reviewable`, `Concept Render Misclassified As Target`, `Design Candidate Media Missing`, `Visual Target State Coverage Missing`, `Visual Selection Ledger Missing`, `Rejected Pattern Ledger Missing`, `Render Authority Level Missing`, `Implementation Started Before Visual Acceptance`, `Implementation Match Proof Missing`, `Implementation Match Proof Not Compared`, `Source-Truth Visual Conflict Unclassified`, `Vague Visual Acceptance Language`, `Visual Target Packet Hygiene Missing`, `Visual Target USER Decision Missing`, `Visual Family Relation Proof Missing`, `Implementation Authority Classification Missing`, `Accepted Reference Not Compared`, `Reference-Derived Parity Unproven`, `Template Claim Unsupported`, `Shared Primitive Claim Unsupported`, `One-Off Implementation Overclaimed`, `Functionality Role Contract Missing`, `Role-Ambiguous Surface Unrouted`, `Packet Reviewability Treated As Product Acceptance`, `Screenshot Path Treated As Visual Acceptance`, `Helper Green Treated As Visual Acceptance`, `CSS Similarity Treated As Visual Family Proof`, and `Implementation-First Visual Target Backfill`.

Returned UTS failure routing validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding a new root validator. Helpers and phase digests must treat a returned USER Test Summary result of `USER Failed`, `FAIL`, `REPAIR`, or equivalent USER rejection as a legal route back to bounded Workstream repair for the failed accepted scope, followed by renewed H1/Hardening, renewed exact-path Live Validation, and regenerated UTS. Helpers must not classify a returned UTS failure as a simple LV retry, PR Readiness-ready state, post-Hardening green state, or USER acceptance. During the returned-UTS Workstream repair loop, bounded Hardening checks may run alongside repair seams/slices as inspection/stabilization support, but they do not replace the required final renewed H1/Hardening and renewed Live Validation after repair. Coverage should fail closed on `Returned UTS Workstream Repair Missing`, `Returned UTS Renewed H1 Missing`, `Returned UTS Renewed LV Missing`, and `Returned UTS Renewed UTS Handoff Missing` when those markers are machine-checkable.

Rebaseline Adoption & Reconciliation validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding a new root validator. `dev/orin_branch_governance_validation.py` should eventually check phase packets and active external branch-plan summaries for `RAR Stage:`, `Trigger Reason:`, `Source-Truth Files Loaded:`, `Incoming Standard / Change Summary:`, `Owned Surface Inventory:`, `Affected File Inventory:`, `Affected Surface Inventory:`, `Code-To-Visual Trace Matrix:`, `Reference / Template / Primitive Classification:`, `Accepted Reference Set / Comparative Synthesis:`, `Accepted Reference / Template / Primitive Comparator Matrix:`, `NDAI Product Experience Contract Comparison:`, `UI Element Inventory:`, `Backend / State Ownership Trace:`, `Screenshot / Video / Contact-Sheet Evidence:`, `Visual Element / Element-Group Inspection Ledger:`, `Vision-To-Proof Matrix:`, `Scope Coverage Manifest:`, `Owned-Surface Nonconformance Ledger:`, `Current Branch Active Repair Ledger:`, `Current Branch Repair Candidates:`, `Previous / Historical Branch Issue Candidates:`, `Historical / Previous Branch Issue Candidate Ledger:`, `Issue-Candidate Table:`, `Repair / Waiver / Defer / Route Decision Table:`, `Validation Summary:`, `USER Packet Path:`, `USER Packet ZIP Path:`, `Exact Next USER Decision:`, and `No Repo Live-State Tracking:` when a merged standard applies. `dev/orin_user_review_bundle.py` should validate RAR USER packets for deterministic clean folder regeneration, timestamped ZIP parity, one primary review file, stale-stage absence, readable code-to-visual / reference-template-primitive classification / accepted-reference-set / NDAI product-experience comparison / issue-candidate / USER-decision tables, and screenshot/video/contact-sheet evidence or explicit proof-gap rows. It should reject RAR packets that are marker-only, summarize whole windows without element-group rows, omit either the current-branch repair ledger or historical/previous-branch issue-candidate ledger, use FAM-owned surfaces as circular comparators without classification, claim visual acceptance from screenshot existence instead of visual adjudication, or claim RAR green while any material owned surface is `PARTIAL`, `UNPROVEN`, `NONCONFORMING`, `EXCEPTION NEEDED`, `ISSUE CANDIDATE`, `SOURCE-TRUTH GAP`, `REFERENCE GAP`, `TEMPLATE GAP`, or `SHARED PRIMITIVE GAP` without a legal disposition. `dev/orin_branch_readiness_planning_fixture_validation.py` should add fixtures where a branch repairs one sample defect while claiming all UI green, omits previous-branch issue candidates, omits current-branch repair candidates, collapses visible element groups into one broad surface row, treats a future template/shared primitive as current proof, claims AI Control Center parity without UIREF comparison, claims template usage when only UIREF reference grammar exists, claims shared primitive usage without an approved code source, performs reference-derived UI work without comparative synthesis, omits deterministic/intuitive/immersive/predictable/reliable/consistent comparison, records active RAR rows in repo docs, uses screenshot-path/helper-green/validator-green evidence as visual acceptance, provides attractive screenshots with no code-to-visual trace, omits backend/state ownership for a visible status claim, ignores duplicate screenshot/hash proof, leaves owned surfaces `UNPROVEN`, or advances toward PR Readiness while RAR blockers remain active. Coverage targets include `Rebaseline Adoption Review Missing`, `Merged Standard Impact Ledger Missing`, `Affected Surface Inventory Missing`, `Owned Surface Nonconformance Ledger Missing`, `Reference / Template / Primitive Classification Missing`, `Accepted Reference Comparator Missing`, `Accepted Reference Set Missing`, `Comparative Synthesis Missing`, `NDAI Product Experience Contract Comparison Missing`, `Product Experience Contract Nonconformance Unresolved`, `Accepted Reference Conflict Unrouted`, `Template / Shared Primitive Missing`, `Template Claim Unsupported`, `Shared Primitive Claim Unsupported`, `Reference-Derived Parity Unproven`, `Template Gap Unrouted`, `Shared Primitive Gap Unrouted`, `Code-To-Visual Trace Missing`, `Backend State Ownership Trace Missing`, `Screenshot Contact-Sheet Proof Missing`, `Circular Validation Evidence`, `Visual Comparator Matrix Missing`, `Current Branch Repair Vs Issue Boundary Missing`, `Owned Surface Issue Candidate Missing`, `RAR USER Packet Missing`, `Reference Parity Claim Unsupported`, `Source-Truth Gap Unrouted`, `Reference Gap Unrouted`, `Unproven Owned Surface`, `RAR Shallow Audit`, `RAR Ledger Pair Missing`, `RAR Scope Coverage Manifest Incomplete`, `RAR Element Group Rows Missing`, `RAR Current Repair Ledger Missing`, `RAR Historical Issue Candidate Ledger Missing`, `RAR Sampling Justification Missing`, `RAR Comparator Classification Missing`, `RAR Row Disposition Missing`, `RAR Live Adoption Ledger In Repo`, `RAR Evidence Lost To Chat Digest`, and `RAR Packet Missing For USER Judgment`.

RAR issue-candidate durability validator guidance: `dev/orin_rar_issue_candidate_durability_validation.py` (`Helper Status: Reusable`) is a read-only helper for RAR issue-candidate durability checks. It must not create, close, label, edit, or comment on GitHub issues and must not write external operational state. It validates the expanded issue-candidate decision surface, stable candidate IDs or lineage fingerprints, exact-token candidate carry-forward, duplicate/regrouped lineage conflicts, predecessor/successor mapping for renamed candidates, durable disposition vocabulary, packeted-only false closure while allowing historical packeted-only repair explanations, non-blocking carry-forward owner/reason/carrier-or-route/trigger requirements, USER rejection reason requirements, USER waiver reason/scope requirements, deferred/routed receipt requirements, GitHub mapping freshness posture from a deterministic read-only snapshot when mapped issue rows are present, closed-issue reconciliation evidence, repaired-and-verified evidence, and active USER packet presence for externally recorded candidates when a packet folder and external ledger are supplied. Packet validation must require active issue-candidate rows in the primary `USER Review` decision surface or a `START_HERE`-routed primary issue-candidate decision file; tables present only in `Review Aids`, copied context, historical files, helper output, or chat digest fail the active gate. `dev/orin_branch_readiness_planning_fixture_validation.py` owns positive, negative, and generated packet/ledger/GitHub-snapshot fixtures proving active external-candidate disappearance failure, eight-candidate carry-forward success, secondary-review-aid-only failure, primary-surface success, historical packeted-only explanation success, current packeted-only failure, USER waive/reject reason failures, non-blocking carry-forward field failures, mapped-open/mapped-closed snapshot mismatch failures, closed mapping with independent reconciliation success, terminal historical absence success, active absence failure, renamed-without-lineage failure, renamed-with-lineage success, and no-target/helper invocation not being counted as durability validation. Coverage targets include `RAR Issue Candidate Durability Missing`, `RAR Issue Candidate Lineage Missing`, `RAR Issue Candidate Durable Disposition Missing`, `RAR Issue Candidate Disappeared From Active Packet`, `RAR Issue Candidate Packeted Only`, `RAR GitHub Issue State Unknown`, `RAR GitHub Issue Mapping Stale`, and `Issue Candidate Table Only In Copied Context`.

PR review hardening for the same helper must preserve documented durable-disposition labels as accepted aliases for compact internal enum values, reject repeated `Candidate ID` rows unless all durable row fields are byte-equivalent, and treat `START_HERE`-routed dedicated issue-candidate decision files as primary USER decision surfaces even when they are not physically under `USER Review`. Generated fixtures must prove documented-label success, conflicting duplicate-disposition failure, and routed-primary decision-file success before another PR review request relies on the helper.

The same PR hardening must also reject `Review Aids`-only issue-candidate tables that share a basename with a missing `START_HERE` primary target, and must allow recognized legacy 8-column external RAR issue-candidate ledgers to be carried into the current primary packet without requiring the legacy external ledger itself to already use the expanded 12-column USER decision surface.

Packet/ledger parity checks must reject copied-context-only issue-candidate tables and must compare externally carried active candidate IDs against parsed primary decision rows or explicit predecessor/successor lineage text, not incidental prose mentions in the packet.

`START_HERE` routing may name a dedicated primary issue-candidate decision file outside `USER Review`, but it must not promote files under `Review Aids` or `Source Truth Context` to primary decision authority.

When a packet has a valid primary issue-candidate decision surface, active issue-candidate rows that also appear under `Review Aids` or `Source Truth Context` must either appear as parsed rows in the primary decision surface or be linked by explicit lineage text; incidental mentions and supporting-only rows do not preserve active candidate durability.

External-ledger carry-forward must compare parsed primary rows by candidate ID plus owning FAM, surface, element group, and defect lineage. Reusing the same `Candidate ID` for different lineage details without explicit predecessor/successor lineage remains a disappearance/regrouping failure.

Issue Candidate Consolidation validator guidance: future coverage should keep atomic issue-candidate durability and add grouping checks before GitHub issue creation is requested. Helpers should fail or warn on `Issue Candidate Consolidation Missing`, `GitHub Issue Group Without Atomic Traceability`, `Issue Candidate Grouping Too Granular`, `Issue Candidate Grouping Too Broad`, `Issue Group Missing USER Choice`, `Issue Group Missing Proposed Carrier`, `Issue Group Missing Evidence Summary`, or `Issue Mutation Requested Before Consolidation`. Grouping validation must not mutate GitHub and must not convert repo docs into live issue ledgers; group rows belong in the primary USER decision packet, active external state, helper output, or Codex digest until USER approves a GitHub action or fold-down receipt.

RAR enforcement fixture coverage is active in `dev/orin_branch_readiness_planning_fixture_validation.py`. The fixture seam must preserve valid RAR adoption reviews for pending USER issue-candidate review, active RAR packets with canonical active RAR stage values and conservative negated green/phase disclaimer wording, resolved no-impact normal-phase continuation, and completed USER-reviewed issue-candidate disposition, plus negative fixtures for marker-only RAR, unanchored/fake resolved or no-impact RAR stage wording, missing code-to-visual trace, header-only code-to-visual trace, header-only accepted-reference comparator table, missing markdown table separators between required headers and rows, malformed, sparse, or over-wide code-to-visual and accepted-reference rows, unresolved nonconformance claimed green, unresolved USER-facing code-to-visual rows with no deterministic USER packet proof, issue-candidate row claimed green, current issue-candidate row omitted from the issue-candidate table, current issue-candidate wording embedded inside a larger status phrase, placeholder/TBD issue-candidate rows, missing previous/historical issue-candidate disposition, historical issue-candidate prose hidden behind `none reviewed yet` wording, pending USER review with no deterministic USER packet path/ZIP, active USER review gate with no deterministic USER packet path/ZIP, bare `RAR3` stage wording with no deterministic USER packet path/ZIP, active USER review gate contradicted by stale no-USER-decision wording and no packet, active USER review with a timestamped ZIP outside `C:\Nexus USER`, ZIP marker text that merely mentions `C:\Nexus USER` while the actual timestamped ZIP path is outside the USER root, suffixed non-ZIP paths such as `.zip.tmp`, traversal ZIP paths that canonicalize outside `C:\Nexus USER`, missing USER review/waiver/repair/route disposition, negated issue-candidate disposition wording, required-future USER review misclassified as completed disposition, normal phase progression claimed while an active RAR stage or RAR review/repair/issue-candidate posture remains active, generic normal phase progression claims in RAR disposition text, and concrete phase advancement such as PR Readiness while RAR remains active even when `Next Legal Phase:` is omitted. Unresolved RAR green checks must parse actual ledger/table status cells instead of scanning explanatory prose, so packets that say closed phrases such as `no UNPROVEN rows remain` or conservative phrases such as `not all adoption checks are green` are not falsely rejected while real `NONCONFORMING`, `UNPROVEN`, `PARTIAL`, `EXCEPTION NEEDED`, `SOURCE-TRUTH GAP`, `REFERENCE GAP`, `TEMPLATE GAP`, `SHARED PRIMITIVE GAP`, or `ISSUE CANDIDATE` row statuses still block green claims until repaired, waived, routed, reviewed, or dispositioned. Resolved or no-impact RAR closeout must also reject unresolved status rows even when no separate green/closed phrase is present; a `RAR Stage:` value of `Resolved` or `No Applicable Impact` cannot coexist with unresolved code-to-visual or accepted-reference rows. RAR rows with unresolved status cells and USER-facing next-action cells must require deterministic USER packet path/ZIP proof even when summary markers avoid `pending USER` wording. RAR green-claim detection must include equivalent closed/green wording such as `Green With Evidence`, `Resolved With Evidence`, `all RAR checks are green`, and similar RAR/adoption closed claims rather than only one exact phrase, while ignoring negated non-green wording. RAR stage parsing must be anchored to canonical `RAR0` through `RAR4`, `Resolved`, or `No Applicable Impact` starts; fake values such as `Not Resolved...` or `No Applicable Impacted...` must not count as resolved/no-impact stage proof. RAR table parsing must not drift into the next table when a required table has only a header and separator, must reject rows before the required markdown separator, and must reject malformed, over-wide, or placeholder-shaped rows before they can count as evidence. Code-to-visual trace and accepted-reference comparator tables must both contain real rows with the exact expected column shape and every expected evidence cell populated before RAR evidence can be treated as reviewable. Issue-candidate rows must contain concrete candidate, surface, element-group, defect, evidence, and proposed-carrier cells before they can satisfy USER-review disposition requirements; issue-candidate status detection must match the status cell even when the phrase is preceded by words such as `current` or `unresolved`. Active USER review classification must recognize active RAR3 USER Review Gate wording, bare `RAR3` stage wording, USER-facing unresolved row actions, and USER-reviews decision text, not only the exact phrase `pending USER`; stale contradictory no-decision wording cannot suppress a detected active USER review gate. Active USER review packet ZIP proof must parse the actual ZIP path, canonicalize or reject traversal segments before checking the root, require exactly one timestamped ZIP path for the marker, require that path to be rooted under `C:\Nexus USER`, and reject `.zip` as a prefix when the marker names a suffixed non-ZIP path. Previous/historical issue-candidate parsing must distinguish an explicit no-candidates value from prose that names a candidate and says none has been reviewed. Phase advancement checks must reject actual advancement requests while active RAR remains unresolved, but must not treat negated disclaimers such as `does not authorize PR creation, merge, or release` as advancement. When Codex Connector PR review identifies a validator seam defect, the same-PR repair must perform comment-family expansion before re-requesting review: classify sibling failure modes, add or update negative fixtures for the whole family where in scope, and validate the exact fixture family instead of patching only the single literal review example. This fixture coverage is source-checking support only: it does not mutate FAM worktrees, create active external-state ledgers, generate USER packets, or create GitHub issues. It exists so rebaselined worktrees cannot rely on helper/validator green, a single visible repair, chat-only issue routing, case-sensitive status wording, malformed table rows, missing table separators, over-wide rows, sparse evidence rows, placeholder issue-candidate rows, embedded issue-candidate status wording, bare RAR3 wording, vague handle-later issue-candidate text, negated disposition text, required-future disposition text, contradictory no-decision text, missing review packets, omitted phase markers, adjacent table drift, empty comparator tables, off-root ZIP paths, root-mention-only ZIP marker text, suffixed non-ZIP marker text, traversal ZIP paths, unanchored/fake resolved stage wording, unresolved rows under resolved/no-impact RAR closeout, or premature phase wording as proof that RAR was completed.

RAR parser precision also requires local ordering and negation-scope rules. First, explicit no-candidate wording such as `no issue candidate is applicable` or `no issue candidates are applicable` must be recognized before candidate-ID matching so valid no-impact RAR packets do not fail by treating `issue candidate is` as a concrete candidate. Second, RAR green-claim negation must be scoped to the detected green phrase itself; unrelated negation elsewhere in the same disposition, such as `not closed` before a separate `Adoption Green With Evidence` claim, cannot suppress an affirmative green claim when unresolved ledger rows remain. Third, issue-candidate durability parsing must distinguish `No current USER decision is needed because <affirmative proof or receipt>` from a missing proof/receipt statement: no-decision wording may negate only the need for a new USER decision, not the following independent verification, waiver reason/scope, or carrier acceptance receipt. Pending, missing, not-recorded, not-verified, or future proof/receipt wording must still fail.

Review-churn budget fixture coverage must model the same OR-trigger contract used by the live PR helper: a root-cause receipt is required when total Codex Connector comments exceed the total budget OR when any same-family count exceeds the same-family budget. Total-only and same-family-only budget overruns are both legal receipt cases; within-budget rows must not require or accept a root-cause receipt by inertia.

Required USER review wording is packet-gated even when the packet does not use the exact phrase `pending USER review`. Active RAR adoption packets that say USER review is required, USER must review, USER judgment is required, USER visual judgment is required, USER adjudication is required, route selection needs USER judgment, or equivalent USER-decision wording must provide deterministic USER packet path and timestamped ZIP proof before reviewability can pass. Unresolved RAR green-claim detection must scan the RAR disposition and summary marker family, including validation summary, repair/blocker, issue-candidate disposition, decision-table, current-violation, merged-standard, and exact-next-decision text, so green claims cannot move from `Adoption Disposition:` into a neighboring marker to bypass unresolved ledger rows.

RAR USER packet folder proof must parse the actual declared `USER Packet Path:` folder value and require exactly one rooted `C:\Nexus USER\<worktree-label>` packet folder. A marker that merely contains the text `C:\Nexus USER`, uses a lookalike root such as `C:\Nexus USERX`, declares a non-USER-root folder with a later explanatory USER-root mention, or includes multiple competing absolute packet paths is invalid even when the ZIP marker is otherwise deterministic.

The `USER Packet Path:` marker must point to the packet folder, not the upload ZIP. A timestamped `C:\Nexus USER\<worktree-label>-YYYYMMDD-HHMMSS.zip` value belongs only in `USER Packet ZIP Path:`, must use the same `<worktree-label>` as the rooted packet folder, and must not satisfy folder-path proof by prefix matching. Green-claim negation remains local to the claim: `not closed and Adoption Green With Evidence` is still an affirmative green claim because `not closed` describes a separate condition, while direct phrases such as `not adoption green`, `not all adoption checks are green`, or `cannot be green` remain negated.

RAR USER packet ZIP proof must tokenize actual ZIP paths before root validation. A ZIP marker that mentions a valid USER folder and then declares an off-root ZIP path, such as `C:\Nexus USER\<worktree-label> copied to D:\Review\<worktree-label>-YYYYMMDD-HHMMSS.zip`, is invalid because the declared upload ZIP is off-root. ZIP extraction must not synthesize one path by spanning explanatory prose between separate absolute paths.

RAR USER packet folder and ZIP proof must treat punctuation suffixes as part of parser-family hardening. Terminal punctuation or inline-code wrapping may end a valid token, but a token followed by extra non-terminal text such as `;extra`, `,extra`, `)extra`, `]extra`, `:extra`, `.tmp`, `.md`, or `.zip` in the folder marker is invalid. Chained wrapper punctuation plus an added suffix, such as `C:\Nexus USER\FAM-006).md` or `C:\Nexus USER\FAM-006-YYYYMMDD-HHMMSS.zip).tmp`, is also invalid because the declared token did not actually end at the wrapper punctuation. The fixture validator must include generated adversarial variants for both accepted terminal punctuation and rejected suffixed folder/ZIP tokens so PR review is not the first suffix fuzzer.

No-candidate wording must not hide a concrete issue candidate later in the same marker. Phrases such as `none recorded yet for Issue Candidate F6-HIST-001 pending review` are candidate-bearing prose, not valid no-candidate disposition. Green-claim matching must prefer the whole applicable green phrase before shorter subphrases, so `not adoption green with evidence` remains negated instead of being reinterpreted as an affirmative `green with evidence` subclaim.

Issue-candidate status detection must treat `ISSUE CANDIDATE` and `ISSUE-CANDIDATE` as the same status vocabulary for RAR code-to-visual rows and accepted-reference gap cells. Hyphenated, slash-separated, or underscore-separated compound forms such as `CURRENT-ISSUE-CANDIDATE`, `CURRENT / ISSUE CANDIDATE`, `CURRENT/ISSUE-CANDIDATE`, and `PENDING_ISSUE-CANDIDATE` must normalize to the same issue-candidate status as `CURRENT ISSUE CANDIDATE`. Current, unresolved, active, or pending issue-candidate wording must still require issue-candidate table and disposition proof. The accepted-reference comparator `Gap / Issue` column participates in unresolved and issue-candidate enforcement even when code-to-visual status rows are `CONFORMING`; green claims cannot override an unresolved accepted-reference gap without a real issue-candidate row, disposition, or USER-approved route.

Resolved or no-impact RAR closeout text must not be reactivated by negated blocker phrases. Wording such as `not blocked`, `no blocker active`, or `no active repair, waiver, review gate, or route remains open` is resolved context when the RAR stage is resolved/no-impact; affirmative active phrases such as `blocker active`, `blocked`, `repair required`, or `USER review pending` still block normal phase progression when they are not locally negated.

RAR code-to-visual and accepted-reference tables must reject malformed evidence rows even when at least one valid row is present. Validators must not silently drop overwide, underwide, placeholder, or malformed rows and then treat the remaining valid row as complete coverage; any malformed substantive evidence row means the affected table remains unproven until repaired.

Resolved or no-impact RAR status does not waive proof for named material surfaces. If a RAR packet names owned, affected, implemented, touched, UI/UX, runtime/backend, or product-visible material surfaces, the code-to-visual trace matrix and accepted-reference comparator matrix must contain substantive rows even when `RAR Stage:` is `Resolved` or `No Applicable Impact`; changing the stage label cannot turn no-op rows into closure proof. Conversely, blocked/held/paused/deferred phase wording is a valid negated phase disclaimer only when it explicitly holds a phase, PR creation, merge, release, Workstream, Hardening, Live Validation, or other advancement path. Wording such as `USER keeps Workstream blocked until RAR3 closes` or `PR creation remains blocked` must not be treated as phase advancement, while wording such as `not blocked so PR Readiness may proceed` remains an affirmative advancement claim and must still fail when active RAR context remains unresolved. USER-reviewed issue-candidate closeout wording may say the reviewed candidate is `not blocked for normal phase progression` when the disposition independently records USER review; `not blocked` must not count as completed disposition by itself. Mixed disclaimer-plus-advancement clauses must split on connectors such as `although`, `though`, and `while` as well as `but`, so `does not authorize merge although PR Readiness may proceed` remains an active-RAR advancement failure.

RAR USER decision tables must be substantive, not marker-only. A `RAR USER Decision` table with only the header and separator is invalid for USER-reviewable RAR packets because it gives the USER no actual repair, waiver, defer, route, approval, or no-action option to review. Accepted-reference `Gap / Issue` cells that explicitly negate issue-candidate applicability, such as `No issue candidate applicable`, `no issue candidates are applicable`, or `issue candidate not applicable`, must be treated as no active issue candidate rather than triggering issue-candidate debt by substring. `USER Packet Path:` must point to exactly one packet root folder in the shape `C:\Nexus USER\<worktree-label>`; child folders such as `Review Aids`, upload ZIP files, lookalike roots, multiple paths, or explanatory mentions are not valid packet-root proof.

PR Readiness Stage 1 review-risk coverage is mandatory for every branch and scales to the changed-file classes. For helper, validator, fixture, packet, RAR, UIREF, Product Experience, PR, release, or issue-governance changes, validation evidence must include a rule-to-code-to-fixture coverage map and adversarial coverage for the parser/proof family being changed, including malformed input, missing rows, marker-only proof, casing, hyphenation, negation, contradictory prose, path-root, traversal, suffix, duplicate-path, stale-packet, false-positive, and sibling failure modes where applicable. A PR Readiness packet that relies on validator green without this changed-file review-risk proof remains blocked on `Validation Adequacy Review Missing` or `PR Readiness Review Coverage Missing`.

When GitHub PR review exposes repeated same-family helper/validator/parser comments, the responsible repair must add a Review-Comment Pattern Matrix and a parser threat model before re-requesting review. Literal one-comment fixtures are insufficient by themselves: the repair must prove each affected rule through source-truth text, implementation code, a positive or negative fixture, and at least one generated mutation/adversarial case or equivalent harness that varies phrasing, negation, row placement, status spelling, path shape, packet proof, and sibling table location for the same family. If that proof is missing, validator green remains non-authoritative and the branch stays blocked on `Rule-To-Code-To-Fixture-To-Mutation Proof Missing`.

Live PR review-churn proof is owned by `dev/orin_pr_review_churn_validation.py` (`Helper Status: Reusable`) with durable fixture manifest coverage under `dev/fixtures/pr_review_churn/`. The helper must use GitHub live evidence as evidence only, fetch every review-thread page and every pull-review-comment page, collect Codex Connector review comments, classify them by parser/helper/validator family, report total/resolved/unresolved/outdated/unresolved-current thread counts, prove that every observed family has source-truth, implementation, fixture, generated-mutation, and sibling-variant coverage in the manifest, and prove that changed helper/validator/parser files are mapped to one or more covered families. It must also enforce a review-churn budget from the manifest: high total connector-comment volume or high same-family volume cannot pass merely because comments are resolved. Once the budget is exceeded, the manifest must contain an exact PR root-cause receipt with connector-comment count, observed family counts, `pr_readiness_stage_1_failure: true`, root cause, prevention summary, preventive changes, receipt file, and receipt marker; the helper must compare that receipt to live GitHub evidence so new comments make the receipt stale until updated. It may report the current-head Codex Connector green/approval latch from either a green approval comment/review or a Codex Connector thumbs-up reaction bound to the latest review request on the current live head; pre-request simulation may pass without requiring that latch, while merge-ready proof must require the live current-head latch separately. When `--require-current-green` finds only the missing live current-head latch, the helper must classify the posture as `Current-Head Revalidation Pending`: merge-ready proof remains non-green and may return nonzero, but PR2 must continue direct polling/revalidation instead of treating that helper result as a terminal blocked handoff. If the helper reports unknown comment families, unresolved-current threads, missing changed-file family coverage, missing source-truth / implementation / fixture / generated-mutation coverage, exceeded budget without a matching root-cause receipt, stale root-cause receipt counts, failed pagination, missing thumbs-up/comment approval-latch handling, or missing simulation digest, PR Readiness remains blocked.

Pre-PR adversarial firewall proof is also owned by `dev/orin_pr_review_churn_validation.py`. For helper, validator, parser, fixture, packet, RAR, UIREF, Product Experience, PR, release, issue-governance, or review-bundle changes, PR Readiness Stage 1 must run the current Python interpreter against `dev\orin_pr_review_churn_validation.py --pre-pr-firewall --base origin/main` before Stage 2 approval, PR creation, or another `@codex` review request. The durable manifest must include `pre_pr_firewall` validation commands, connector corpus replay rows, unknown-comment guardrails, changed-file family mapping, and at least three generated mutation variants plus two sibling replay variants for every mapped changed family. Nested manifest Python validation commands must use the portable `{python}` token, which the helper resolves to the current interpreter, rather than `py`, `python`, `python3`, or another runner-specific launcher. The command must fail on unmapped changed helper/validator/parser files, missing source-truth / implementation / fixture / generated-mutation / sibling-variant coverage, corpus comments that do not classify into the expected family, unknown guardrails that overmatch into a covered family, non-portable nested Python validation command launchers, or nested local validation command failures. This firewall supplements live PR review-churn validation; it does not replace final direct PR2 live review-thread, approval-latch, mergeability, or merge/close verification.

Governance reliability validator guidance: extend existing helpers before adding new root validators. Future machine-checkable coverage should prefer `dev/orin_branch_governance_validation.py` (`Helper Status: Reusable`), `dev/orin_branch_readiness_planning_fixture_validation.py` (`Helper Status: Reusable`), `dev/orin_user_review_bundle.py` (`Helper Status: Reusable`), `dev/orin_pr_body_quality_audit.py` (`Helper Status: Reusable`), `dev/orin_public_leak_prevention_validation.py` (`Helper Status: Reusable`), and `dev/orin_governance_efficiency_validation.py` (`Helper Status: Reusable`) before creating a new helper. Coverage targets include `Backlog Family Rejection Test Missing`, `Backlog Identity Created By Inertia`, `Authority Hierarchy Ambiguous`, `Evidence Treated As Source Truth`, `Governance Mirror Drift`, `Architecture Impact Unclassified`, `Experience Layer Impact Unclassified`, `Cross-Family Policy Impact Unclassified`, `New Owner Candidate Without Placement Preflight`, `USER Review Packet Human-Readability Missing`, `USER Review Packet Metadata Dump`, `PR Body Drift Check Failed`, `Private Reference Leak`, `Owner Data Leak`, `Dev Private Evidence Leak`, and reliability-class evidence labels. Helpers and validators remain evidence; green output does not replace changed-file review, source-truth owner review, packet readability review, or Codex responsibility for the final recommendation.

BR1 candidate reliability validator guidance: future machine-checkable coverage should extend `dev/orin_branch_governance_validation.py` and `dev/orin_branch_readiness_planning_fixture_validation.py` before adding a new helper. Coverage targets include `BR1 Candidate Viability / Grouping Matrix`, concrete feature outcome presence, implementation-bearing route class, behavior-change classification, Family Feature Vision context or not-applicable reason, Deferred Feature Carryforward consumption, grouping recommendation, split reason when not grouped, expected Slice/SLC/seam map, proof path, largest safe coherent package explanation, tiny-branch sprawl review, and blockers `BR1 Candidate Implementability Missing`, `BR1 Candidate Grouping Matrix Missing`, `BR1 Planning-Only Candidate Drift`, `BR1 Support-Only Candidate Drift`, `BR1 Readiness-Only Candidate Drift`, `BR1 Manifest-Only Candidate Drift`, `BR1 Tiny-Branch Sprawl`, `BR1 Feature Vision Context Missing`, `BR1 Candidate Split Reason Missing`, `Implementation-Bearing Route Unproven`, `Post-Merge Release Readiness Decision Missing`, and `Release Readiness Handoff Skipped`. Fixtures should prove weak first-pass BR1 candidate lists fail when they present tiny planning/support/manifest branches without real implementation output, and strong grouped implementation-bearing candidates pass when they preserve Family Feature Vision and deferred carryforward boundaries.

Family Feature Vision drift validator guidance: future machine-checkable coverage should extend existing reusable helpers before creating a new root helper. `dev/orin_branch_governance_validation.py` should scan every tracked `Docs/family_feature_visions/*.md` file in the active worktree when that directory exists, classify each FFV as category-level, transition alias, rename/reframe required, compact-ID missing, pointer-migration required, live-state wording repair required, or USER-decision-required, and block selected feature-bearing BP1 entry when a relevant FFV is slice/SLC/seam/branch-route scoped. `dev/orin_branch_readiness_planning_fixture_validation.py` should add fixtures for a weak slice-named FFV that fails, a category-level FAM-006-style FFV that passes, a FAM-007 private-boundary FFV that passes after category reframe, and stale-pointer cases that fail after an FFV rename. `dev/orin_user_review_bundle.py` should reject BP1/BP2/BP3 packets that cite a Family Feature Vision generally but omit selected/deferred element matrices, element-to-Slice/SLC/seam/proof mapping, gate-state consistency, or pointer updates to the current FFV path. If a validator reports drift in a product worktree, the owning worktree must repair its own FFV files and pointers under its own approved carrier; the Governance worktree records the global rule and helper expectation but must not mutate sibling FFV files by convenience. Coverage targets include `Family Feature Vision Slice-Scoped`, `Family Feature Vision Compact ID Missing`, `Family Feature Vision Inventory Repair Required`, `Family Feature Vision Pointer Migration Missing`, `FFV Live-State Leakage`, `FFV Element Selection Matrix Missing`, `FFV Element Proof Chain Missing`, `Deferred Carryforward Applicability Missing`, and `Deferred Carryforward Branch Sprawl`.

Feature registry / vision boundary validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding a new root helper. `dev/orin_branch_governance_validation.py` and `dev/orin_governance_efficiency_validation.py` should flag compact pointer surfaces when `Docs/feature_backlog.md` or `Docs/prebeta_roadmap.md` carry BP1/BP2/BP3 gate state, Workstream/Hardening/Live Validation state, UTS posture, SLC implementation status, active branch lifecycle, selected-next state, live PR/review state, release-window state, active dependency queue wording, or full vision narrative instead of compact pointers. `dev/orin_branch_readiness_planning_fixture_validation.py` should keep fixtures that distinguish FAM rows, FFV registry rows, FFV element inventories, Branch Vision snapshots, Slice/SLC maps, and seam proof plans. `dev/orin_user_review_bundle.py` should surface backlog/roadmap pointer drift in review aids without turning USER packets into source truth. Coverage targets include `Backlog Lifecycle Ledger Drift`, `Roadmap Branch Lifecycle Ledger Drift`, `Feature Registry Vision Narrative Drift`, `FFV Index Active-State Leakage`, `Slice Treated As Vision Identity`, `SLC Treated As Family Feature Vision`, and `Seam Treated As Feature Category`.

Runtime observability / Live Validation proof validator guidance: machine-checkable branch-plan coverage extends existing reusable helpers before creating a new root helper. `dev/orin_branch_governance_validation.py` enforces `Runtime Observability Decision Matrix:`, `Exact USER Desktop Launcher Path:`, `Launcher Parity Proof Plan:`, `Photo / Video Proof Plan:`, `Manual USER Validation Plan:`, `Troubleshooting Mode Decision:`, and `USER Packet Evidence Plan:` in active Branch Runtime Engineering Plan validation. `dev/orin_branch_readiness_planning_fixture_validation.py` proves the valid runtime plan carries those markers and rejects derived negative fixtures where direct runtime / Dev Toolkit proof is treated as formal USER launcher proof, exact desktop launcher proof is missing, photo/video proof is replaced by screenshot-exists metadata, or USER packet evidence is omitted. Runtime, desktop, user-facing, file/folder, launch, bridge, Dev Toolkit, or validation-critical helpers must preserve the proof hierarchy in `Docs/phase_governance.md`. A Live Validation helper may use direct runtime, WebView, sandbox/offscreen, helper launch, troubleshooting launcher, Dev Toolkit, logs, markers, manifests, and generated artifacts for diagnosis and consistency, but must label those as supporting evidence when they are not the exact normal USER desktop runtime launcher path. If a branch-local normal runtime launcher is missing, a Live Validation helper may create or repair that launcher only as validation setup, only targeting the active branch's normal product runtime entrypoint and working directory, and never targeting troubleshooting mode, validation helpers, diagnostic-only entrypoints, installer/startup/package behavior, provider/model/cache/memory/private setup, or persistent OS integration; the helper or authority record must expose `Normal Worktree Launcher Validation: PASS` before using it as the formal USER path. Formal LV/UTS handoff for desktop/user-facing behavior requires `Exact USER Desktop Launcher Validation: PASS` or `WAIVED` with reason. Troubleshooting launcher evidence may substitute only with `Troubleshooting Runtime Launcher Consent: GRANTED` and `Launcher Parity Proof: PASS`. Visible USER-facing acceptance claims require photo/video, ordered frame-sequence, or focused screenshot evidence that Codex adjudicates artifact-by-artifact; a manifest path, marker, or screenshot existence flag is not acceptance proof by itself. If a required claim cannot be proven in photo/video, helper output must expose `Manual USER Validation Required`, `USER Waiver Required`, or a named blocker instead of reporting final green. USER review bundles for Live Validation must include or reference proof manifests, runtime logs, Dev Toolkit logs when used, screenshot/video folders, interaction matrices, visual adjudication, source-truth alignment checks, validator outputs, pass/fail/blocked/unproven mapping, and raw evidence references. Coverage targets include `Exact USER Desktop Launcher Proof Missing`, `Normal Worktree Launcher Validation Pending`, `Launcher Parity Proof Missing`, `Photo Or Video Proof Missing`, `Unphotographable Proof Not Elevated To USER`, `Direct Runtime Proof Misclassified`, `Troubleshooting Consent Missing`, `Live Validation Evidence Packet Incomplete`, and `User-Visible Internal Path Leakage`.
Vision-To-Proof Matrix validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new validators. `dev/orin_branch_governance_validation.py` and `dev/orin_branch_readiness_planning_fixture_validation.py` should eventually require `Vision-To-Proof Matrix:` rows in BP2/BP3, Hardening, and Live Validation packets when the branch carries material runtime, UI, UX, workflow, failure/recovery, security/privacy, helper/validator, source-truth, or subjective USER claims. `dev/orin_user_review_bundle.py` should surface matrix gaps in USER packets and UTS handoffs without turning screenshots, videos, manifests, logs, helper output, or external operational state into repo ledgers. Machine-checkable coverage should reject matrix rows that omit accepted vision source, accepted requirement, claim class, minimum proof strength, implementation evidence, observed runtime evidence, comparison evidence, reference surface/baseline, Codex adjudication, USER validation need, or final verdict where required. Coverage targets include `Vision-To-Proof Matrix Missing`, `Accepted Requirement Missing`, `Observed Runtime Comparison Missing`, `Reference Surface Missing`, `Hardening Proof Gap Not Routed`, `Evidence Input Treated As Comparison`, `Screenshot Or Video Not Adjudicated`, `Runtime Log Treated As Visual Proof`, `Helper Output Treated As Runtime Observation`, `Subjective UX Claim Not USER-Routed`, `Vision-To-Proof Verdict Missing`, and `Live Validation Comparative Proof Missing`.

Hardening / Live Validation repair-loop validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new validators. `dev/orin_branch_governance_validation.py` and `dev/orin_branch_readiness_planning_fixture_validation.py` should eventually reject phase packets, active branch-plan summaries, Live Validation digests, UTS handoffs, or PR Readiness packets that treat pre-repair Hardening as final after LV-driven repairs, skip post-LV-repair Hardening, skip LV reconfirmation after rerun Hardening, rerun full Hardening while the LV path is still known-failing without first repairing the LV blocker, or ask USER to accept final green from a stale proof state. `dev/orin_user_review_bundle.py` should surface these loop-state gaps in USER packets without copying raw current evidence or active state into repo docs. Coverage targets include `Known-Failing LV Repair First`, `Post-LV-Repair Hardening Rerun Missing`, `Post-Hardening LV Reconfirmation Missing`, `Pre-Repair Hardening Treated As Final`, `Final USER Validation Missing`, and `PR Readiness Uses Superseded Hardening`.

Scope Coverage Manifest validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new validators. `dev/orin_branch_governance_validation.py` should eventually check phase packets, active external branch-plan summaries, PR Readiness packets, and Live Validation/UTS digests for `Scope Coverage Manifest:` when a branch claims `green`, `complete`, `LV passed`, `PR-ready`, `accepted`, `no drift`, `all fixed`, or equivalent full-scope success. `dev/orin_branch_readiness_planning_fixture_validation.py` should add fixtures for broad/multi-issue USER complaints that fail when only one target is repaired, fixtures for valid atomic defect ledgers, fixtures for unjustified sampling, and fixtures for vague final acceptance language. `dev/orin_user_review_bundle.py` should surface coverage manifest gaps in USER packets without moving active target rows, screenshots, videos, manifests, or external operational state into repo docs. Coverage targets include `Completeness Claim Without Coverage Manifest`, `Relevant File Class Not Inventoried`, `USER-Facing Surface Not Inventoried`, `Visual Element Coverage Missing`, `Element Group Coverage Missing`, `Accepted Vision Claim Not Mapped To Proof`, `Validator Green Accepted Without Coverage Review`, `Generated Artifact Not Checked`, `Human-Judgment Surface Not Elevated`, `Coverage Exclusion Not Justified`, `Sampling Used Without Justification`, `Broad Request Decomposition Missing`, `Multi-Issue Repair Ledger Missing`, `Quantity-Sensitive Repair Drift`, `Partial Repair Reported Complete`, `All-Claim Coverage Missing`, and `Unverified Defect Target`.

Standalone window geometry recovery validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new validators. `dev/orin_branch_governance_validation.py` and `dev/orin_branch_readiness_planning_fixture_validation.py` should eventually require `Standalone Window Geometry Recovery Matrix:` or equivalent fields when a branch creates, touches, restores, moves, resizes, persists, or proves a standalone/top-level/restorable Nexus-owned product window. `dev/orin_user_review_bundle.py` should surface missing window classification, reset-route, exception, and proof gaps in USER review packets without copying current window coordinates or live window state into repo docs. Coverage targets include `Window Position / Size Reset Route Missing`, `FAM-003 Window Recovery Dependency Required`, `Child Window Geometry Reset Not Applicable Unproven`, `Offscreen Window Recovery Path Missing`, `Window Geometry Classification Missing`, `FAM-008 Runtime Reset Ownership Drift`, and `Repo File-State Tracking`.

Window geometry / resize contract validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new validators. `dev/orin_branch_governance_validation.py`, `dev/orin_branch_readiness_planning_fixture_validation.py`, `dev/orin_user_review_bundle.py`, and surface-specific validators should eventually require `Window Geometry / Resize Matrix:` or equivalent fields when a branch creates, changes, resizes, scrolls, proves, or visually accepts a Nexus-owned product window or surface that has geometry, size-state composition, responsive layout, fullscreen/maximize behavior, or multi-monitor posture. Helpers should reject marker-only conformance, default-size-only proof, missing min/default/medium/wide/max evidence, missing fullscreen/maximize disposition, missing display-scale or multi-monitor reasoning, missing accepted-reference comparison, hidden bottom-right resize-grip reliance without policy, global zoom used as a layout substitute, sparse wide-state shells, current coordinates stored in repo docs, and green claims while owned surfaces remain unproven or issue candidates are undispositioned. Coverage targets include `Window Geometry Contract Missing`, `Window Classification Missing`, `Window Min Default Max Policy Missing`, `Window Fullscreen / Maximize Policy Missing`, `Window Resize Mechanics Proof Missing`, `Resize Cursor / Grip Policy Missing`, `DPI Display Scale Proof Missing`, `Multi-Monitor Geometry Proof Missing`, `Responsive Layout Proof Missing`, `Sparse Wide-State Shell`, `Global Zoom Used As Layout Substitute`, `Detached Child Acceptance Missing`, `Embedded Child Inheritance Unproven`, `Geometry Issue Candidate Missing`, `Marker-Only Geometry Conformance`, and `Repo File-State Tracking`.

NDAI top-level window-control grammar validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new validators. `dev/orin_branch_governance_validation.py` and `dev/orin_branch_readiness_planning_fixture_validation.py` should eventually require `NDAI Top-Level Window Control Grammar Matrix:` or equivalent fields when a branch creates, changes, proves, or visually accepts a Nexus-owned top-level/standalone/restorable product window. `dev/orin_user_review_bundle.py` should surface missing control-cluster classification, large-header-close exceptions, header/footer close conflicts, minimize/maximize/restore applicability gaps, accessibility gaps, and visual-state proof gaps in USER review packets without requiring runtime UI mutation during governance-only passes. Coverage targets include `Top-Level Window Control Grammar Missing`, `Large Header CLOSE Pill Requires Exception`, `Default Windows Chrome Regression`, `Window-Control / Content-Action Boundary Missing`, `Minimize / Maximize / Restore Applicability Missing`, `Window Control Accessibility Proof Missing`, `Header/Footer Close Conflict Unresolved`, and `Visual Window Control Proof Missing`.

Merged-standard adoption validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new root validators. `dev/orin_branch_governance_validation.py` should eventually detect when an active or re-entering branch baseline predates merged Project Vision, Family Vision, Family Feature Vision, UI immersion/window chrome, claim/evidence, proof-strength, Vision-To-Proof Matrix, Live Validation, UTS, failure/recovery, or USER-validation standards and require `Merged Vision Standard Adoption:` or `Merged Vision Standard Adoption Review:` at the next legal gate. `dev/orin_branch_readiness_planning_fixture_validation.py` should add fixtures where an old BP1/BP2/BP3/Hardening/LV/PR packet ignores newly merged standards and fails, plus fixtures where the same branch records `Not Applicable`, `No Affected Surface`, `Adopted`, `Repair Required`, `USER Waiver Required`, or `Deferred To Next Legal Gate With Reason` correctly. `dev/orin_user_review_bundle.py` should surface adoption gaps in USER review packets without copying mutable branch status into repo docs. Coverage targets include `Merged Vision Standard Adoption Missing`, `Merged Vision Standard Adoption Repair Required`, `Merged Vision Standard Adoption Waiver Missing`, `Merged Vision Standard Adoption Live-State Leakage`, and `Merged Standard Ignored By Re-Entering Branch`. Validators must not force immediate FAM-006/FAM-007 mutation, require external state in clean-clone CI, or treat historical merged receipts as newly failing only because the standards were created later.

Backend predictability / frontend-backend contract validator guidance: future machine-checkable coverage should extend existing reusable helpers before adding new root validators. `dev/orin_branch_governance_validation.py` should eventually require Backend Predictability / Reliability Contract fields in runtime/backend-affecting BP2/BP3, Workstream, Hardening, Live Validation, PR Readiness, and rebaseline adoption packets. `dev/orin_branch_readiness_planning_fixture_validation.py` should add fixtures where a UI claims green while runtime truth is unknown, backend logs are treated as USER proof, recovery UI lacks a real recovery route, schema/config changes omit compatibility proof, and previously implemented branch output requires a USER-reviewed issue candidate instead of automatic issue mutation. `dev/orin_user_review_bundle.py` should surface backend/UI mismatch, failure/fallback/recovery gaps, degraded/disabled state gaps, and issue-candidate packets in USER review surfaces without moving active runtime evidence or mutable issue state into repo docs. Coverage targets include `Backend Predictability Contract Missing`, `Runtime State Owner Missing`, `Runtime State Machine Missing`, `Nondeterministic Backend Behavior`, `Failure Path Missing`, `Fallback Behavior Hidden`, `Schema Migration Proof Missing`, `Config Compatibility Proof Missing`, `Backend/UI Contract Mismatch`, `User-Facing State Not Backed By Runtime Truth`, `Backend Logs Treated As User Proof`, `Recovery Route Missing`, `Rollback Path Missing`, `Issue Candidate Disposition Missing`, and `Issue Mutation Approval Missing`.

UI reference / template / shared primitive dependency validator guidance: helpers must reject claims that an accepted reference, implementation template, design token, shared primitive, or automated enforcement path exists by inference. A promoted UI reference must cite a concrete `Docs/ui_reference_catalog/UIREF-*` record. A claimed implementation template or shared primitive must cite the actual approved source record or code owner. Otherwise helpers should detect `Template Dependency Unresolved`, `Template Claim Unsupported`, `Shared Primitive Promotion Blocked`, `Shared Primitive Claim Unsupported`, `Limited PR Path USER Approval Missing`, `Reference Treated As Existing Template`, or `Template Treated As Existing Proof` when machine-checkable. After a valid UIREF record exists, helpers should enforce the record's known limitations and adoption rule instead of treating the whole reference class as blocked. This guidance does not authorize runtime implementation, design-token implementation, shared UI primitive implementation, helper code, validator code, or fixture mutation.

Pre-Live Visual Purpose Conformance validator guidance: helpers and validators must treat the visual gate as a required precondition for visible user-facing UI, window, control, state, and interaction claims. A helper must not mark Live Validation, UTS handoff, PR Readiness, or release-facing UI proof green when any unwaived visual-purpose row is missing, `REPAIR`, `BLOCKED`, `UNPROVEN`, or described only with subjective progress language such as `better`, `closer`, `improved`, `looks acceptable`, `seems green`, or `good enough`. Helpers should require source-truth purpose contract, recorded photo/video element-group visual adjudication, reference/comparator surface, code-to-visual trace, PASS/REPAIR/BLOCKED/WAIVED_WITH_REASON disposition, and exact repair routing before functional interaction proof. Helpers must reject stale same-class primitive drift when a branch reintroduces duplicate action buttons, stretched-grid action layouts, unequal content-fit button gutters, per-window button font/radius/glow changes, or debug/proof vocabulary in product UI after the accepted vision contract requires shared primitives.

Codex User Guide operator-boundary validator guidance: `Docs/codex_user_guide.md` is a USER-facing operator guide and must not be used as a binding governance mirror. Validators may check that it remains an operator quick-reference with prompt examples, `Scope Coverage Manifest` wording, and common context fields, but binding phase law, approval gates, release rules, source-truth ownership, helper behavior, validator behavior, and external-state schema checks must target their owner files. If the guide begins carrying governance mirror content again, the repair is to move that language to the owning source-truth file or prompt template and keep the guide subordinate to `Docs/Main.md`.

## Registered Root Dev Helpers

### Repo-Wide Governance

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_branch_governance_validation.py` | Helper Status: Reusable | governance validator | Extend when repo-wide source-of-truth, phase, branch, release, helper registry, prompt-contract, Source-Truth Placement Preflight, Element Validation Ledger placement, Element Validation Ledger drift, Branch Readiness product-system planning gates, PR Readiness post-merge source truth, Release Readiness Health Pass rules, `Release Readiness Candidate Anchor` rules, `Release Window Aggregation Ownership` rules, `Prompt-Entry Origin/Main Freshness Gate` rules, `Origin/Main Freshness Check` rules, `Pre-Rebaseline Impact Audit` rules, `Rebaseline Overlap Intent Gate` rules, `Current-Main Reconciliation Identity Guard` rules, `Standing Governance Intake Branch` rules, `automation/worktree governance intake` rules, `Assigned Worktree Confinement` rules, `Prompt-Ingress Lane Lock` / `Prompt-Ingress Lane Mismatch` rules, `Family-Scoped Branch Readiness Confinement` rules, `Target Family` and `Sibling Worktree Candidate Exclusion` validation where Sibling worktrees are overlap context only and not successor authority, active thread/worktree ownership rules, same-worktree/same-branch collision rules, dirty-worktree recovery rules, off-worktree work routing rules, Governance Routing Barrier rules, New Worktree Decision Gate rules, bounded Workstream continuation rules, USER Branch Plan Review Gate, Workstream Entry Whole-Package Analysis Gate, Formal Next Legal Phase Digest non-compaction, Forwarded Digest Non-Compaction Rule, Main-first loader chain routing, external-active branch plan routing, or merged-main active-authority closeout rules become machine-checkable. It owns source-checking for `Prompt-Entry Freshness Check:`, `Fetched origin/main:`, `Origin/Main Advanced Since Last Action:`, `Pre-Rebaseline Impact Audit Required:`, `Rebaseline/Reconciliation Status:`, `Prompt-Entry Origin/Main Freshness Missing`, `Pre-Rebaseline Impact Audit`, `No Baseline By Inertia`, `Incoming Main Change Set`, `Incoming Changed Files`, `Current Worktree Changed Files`, `Branch Changed Files`, `Rebaseline Overlap Files`, `Rebaseline Overlap Intent Gate`, `Branch Change Intent Ledger`, `Rebaseline Overlap Intent Missing`, `Incoming Runtime / Source-Truth Risk`, `Validation Before Rebaseline`, `Recommendation Only`, `Rebaseline Mutation Approval`, and `Rebaseline Mutation Status` so future worktree fast-forward, merge, rebase, branch-switch, current-main reconciliation operations, resumed-thread work, post-PR-merge handoffs, and validation-green claims cannot be normalized in governance without an analysis-first approval gate. It owns active `Phase: Workstream` checks for `Seam Continuation Decision`, `Backlog Completion Status`, `Completion Status`, `Continue Decision`, `Continuation Execution Latch`, `Single-Seam Workstream Waiver`, `Single-Seam Or Single-Slice Waiver Authority`, `Single-Seam Or Single-Slice Workstream Blocker`, and `Bounded Seam Default`, preventing a Workstream from stopping after one seam or one slice unless the whole Workstream is green and ready for Hardening, or Red with a named blocker/USER waiver. It owns `--standing-governance-intake-gate` for `feature/release-readiness-source-truth-intake`, `Release Readiness digest` intake plus USER-approved non-runtime automation/worktree governance intake and USER-approved phase-gate governance intake, `RRI-YYYYMMDD-NNN`, operational `One Active Cycle`, the external-owner rule for active RRI cycle values, current PR posture, return-digest progress, release-window state, and target commit facts, the pre-intake `Sync Rule`, `Bootstrap Exception Limit`, `Waiting For Governance Intake` / `Waiting For Updated Main`, the required `Return Digest`, `Neutral Main Workspace Rebaseline`, `Pre-Rebaseline Impact Audit`, the standing-branch exemption from dedicated cycle-ledger closeout PRs, `Return Digest Identity Guard`, `Return Digest Origin Identity Missing`, the standing-lane allowance for registered release-readiness validators such as `dev/orin_release_body_validation.py` and automation-observability repair support in `dev/automation_observability_report.py`, the merged-main `No Active Branch` exception that allows only this standing governance authority record while rejecting all active runtime/implementation/release/repair carriers, and `Merged Active Branch Authority Not Folded Down` ancestry checks that reject any non-standing active authority record whose branch ref is already merged into `origin/main`; it also owns Branch Readiness active implementation `Product Definition Plan` markers and substance checks for `Project-Wide Vision Alignment:`, `Branch-Specific Vision Alignment:`, `System Concept Model:`, `Entity / Profile Model:`, `User Workflow Model:`, `Scale / Data Volume Model:`, `Configuration And State Model:`, `Expected User-Facing Outcomes:`, `Codex Additional Recommendations:`, `USER Critique Loop:`, `USER Decision Ledger:`, `Deferred Ideas / Future Package Ledger:`, `Planning Adequacy Review:`, `Rejected Shallow Plan:`, `Alternatives And Tradeoffs Reviewed:`, `Whole-System Interaction Map:`, and `Open Questions / USER Decision Points:`, rejecting placeholder/self-assessed values such as `simple`, `basic`, `minimal`, `see above`, and `not applicable`; it owns runtime-focused `Runtime Branch Engineering Contract` markers including `USER Engineering Planning Review:`, `Runtime Implementation Approval:`, `Current Runtime Baseline:`, `Planned Runtime Delta:`, `User-Facing Runtime Delta:`, `State / Config / Schema Delta:`, `Validator / Helper Delta:`, `Workstream Seam Map:`, `Proof Expectations:`, and `Plan-To-Implementation Traceability:` so broad seam labels must be paired with concrete engineering deltas; it owns marker-first `Element-to-Phase Proof Matrix` checks for active external branch plans and repo historical branch-plan receipts, requiring planned/current elements to map Workstream implementation, Workstream proof, Hardening proof, Live Validation proof or waiver, UTS / USER acceptance, USER decision state, and source owner / ledger owner before Workstream entry; it owns marker-first USER Branch Plan Review Gate checks for plain-language branch goal, planned user-facing outcome, USER Review Packet Finding, implementation breakdown, Hardening plan, Live Validation / UTS plan, recommendations, alternatives/tradeoffs, accepted/deferred/rejected scope, exact USER decision, and implementation approval boundary; it owns marker-first `USER Feedback Disposition` checks for `UFD-*` IDs, `USER Decision State:`, `Owner Class:`, `Canonical Owner File:`, `Workstream Severity:`, `Status:`, `Fold-Down Target:`, and `No Durable Owner Needed` no-action guardrails while natural-language duplicate feedback detection remains report-only until approved fixtures, false-positive review, and USER approval; owns `--worktree-confinement-gate` for `Active Thread Owner:`, `Thread Assignment Status:`, `Worktree Ownership Ledger:`, `Intended Write Set:`, `Same Worktree / Same Branch Collision Check:`, `Dirty Worktree Collision Check:`, `Dirty Worktree Recovery Packet:`, `Off-Worktree Work Routing`, `Governance Routing Barrier`, `New Worktree Decision Gate`, `Expected Worktree Root`, `Actual Worktree Root`, `No Cross-Worktree Mutation`, `GitHub Desktop-bound worktree`, `Worktree Escape User Waiver Missing`, `Worktree Escape User Waiver: Granted`, `Prompt-Ingress Lane Lock`, and `Prompt-Ingress Lane Mismatch` validation, `--release-readiness-health-gate` for `Release Candidate Anchor Projection:`, `Release Window Contributor Inventory:`, and `Governance Intake Routing:`, and `--pr-readiness-gate` plus the default validation path for PR Readiness `Branch Creation Base:`, `Current origin/main:`, `Origin/Main Advanced Since Branch Creation:`, `Reconciliation File List:`, `Reconciliation Recommendation:`, `Current-Main Reconciliation Identity Guard:`, `Branch-Local Authority Reassertion:`, `Incoming Main Active-Branch Blocks Accepted: NO`, merged-main active authority closeout, and no file fixes during Stage 1. |
| `dev/orin_external_state_target_reconcile.py` | Helper Status: Reusable | target-scoped external-state writer | Use only for an explicitly admitted live projection target after snapshot and lock proof. It updates live fields before the first historical receipt boundary, preserves receipt history, performs atomic replacement and post-write validation, and records audit/rollback proof. It must not traverse, rewrite the root manifest, or mutate historical receipts. |
| `dev/orin_external_state_lock_release.py` | Helper Status: Reusable | external-state lock release helper | Release only the exact admitted target lock after a completed or rolled-back target-scoped transition; a remaining lock is an incomplete closeout. It must not release unrelated locks or mutate repo source truth. |
| `dev/orin_external_state_target_currentness_fixture_validation.py` | Helper Status: Reusable Fixture Validator | external-state currentness regression fixtures | Prove positive, wrong-identity, stale, malformed, historical, path traversal, negated, nominal, forged, invalid, unverified, non-operational, or duplicate live-role and conditional or duplicate historical-boundary rejection, modern and legacy snapshot timestamp syntax and transaction-order rejection, modern and legacy released-lock timestamp syntax and transaction-order rejection, canonical legacy receipt timestamps, flat audit-log namespace enforcement, nested audit-directory rejection, committed live-target After SHA256 binding through final revalidation, snapshot-enumeration failure, snapshot-directory replacement, post-hash child mutation and inventory creation, snapshot-manifest replacement, final-boundary snapshot mutation during later lock validation, audit-inventory creation before and after final hashing, rediscovery failure, and same-name replacement races, lock-receipt replacement, reparse, TOCTOU, writer, audit, lock-release, modern transaction-journal, immutable malformed root-frame recovery, quote-aware structural root discovery after quoted junk braces, exact legacy completed-audit compatibility, complete-target row-level completion, contradictory/partial/free-text completion rejection, controlled mutation kills, and scoped-PASS-versus-root-PASS behavior for target-scoped currentness. It must not mutate live external state outside temporary fixtures. |
| `dev/orin_worktree_rebaseline_audit.py` | Helper Status: Reusable | worktree rebaseline audit helper | report-only helper for `Pre-Rebaseline Impact Audit` packets. It must not fetch, merge, rebase, checkout, reset, or mutate files. Reuse before any FAM, Governance, or neutral-main worktree baselines to newer `origin/main`; it reports cwd, git root, worktree role from `Docs/worktree_slots.md` or generic runtime-slot fallback, branch, upstream, `HEAD`, target ref, merge base, incoming commits, incoming changed files, `Current Worktree Changed Files:`, `Branch Changed Files:`, `Rebaseline Overlap Files:`, runtime/source-truth risk, sibling worktree dirty-file overlap, active branch authority record, `Current-Main Reconciliation Identity Guard`, recommendation-only posture, `Rebaseline Mutation Approval: Pending`, and `Rebaseline Mutation Status: Not started` or blocked. Incoming changed files must compare `merge_base..target_ref`, branch changed files must compare `merge_base..HEAD`, current branch/worktree changed files must union branch changed files with staged/unstaged/untracked files, `Rebaseline Overlap Files:` must be the intersection of incoming changed files and current branch/worktree changed files, and branch authority matching must use the exact `- Branch:` field instead of substring matching. When overlap exists, the helper must resolve or accept the active external branch plan at `C:\Nexus Governance State\branches\<branch_slug>\branch_plan.md`, parse `Branch Change Intent Ledger` / `Changed Surface:` entries when available, report actual `Branch Change Intent Present:` YES/NO, classify `Semantic Merge Risk:` and `Regression / Gating Impact:`, and report `Rebaseline Overlap Intent Gate`, `Overall Overlap Gate Result:`, `Rebaseline Overlap Failure Procedure`, and `Rebaseline Overlap Intent Missing` guidance without treating fallback evidence as a compatibility bypass. Repo branch-plan files may be fallback evidence only after the external-state transition. |
| `dev/orin_validation_suite.py` | Helper Status: Reusable | validation suite recommendation helper | report-only helper for `Recommended Validation Suite:` packets. It accepts `--phase` and optional `--changed-file` values, inspects committed, unstaged, staged, and untracked changed files when changed files are omitted, and recommends validation commands with rationale; it does not execute commands or mutate files. Use it to reduce copy/paste drift in Governance, Branch Readiness, PR Readiness, Release Readiness, FAM-006 runtime, and FAM-007 runtime validation planning. |
| `dev/orin_governance_efficiency_validation.py` | Helper Status: Reusable | governance efficiency operating model validator | Reuse for governance/source-truth efficiency reform checks before adding another source-truth owner. It validates `Docs/governance_efficiency_operating_model.md`, confirms compact pointers from routing and governance docs, enforces the Docs Source-Truth Reform Model, blocks `Package Trace:` / `Slice Trace:` detail from backlog/roadmap, keeps backlog/roadmap from absorbing live release, live branch, or Branch Runtime Engineering Plan detail markers, verifies the worktree slot registry does not own volatile live state, checks branch-record / branch-plan / workstream fold-down ownership rules, and ensures USER review hub metadata routes active packets to `C:\Nexus USER\<label>` plus mandatory timestamped upload ZIPs shaped `C:\Nexus USER\<label>-YYYYMMDD-HHMMSS.zip`. |
| `dev/orin_docs_inventory_reform_audit.py` | Helper Status: Reusable | docs inventory reform audit generator | Reuse for full `Docs/` source-truth reform reviews before claiming the Docs inventory is complete. It enumerates every file under `Docs/`, regenerates `Docs/governance_docs_full_inventory_reform_audit.md` with a complete manifest, complete cleanup/disposition table, ambiguity pass, structure pass, file-by-file review table, file-by-file dossier, duplicate truth map, source-truth ownership map, branch-plan lifecycle/deletion rule, retirement/deletion candidate table, validator enforcement table, and `Docs/governance_docs_reform_user_review_index.md`; it must classify `Docs/ui_reference_catalog/` as the USER-promoted UI reference contract catalog, not as unknown docs reference or a cleanup candidate, while keeping candidate evidence outside the catalog even after promoted UIREF records exist; promoted records may cite accepted evidence, known limitations, and adoption rules without becoming live proof ledgers; it does not mutate runtime code or sibling worktrees. `--help`, `-h`, `--dry-run`, `--read-only`, and `--report-only` must remain no-write paths; audit regeneration belongs to approved write/generation use. |
| `dev/orin_user_review_bundle.py` | Helper Status: Reusable | USER review hub helper | Reuse whenever Codex asks USER to inspect repo files, review a generated dossier, approve a planning packet, compare source-truth reform files, or green-light Branch Planning / Workstream Entry. It creates or refreshes the active worktree child folder under the stable local USER hub `C:\Nexus USER`, writes root `START_HERE.md`, writes exactly one primary current-gate USER decision file under `USER Review`, writes generated supporting digests/checklists under `Review Aids`, copies only selected repo-relative context files under `Source Truth Context`, maps copied filenames back to source paths, and does not edit source truth or runtime files. It also removes legacy same-name upload artifacts plus previous same-label timestamped upload ZIPs and creates a fresh timestamped upload ZIP at `C:\Nexus USER\<worktree-label>-YYYYMMDD-HHMMSS.zip` from the refreshed folder so ChatGPT uploads cannot collide on the same artifact name or accidentally receive a stale same-label upload. The helper validates directly that the exported zip filename contains the creation timestamp, the exported zip lives beside the packet folder, the exported zip contains no duplicate member names, the exported zip contains `START_HERE.md`, applicable `USER_BRANCH_VISION_REVIEW.md` / `USER_BRANCH_PLAN_REVIEW.md`, required digest/checklist files, no unresolved shell-style branch/head/origin-main/packet/zip template variables, no unevaluated shell expressions, no generated USER-facing technical metadata in root `START_HERE.md`, `USER Review`, or `Review Aids`, exactly one primary USER review file, approved file-class layout, stale-stage scan, stable ZIP rejection, stale same-label ZIP absence, and a consistent folder/ZIP file list plus content hash set before it exits green. Custom roots, manually supplied labels, legacy one-off folder names, and cloud-backed Desktop / OneDrive active upload roots require `--allow-custom-review-path` plus a recorded reason and must appear in `START_HERE.md` as a custom review path waiver. Branch Planning packets should include the Project Vision, Family Vision, Branch Vision review, active external branch plan or historical repo branch-plan receipt, Element-to-Phase Proof Matrix owner, branch authority record, Nexus/family vision files, UFD/change-intent surfaces when applicable, and relevant source-truth files for USER inspection before implementation begins or resumes. Its Workstream Entry packet validation mode checks required digest files, USER decision presence, next-legal-phase consistency, unresolved-placeholder absence, generated USER-facing technical metadata absence, copied source-file content against the expected HEAD, expected branch/current branch identity, expected HEAD/current HEAD identity, expected origin/main/current origin/main identity, and whether a packet still blocks implementation approval. Its local USER packet validation mode checks an existing folder and exact timestamped ZIP for ZIP-beside-folder placement, duplicate ZIP entry rejection, folder/ZIP file-list plus content-hash parity, stale sibling ZIPs, stable ZIP drift, one-primary-review-file layout, file-class layout, stale-stage language, unresolved placeholders, generated USER-facing technical metadata in root, USER Review, and Review Aids, and final packet proof reporting. These identity checks must use helper output and copied source content rather than putting raw commit, branch-status, or baseline metadata into USER-facing files. Active branch status, current commit, current baseline, validation status, PR state, worktree status, ZIP SHA256, ZIP hash, packet hash, upload hash, and similar byte-proof metadata belong in helper output, validator output, Codex chat digest, or external governance state. If the local USER hub cannot be written, the zip cannot be written, local packet validation fails, or packet decision-path validation fails, stop on `USER Review Hub Missing`, `USER Review Packet Stale`, `Branch Planning Review Packet Missing`, or `Branch Planning Packet Decision Path Conflict` with the exact helper command USER can run. |
| `dev/orin_user_review_bundle_false_green_fixture_validation.py` | Helper Status: Reusable | USER review packet false-green fixture validator | Reuse after changing `dev/orin_user_review_bundle.py` or packet-proof generation rules. It builds temporary known-bad local USER packet fixtures and proves the validator rejects empty primary USER review files, stale active Review Aid PR-readiness language, wrong primary/current decision file references, copied external branch state that still says packet regeneration is pending, stale copied Source Truth Context `Source Repo HEAD` values in the final ZIP, copied branch-state / branch-plan current metadata disagreement, UDL false closure while the final ZIP still contains a known currentness defect, folder-green / ZIP-stale drift, focused/cropped screenshot proof without full-desktop proof, duplicated full-desktop screenshot bytes, and missing required FAM-007 live-proof manifest checks. It must not mutate repo source, external state, USER packets, runtime files, or sibling worktrees. |
| `dev/orin_source_owner_marker_validation.py` | Helper Status: Reusable | source-owner marker validator | Reuse for repo-wide source-owner marker checks before adding another marker validator. It validates `NEXUS-SOURCE-OWNER` syntax, `source-owner-v1` schema, allowed owner categories, marker-to-ledger consistency, required first-pass high-risk marker coverage, duplicate marker surfaces, production UI comment-only placement, Compact-AI protected unique-commit / fold-down posture, and worktree/rebinding proof posture while preserving the Element Validation Ledger as canonical and treating markers as source-only backlinks. |
BP1 packet false-green guard: `dev/orin_user_review_bundle.py` validation must reject generated USER-facing BP1 files that still contain stale Workstream Entry final-decision or compatibility wording, the `Must-Not-Do / Regression-Risk Rules` heading, or command-wall `Do not` bullets. BP1 packet boundary language should frame private/runtime/provider/cache/memory/PR/merge/release/cleanup/sibling actions as future-gated USER decisions, pending approvals, and regression-risk controls.

FAM-007 BP2 packet false-green guard: `dev/orin_user_review_bundle.py` validation must reject a FAM-007 Dev/Owner Skeleton Readiness packet when `USER_BRANCH_PLAN_REVIEW.md` is the primary BP2 decision file but still contains BP2-preview / pending-BP1 wording or lacks the integrated Dev/Owner, Edition/Lane, Dev Readiness, Owner Readiness, Private Root / Remote, GitHub Desktop Binding, Backup / Import, Provider / Runtime / Cache / Memory Deferral, Watermark / Identity, Proof / Validation, and Future USER Gate matrices required by the accepted BP1 Option A direction. The same guard must inspect the supporting `USER_BRANCH_VISION_REVIEW.md`; for a BP2-primary packet it must read as accepted or waived BP1 context, not an active Draft / Pending USER Review BP1 gate, and must reject stale wording such as final BP1 acceptance pending, BP1 remains open, or pending USER acceptance or waiver. BP2 primary text must not describe itself as a PR Readiness packet in the exact BP3 approval section.

FAM-007 Dev/Owner H1 handoff guard: `dev/orin_public_leak_prevention_validation.py` and `dev/orin_user_review_bundle.py` must prove the FAM-007 Dev/Owner Skeleton Readiness successor can move from Workstream Green to Hardening H1 Green without reusing the older Breakpoint-only H1 packet path. The fixture must include direct H1 comparison lanes for action gates, exact USER decision proof, Dev/Owner matrices, private root/remote and GitHub Desktop safety, backup/import deferral, provider/runtime/cache/memory deferral, packet/fixture/validator/source-truth fold-down, and external-state proof. The packet helper must classify the exact next decision as `hardening-final-review` only when H1 is green and Live Validation LV1/no-visible-runtime proof remains pending USER approval.

FAM-007 Dev/Owner LV1 handoff guard: `dev/orin_public_leak_prevention_validation.py` and `dev/orin_user_review_bundle.py` must prove the FAM-007 Dev/Owner Skeleton Readiness successor can move from Hardening H1 Green to Live Validation LV1 Green without falling back to generic PR Readiness wording or the older Breakpoint-only LV1 packet path. The fixture must include direct LV1 no-visible-runtime proof, UTS waiver evidence, preserved private/runtime/provider/cache/memory gates, and PR Readiness Stage 1 analysis-only handoff proof. The packet helper must classify the exact next decision as `live-validation-final-review` only when LV1 is green and PR Readiness Stage 1 remains pending USER approval.

| `dev/orin_branch_readiness_planning_fixture_validation.py` | Helper Status: Reusable | governance regression fixture validator | Reuse whenever Branch Readiness product-system planning gates change. It validates checked-in fixtures under `dev/fixtures/branch_readiness_planning/`, proving that a shallow Live Validation product-plan fixture fails while a concrete whole-system planning fixture passes, including `Runtime Branch Engineering Contract`, `Current Runtime Baseline:`, `Planned Runtime Delta:`, `User-Facing Runtime Delta:`, `State / Config / Schema Delta:`, `Validator / Helper Delta:`, `Workstream Seam Map:`, `Proof Expectations:`, and `Plan-To-Implementation Traceability:`. It also validates Branch Runtime Engineering Plan fixture semantics for active external branch plans and repo historical receipts, proving valid detailed plan structure, invalid shallow runtime plan wording, invalid backlog planning sprawl, valid PR fold-down / retention posture, valid Branch Vision Contract, valid USER Branch Plan Review Gate, invalid missing user-facing outcome, invalid missing USER Review Packet Finding, invalid missing Hardening plan, invalid missing Live Validation / UTS plan, invalid first-seam-only review packet, USER Review Response / Codex Response Digest markers, valid deferred/future branch-plan scope, valid multi-item USER Feedback Disposition, valid Element-to-Phase Proof Matrix coverage, invalid missing Hardening proof path, invalid missing Live Validation proof or waiver path, valid deferred/future boundary handling, invalid BP2 product/design contract wording, valid Branch Planning review-gate state separation, invalid packet validation treated as USER acceptance, invalid USER-facing packet active branch metadata, invalid USER-facing packet ZIP/hash metadata, invalid Desktop/OneDrive active review or upload path wording, valid overlap intent, invalid missing high-risk intent, invalid fallback-only PASS after the Effective Point, valid low-risk WARN with fallback evidence, and valid/invalid Branch Change Intent Ledger fixtures for the Rebaseline Overlap Intent Gate, so future governance changes cannot silently weaken planning adequacy, engineering traceability, feedback preservation, vision control, element proof planning, branch-plan review, USER packet metadata boundaries, USER Review Gate state separation, or overlap-intent gates. |

Branch Planning helper/validator reinforcement: review-bundle helpers and planning fixtures must enforce `USER_BRANCH_VISION_REVIEW.md` as the BP1 Branch Vision Contract and `USER_BRANCH_PLAN_REVIEW.md` as the BP2 Branch Plan Contract. BP1 must include Project Vision Context, Family Vision Context, Feature Vision Context, Branch Goal, End-State Vision, what USER will see and where, functional flow, Surface Map, Codex line-item recommendations, USER response, Codex digest, accepted Branch Vision, deferred/future-gated ideas, design assumption ledger, and decision area. BP1 validation must reject template-shell review artifacts, copied-file-list-only surface maps, generic accept/revise/waive/reject option lists, generic Codex recommendations, broad non-decision-driving USER questions, and runtime-item-selection shells that ask USER to name the concrete branch outcome, choose the strongest implied outcome, choose a narrower surface path, or pick a broader owner/family impact path instead of presenting the actual branch-specific vision. Generated BP1 packets must present applied branch-specific vision content rather than instructions for what a section should contain; when the branch is a runtime lane, the packet must name the real runtime item and its user-visible behavior, state model, proof expectations, product options, recommendations, and open design questions. BP2 must include accepted BP1 trace, implementation package summary, Branch Scope Size Test, Slice/SLC/seam plan, affected surfaces, likely files, validators/helpers, proof requirements, H1/LV/UTS expectations, rollback/safety plan, risks, future-gated boundaries, plan acceptance checklist, and exact BP3 approval text. BP2 validation must reject packets that merely repeat BP1/product-design wording, list markers, point USER at copied files, or present generic implementation options without an applied engineering plan. BP3 validation must reject helper-green, first-seam-only, or generic command-wall packets that do not prove orchestration readiness against accepted or waived BP1/BP2. BP3 and Workstream approval helpers must describe approval as bounded Workstream execution for the admitted same-branch package with an entry seam or initial seam sequence; phrases that treat the first seam as terminal authority, such as `Seam 1 only` or `first bounded Workstream seam only`, are invalid unless an explicit USER single-seam waiver or backlog split is recorded. Dogfood fixtures should cover at least a user-visible UI/runtime lane such as FAM-006, a private-boundary/decision-readiness lane such as FAM-007, and a governance-only source-truth lane so future changes prove the standard across product and non-product branches. Validators must reject implementation approval while BP1 or BP2 Contract Status is Draft, Pending USER Response, Pending Codex Digest, Pending USER Confirmation, missing, stale, rejected, or unwaived; only Complete or Waived by USER may unlock BP3 and implementation approval. They must also preserve SLCs as Slice-level engineering route details inside a branch, not seams, backlog identities, or automatic separate branches.

Runtime focus selection reinforcement: when USER asks Codex to select, recommend, or present options for the next runtime focus, `dev/orin_user_review_bundle.py`, Branch Readiness planning packets, and `dev/orin_branch_readiness_planning_fixture_validation.py` must survey the full family/runtime source-truth space before issue evidence can influence the recommendation. The packet must load family vision, backlog/roadmap posture, workstream and branch-history receipts, completed recent work, remaining runtime layers, and external selected-next state before loading GitHub issue evidence. Issue evidence may be mapped as future BP2/BP3 proof input after USER selects a real runtime focus, but it must not become BR2/BP1 branch identity by helper special case or Codex inertia. The recommendation must name a concrete user-visible feature outcome that USER can inspect; labels such as `Execution Foundation`, `Persistence Foundation`, `Infrastructure Foundation`, `schema`, `hydration`, or `groundwork` are allowed only as supporting implementation scope inside the named feature outcome. Fixture coverage must reject runtime-focus packets where issue #258 or any single issue thread is the selection source before the neutral family/runtime survey, and must reject infrastructure/foundation-labeled recommendations that do not state a concrete feature outcome.

Family Feature Vision / Deferred Feature Carryforward / Cross-FAM Dependency validator guidance: extend existing reusable helpers before adding a new root validator. `dev/orin_user_review_bundle.py` must surface the relevant Family Feature Vision as BP1 `Feature Vision Context` when one exists and must keep copied feature-vision files under `Source Truth Context`. `dev/orin_branch_readiness_planning_fixture_validation.py` must add fixtures for selected feature-bearing branch routes that lack required Family Feature Vision records, shallow or placeholder Family Feature Vision records that fail the `Feature Vision Sufficiency Check`, valid `Family Feature Vision Not Applicable` non-product exceptions, BR2 option packets that include an applicability matrix for deferred carryforward items, option packets that ignore relevant deferred items, missing reason/dependency-trigger/grouping recommendation fields, Family Feature Vision wording that stores active branch-state terms, and branch recommendations that split applicable deferred items into tiny branches when one coherent FAM/package/validation path is supported. It must also include fixtures for cross-FAM dependency classification, shared surface overlap safe harbor, direct worktree-to-worktree mutation rejection, priority carry-in without scope capture, platform-contract adoption matrices, repo-wide migration / halt neutralization proof, transferred FAM work routing, affected-FAM receipt / fold-down expectations, and dependency candidate rows that name affected FFV / element or `Not Created` without creating live repo-state ledgers. The current fixture seam includes `valid_cross_fam_dependency_candidate.md` and `invalid_cross_fam_dependency_unclassified.md`, plus a current-worktree tracked-FFV content-file scan that skips the compact `Docs/family_feature_visions/index.md` registry and flags cross-FAM dependency wording when an FFV mentions affected FAMs but lacks a Cross-FAM Dependency record. This ensures existing FAM worktrees reconcile FFV content that belongs to or depends on another FAM without mutating sibling worktrees or repo live state. `dev/orin_branch_governance_validation.py` must enforce marker-first blockers such as `Family Feature Vision Required For Selected Feature`, `Deferred Feature Carryforward Review Missing`, `Deferred Carryforward Applicability Missing`, `Deferred Carryforward Branch Sprawl`, `Cross-FAM Dependency Undocumented`, `Cross-FAM Dependency Scope Unclassified`, `Cross-FAM Carry-In Not Evaluated At BR1`, `Transferred FAM Work Requires Owning Branch`, `Affected FAM Receipt Missing`, `Platform Contract Adoption Matrix Missing`, `Repo-Wide Migration Neutralization Missing`, `Worktree-To-Worktree Mutation Approval Missing`, `FFV Dependency Candidate Missing`, and `Cross-FAM Dependency Fold-Down Missing` only after source-truth content files and fixtures are USER-approved. Validators must not require repo docs to track live changed-file state, active branch status, active worktree assignment, PR state, release-window posture, selected-next truth, or mutable dependency queues; those facts remain external/Git/GitHub/helper-owned. This follow-up approves the fixture/current-FFV scan seam only; new Family Feature Vision content files, active FAM worktree mutation, external-state schema mutation, and broader helper/validator implementation remain separate USER decisions.

Implementation-Bearing Branch Planning Validation Invariant: Branch Readiness and Branch Planning helpers, templates, validators, and fixtures must prove that an admitted branch carries a concrete `Selected Implementation Route:` before BP1 begins. Fixture coverage must include an invalid planning-only lane/setup carrier, a valid BR2 blocker packet that identifies infrastructure or lane groundwork prerequisites and asks USER to approve groundwork, defer to a concrete route, retarget/rename, or hold, a valid infrastructure/setup route tied to a concrete Security/trust-boundary enforcement deliverable, a valid BP2 hold packet with an exact USER action gate and blocked scope, a valid retarget/rename recommendation when the current carrier name no longer matches the implementation route, an invalid SLC/Slice/Seam terminology ambiguity packet, a valid multi-slice implementation carrier, a required separate-branch case, and invalid fake-feature language where proof, readiness, setup, registry, packet, decision-path, or boundary-control labels are presented as the feature without naming the actual control or behavior to implement. Deterministic simulated-bypass coverage must also reject TBD/BP2-will-decide-later implementation output and BR2 blocker packets that list proof packets, readiness matrices, decision paths, or boundary-control labels as concrete routes. Validators must reject branches whose deliverable is only planning, readiness, lane setup, skeleton setup, registry creation, proof generation, boundary-label evidence, or choosing later branches; they must also reject SLC treated as a seam, SLC treated as a separate branch identity, slice treated as a proof artifact, seam treated as the branch deliverable, missing multi-slice FAM/package/route/owner/validation evidence, and `Dev lane` terminology in current planning text in favor of `Developer lane`. Helper output may recommend `continue`, `hold`, `retarget`, or `rename`, and BR2 may stop with a USER blocker packet while prerequisites are unresolved, but helpers must not call BP1/BP2/BP3 reviewable or green for a route that lacks a concrete deliverable, implementation output, setup relationship, USER action gate, route disposition, and real feature implementation behavior. When the local external state store is present, validators must also reject an active external branch state that routes directly to BP1 while its active branch plan lacks the implementation-bearing route fields, carries SLC/Slice/Seam terminology ambiguity, omits required multi-slice FAM/package/route/owner/validation evidence, or remains under a BR2 HOLD/RETARGET route-resolution packet.

Concrete USER-facing feature classification validator guidance: future machine-checkable coverage should extend `dev/orin_branch_governance_validation.py`, `dev/orin_branch_readiness_planning_fixture_validation.py`, and `dev/orin_user_review_bundle.py` before adding a new root validator. Branch plans, Workstream approval packets, Hardening/LV/UTS packets, PR Readiness packets, and release-facing packets should require planned-work and delivered-work classification rows using the categories from the `Concrete USER-Facing Feature Classification Gate`. The helpers should require a plain-USER concrete feature candidate when a branch is being treated as feature delivery, require `BP Hold Required` / bounded foundation-cycle classification when no concrete feature exists, and block PR Readiness when the branch is infrastructure/diagnostic/proof/governance-only unless USER separately approved foundation-only PR consideration. Fixture coverage should reject false feature-complete claims for runtime-visible status, no-provider result copy, blocked-state explanation, provider-visible-data `none`, capability unavailable copy, truthful `AI not implemented` copy, validators, proof helpers, USER packets, RAR/UIREF carry-ins, governance docs, or green validation by themselves. Coverage targets include `Concrete Feature Classification Missing`, `Concrete USER-Facing Feature Candidate Missing`, `Feature Delivery Overclaimed`, `Diagnostic / Status Work Masquerading As Feature`, `Proof / Packet Work Masquerading As Feature`, `Foundation Cycle Not Explicitly Approved`, `PR Readiness Blocked By No Concrete Feature`, and `Foundation-Only PR Consideration Approval Missing`.
| `dev/orin_public_leak_prevention_validation.py` | Helper Status: Reusable | FAM-007 public leak-prevention validator | Reuse for FAM-007 AI Edition public leak-prevention proof before Dev/Owner skeleton creation or public provider/model execution. It validates the accepted Protected Assets Table, public-safe fixtures, public review-bundle leak prevention, Workstream Entry packet decision-path consistency fixtures, edition-boundary manifest public-safe posture, public build exclusion/audit posture, Dev/Owner handoff criteria, Dev/Owner skeleton readiness action-gate proof, Breakpoint 2 Seam 1 action-gate registry proof, Breakpoint 2 remaining Workstream readiness proof, private/public boundary and private remote safety proof, exact USER decision proof, private repo/local-only setup remains USER-gated, GitHub Desktop private remote safety remains planning-only, off-boot backup/recovery planning remains USER-gated, public-to-private separation remains planning-only, provider/model deferral hardening, future handoff criteria, AI runtime/trust-boundary readiness Options 1 through 4, permission-state/provider-boundary readiness, deterministic-routing reliability readiness, Trust Journal and AI Operational Cache Governance readiness, capability-pack/local-only handoff readiness, synthetic blocked canaries for protected assets, and provider-boundary preservation without creating private repos, configuring private remotes, implementing backups/restores, implementing Public-to-Dev import, provider SDK/model execution, downloads, external calls, memory/learning/personalization, voice/Core sync, shortcut/installer work, or release artifacts. |

FAM-007 Owner AI Operational Foundation Gates extension: `dev/orin_ai_provider_state_validation.py` | Helper Status: Reusable | and `dev/orin_public_leak_prevention_validation.py` | Helper Status: Reusable | now directly validate the public-safe Owner AI gate contract for `feature/fam-007-owner-ai-operational-foundation-gates`, including SLC-001 through SLC-006, all eighteen accepted Workstream seams, protected artifact exclusion targets, provider/runtime disabled-state proof, cache-versus-memory consent separation, capability install-intent blocking, Developer/Owner lane readiness gates, Owner AI memory/agent prerequisite schema blocked states, Hardening H1 handoff posture, and continued denial of private setup, provider/model execution, runtime cache activation, real Owner memory, real Owner agents, private paths, secrets, protected artifacts, PR, merge, release, cleanup, and v1.8.0 work.
| `dev/orin_pr_body_quality_audit.py` | Helper Status: Reusable | GitHub PR body audit helper | Reuse for live GitHub PR body quality audits before creating another PR-body normalizer. It preserves exactly `## Summary` and `## What Changed`, removes repeated Summary/Purpose/Overview text from What Changed, rejects phase-digest/operator PR body firewall markers, removes `## Validation`, `## PR posture`, `## Branch Evidence`, Testing/Checks sections, deferred/exclusion wording, and generic defensive scope framing from GitHub PR bodies, writes local before/after backups outside the repo, and can apply normalized bodies through `gh pr edit` when USER-approved. PR Readiness Stage 2 / PR creation must run this helper against the proposed local PR body with `--body-file <path> --body-title "<PR title>"` before creating the GitHub PR, stop on `PR Body Drift Check Failed` if normalization would change the body or warnings exist, rerun green after normalization, verify the live PR body after creation with `--limit 1` or narrower equivalent, scan all visible PR bodies with this helper, and repair every nonconforming PR body inside the approved GitHub correction scope before reporting the PR-body standard green. The helper must preserve useful trimmed Summary detail by moving it into `## What Changed`; lossy summary trimming is invalid. |
| `dev/orin_pr_review_churn_validation.py` | Helper Status: Reusable | GitHub PR review-churn adversarial harness | Reuse when repeated same-family Codex Connector review comments expose helper/validator/parser churn. It paginates GitHub review threads and pull-review comments, collects Codex Connector review comments, clusters comments by failure family, validates `dev/fixtures/pr_review_churn/pr_276_rar_review_churn_matrix.json` style manifests, proves source-truth / implementation / fixture / generated-mutation / sibling-variant coverage, reports resolved/unresolved/outdated/current thread counts, checks changed helper/validator/parser file family coverage, honors current-head green approval comments/reviews and Codex Connector thumbs-up reaction latches, and emits the local Codex Connector simulation digest required before another `@codex` review request while `Review Churn Root-Cause Gate` is active. |

GitHub issue intake / closeout helper guidance: future helper or validator coverage should extend existing PR/branch governance helpers before adding a new root helper. Report-only coverage should scan open GitHub issues relevant to the current FAM/package, branch receipts, release-window contributors, UTS returned issue forms, diagnostics issue flows, and proposed PR bodies. It should report `GitHub Issue Relevance Scan Missing`, `Relevant Current-FAM Issue Not Dispositioned`, `Release Issue Closeout Inventory Missing`, `Carried GitHub Issue Not Dispositioned`, `Issue Closeout Live State Unknown`, `Issue Closeout Approval Missing`, `Issue Auto-Close Keyword Not Approved`, `Issue State Treated As Repo Truth`, and `Issue Mutation Approval Missing` without mutating GitHub unless USER separately approves that exact issue action. PR-body checks must treat `Fixes #`, `Closes #`, and `Resolves #` as auto-close mutation risk unless closeout approval is recorded.
| `dev/orin_release_body_validation.py` | Helper Status: Reusable | release body validator | Reuse for GitHub Release body format checks before creating another release-note validator. It paginates the GitHub Releases API and validates every published Nexus pre-Beta release body has a concise `Pre-Beta v<major>.<minor>.<patch>` title, starts with `Release Summary` / `Release Overview`, carries rich user-facing sections before generated `What's Changed` and `Full Changelog` content, and excludes internal automation/tooling brand tokens, generated branch-prefix noise, release-candidate anchor hashes, and internal source-truth/release-readiness governance phrasing from public release notes. |
| `dev/automation_observability_report.py` | Helper Status: Reusable | Automation Observability Source-of-Truth Report | Reuse for active automation audits before creating another automation watcher audit helper. It reads Codex automation run/inbox rows and `$CODEX_HOME/automations/*/memory.md`, reports `BLOCKER_CANDIDATE`, `REVIEW_REQUIRED`, and `REVIEW_INFO`, inspects configured cwd/worktree identity for active automations, flags `Automation CWD Worktree Mismatch` when a lane-sensitive automation runs from stale neutral main, a missing cwd, a parked folder, or the wrong worktree, treats automation memory/reporting mismatch as evidence requiring bounded repair rather than direct canon mutation, and treats background-observability-only automations as non-authoritative for PR/merge/release gates unless current runtime proof exists. Neutral-main background audits must require fresh main equality before reporting blockers. Stale toolchain-path findings are downgraded to `REVIEW_INFO` when source truth records the historical `dev/i/runtime/service.py` expectation as retired and no current owner reintroduces that path. |

### External Operational State

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_external_state_common.py` | Helper Status: Reusable | external operational state shared support | Shared Stage 1 scaffold for external-state path safety, canonical-root checks, manifest metadata, atomic writes, snapshots, and packet printing. Extend this module before duplicating external-state path, schema, version, lock, or audit support in another helper. |
| `dev/orin_external_state_init.py` | Helper Status: Reusable | external operational state bootstrap | Stage 1 scaffold for bootstrap packets and future `--apply` root initialization. Default mode is dry-run/report-only and must not create `C:\Nexus Governance State`; applied initialization requires later USER approval and must keep the root outside every Git worktree. |
| `dev/orin_external_state_report.py` | Helper Status: Reusable | external operational state report | Report-only helper for root existence, canonical-root posture, manifest metadata, and `External State Missing` / `External State Corrupt` evidence. It must not mutate external state or repo files. |
| `dev/orin_external_state_validation.py` | Helper Status: Reusable | external operational state validation | Local external-state validation scaffold for manifest fields, schema consistency, corrupt JSON, clean-clone-safe missing-root posture, approved Stage 4 migrated record presence, required markdown metadata fields, source repo HEAD alignment, released-lock posture, fail-closed modern target-set transaction journals, and exact legacy completed-audit compatibility. Production legacy admission is restricted by `dev/orin_external_state_legacy_receipt_compatibility.json` to three normalized audit paths plus exact raw SHA256, receipt class, immutable purpose, and completion profile identities, then requires coherent released-lock, workload, snapshot, target, and complete-row evidence. The selector uses parsed exact `Transition` equality; phrase-only notes are unrelated, while malformed JSON explicitly declaring the transition fails closed, including when quoted leading junk contains an earlier nonstructural brace. Legacy completion is exact normalized field-state matching, not a `pass`/`complete` substring search; every live row must prove the identity-bound profile and explicit historical-only rows must carry no live completion evidence. Missing root is informational unless `--require-root` is supplied for an approved local workflow. Use `--require-stage4-records` and `--expected-source-head <sha>` only for approved local Stage 5 / active-workflow validation. This helper is not wired into GitHub Actions repo validation and must not become a clean-clone requirement. |
| `dev/orin_external_state_lock.py` | Helper Status: Reusable | external operational state locks | Stage 1 scaffold for lock packets across state-root, migration, release-window, worktree, branch, review-bundle, fold-down, and governance-candidate scopes. Default mode is dry-run; applied lock writes require an initialized root and later USER-approved state use. |
| `dev/orin_external_state_snapshot.py` | Helper Status: Reusable | external operational state snapshot | Stage 1 scaffold for snapshot packets and future applied snapshots. Default mode is dry-run; applied snapshots require an initialized root and explicit approval for the relevant local operational workflow. |
| `dev/orin_state_fold_down_preview.py` | Helper Status: Reusable | external operational state fold-down preview | Report-only preview helper for external-state files that may need durable repo fold-down. It must not edit external state, repo docs, branch records, branch plans, or generated indexes. |
| `dev/orin_external_state_promote_preview.py` | Helper Status: Reusable | external operational state promotion preview | Report-only promotion preview helper that names source state, target state, reason, and required lock/version/conflict/validation/audit steps before any staging-to-central promotion. |
| `dev/orin_external_state_promote.py` | Helper Status: Reusable | external operational state promotion | Stage 1 promotion scaffold. Default mode is dry-run; applied promotion requires initialized external state, explicit `--apply`, and must preserve the staging source while writing audit evidence so the no-loss rule is not bypassed. |
| `dev/orin_repo_live_state_leakage_scan.py` | Helper Status: Reusable | external operational state repo leakage scan | Report-only Stage 4A helper that scans repo Docs for live operational state, classifies historical receipts, transition-legal current owners, migration candidates, and `Repo Live-State Leakage`, and prints the migration-map target owner for each live-state category. It must not edit repo docs, write external branch/worktree/release-window records, transition validators, or move/delete/archive files. |

Governance intake triage and digest profile source-checking is owned by `dev/orin_branch_governance_validation.py`. It must preserve `Docs/governance_intake_triage_and_digest_profiles.md`, the `Governance Intake Triage Packet`, the `Digest Non-Compaction Rule`, and the `Digest Profile` vocabulary (`Decision Packet`, `Return Digest`, `Validation Digest`, `Full Audit Packet`, and `Delta Digest`) so future governance reform can stay focused and avoid broad policy replay without compacting the selected digest.

PR watcher mode source-checking is owned by `dev/orin_branch_governance_validation.py`. It must preserve `Docs/pr_watcher_mode_contract.md`, the rule that bounded PR Readiness Stage 2 denies recurring PR watcher automation by default, direct PR verification as the normal live PR/comment/review/mergeability/merge-close proof path, the Direct PR2 Continuation Rule that blocks quiet handoff after revalidation requests or repair pushes, the exception-only PR Watcher Mode Contract modes (`Silent Monitor`, `Verify Once`, `Repair Mode`, and `Blocked Mode`), and the exception-only `Watcher Health Proof:` fields (`Watcher Mode:`, `Configured CWD:`, `Worktree / Branch:`, `PR:`, `Head SHA:`, `Mergeability:`, `Unresolved Review Threads:`, `Latest Bot Review:`, `Repair Authority:`, `Delivery Route Proof:`, `Runtime Proof:`, and `Next Watcher Posture:`) so future PR Readiness runs do not provision heartbeat, cron, same-thread, or fallback watcher automations by inertia or stop while direct PR verification can continue. It must also preserve the post-repair bot thumbs-up/approval latch: `Comment addressed` is repair evidence only, PR green/merge requires a later Codex Connector bot thumbs-up reaction or green approval comment after any bot-comment repair, approval proof must be bound to the current live PR head by review commit SHA, PR timeline order, or equivalent GitHub live-head evidence rather than local commit time, and Codex Connector review-request or revalidation PR comments must be 3-5 words only.

Repo-wide high-risk source owner marker adoption branch-authority checks remain owned by `dev/orin_branch_governance_validation.py`. Dedicated source-owner marker validation is now owned by `dev/orin_source_owner_marker_validation.py`, which checks marker syntax, marker-to-ledger consistency, limited pilot coverage, inventory-only dispositions, orphan/stale/duplicate marker risks, comment-only placement, and production UI exclusion while preserving the Element Validation Ledger as canonical and treating markers as backlinks only.

Vision Contract / Vision-to-Plan scaffolding is owned by `dev/orin_branch_governance_validation.py` and fixture-proven through `dev/orin_branch_readiness_planning_fixture_validation.py`. Current scaffolding validates Branch Vision Contract Snapshot marker shape, implementation-safe assumption states, USER Vision Green posture, non-blocking/seam-blocking/workstream-breaking question severity, Vision Question Digest readiness, Branch Plan Revision Packet USER-decision boundary, and vision-to-implementation traceability. Heavy enforcement against historical records remains a pending USER decision.

USER Feedback Disposition scaffolding is owned by `dev/orin_branch_governance_validation.py` and fixture-proven through `dev/orin_branch_readiness_planning_fixture_validation.py`. Current scaffolding validates UFD ledger marker shape, repeated `### UFD Item:` blocks, `UFD-*` namespace, canonical owner file, owner class, item status, USER decision state, Workstream severity, fold-down target, pointer-only posture, count consistency, exact-normalized duplicate `Feedback Summary:` blocking inside one active ledger, and `No Durable Owner Needed` no-action guardrails. Historical branch-plan enforcement is covered through `Docs/branch_plans/retirement_index.md` and `dev/orin_governance_efficiency_validation.py`; broader fuzzy semantic duplicate-feedback detection remains human-review territory unless future fixtures and false-positive review prove it safe.

FAM-007 setup contract readiness extension: the FAM-007 provider state validator now also owns setup contract readiness state/schema, provider profile/config requirements, setup/execution consent prerequisites, setup approval, execution approval, setup handoff, PR/release fold-down posture, desktop AI-owned readiness display suppression continuity, provider-visible-data none, prompt/model execution disabled, downloads/install blocked, network blocked, memory disabled, and voice/Core gated proof for `feature/fam-007-local-ai-provider-setup-contract-readiness`.
FAM-007 setup implementation foundation extension: `feature/fam-007-local-ai-provider-setup-implementation-foundation` extends the same FAM-007 provider state validator for local setup entry/write-path foundation, provider profile/config fail-closed validation, local/null fallback persistence proof, status/UI mapping, setup consent and execution consent prerequisites, future setup handoff, desktop readiness display suppression continuity, and continued provider-visible-data none / prompt-model execution disabled / downloads-network-memory-voice gated posture before any PR readiness claim.
FAM-007 consent collection foundation extension: `feature/fam-007-local-ai-provider-consent-collection-foundation` extends the same FAM-007 provider state validator for consent collection foundation state/schema, consent capture readiness, setup consent and execution consent boundary preservation, consent provenance/audit posture, provider-visible-data none, local-only data visibility posture, status/UI proof expectations, future consent handoff markers, static consent collection fixtures, and continued prompt-model execution disabled / downloads-network-memory-voice gated posture before any H1, PR readiness, or release-readiness claim.
FAM-007 consent collection implementation foundation extension: `feature/fam-007-local-ai-provider-consent-collection-implementation-foundation` extends the same FAM-007 provider state validator for completed local consent capture/write-path foundation proof, local consent capture transition schema, local consent record schema, consent record storage-boundary schema, durable-storage-deferred posture, local-only revocation/reset model, setup/execution consent separation, provenance/audit proof, hidden-telemetry UI/status proof, future provider setup handoff, functional-AI/v1.8.0 continuation criteria, missing/invalid/no-selection/revoked/reset/captured-local-only fail-closed fixtures, provider-visible-data none, sentToProvider false, canAcceptPrompts false, no-secrets posture, and continued prompt-model execution disabled / downloads-network-memory-voice gated posture before any H1, PR readiness, or release-readiness claim.
FAM-007 durable consent persistence foundation extension: `feature/fam-007-local-ai-provider-durable-consent-persistence-foundation` extends the same FAM-007 provider state validator for Seam Group A durable local consent record persistence, schema versioning, local storage-boundary proof, isolated temp-store read/write/load proof, corrupt/missing/invalid/stale/unsupported fail-closed fixtures, revocation/reset/expiry durable semantics, provenance/audit metadata, provider-visible-data none, sentToProvider false, canAcceptPrompts false, and continued prompt-model execution disabled / downloads-network-memory-voice gated posture. Seam Group B extends the same validator for setup/execution durable consent separation, setup-only, execution-only, both-absent, both-present, revoked-setup, revoked-execution, reset-setup, reset-execution, expired-setup, expired-execution fixtures, Core/Desktop/ORIN hidden-telemetry proof, desktop readiness display suppression continuity, and future handoff criteria before any H1, PR readiness, or release-readiness claim.
FAM-007 user-operated consent UX foundation extension: `feature/fam-007-local-ai-provider-user-operated-consent-ux-foundation` extends the same FAM-007 provider state validator for local consent UX state/schema, local intent schema, durable-consent-derived setup and execution display labels, revocation/reset local UX mapping, local-intent write posture, Core/Desktop/ORIN hidden status proof, desktop readiness display suppression continuity, provider setup/execution UX gates, functional-AI/v1.8.0 continuation criteria, default/blocked/setup-only/execution-only/both-present/revoked/reset/expired/setup-intent/execution-intent/revoke-intent/reset-intent/invalid-intent fixtures, provider-visible-data none, sentToProvider false, canAcceptPrompts false, and continued prompt-model execution disabled / downloads-network-memory-voice gated posture before any H1, PR readiness, or release-readiness claim.
FAM-007 setup completion foundation extension: `feature/fam-007-local-ai-provider-setup-completion-foundation` extends the same FAM-007 provider state validator for implemented local setup completion state/schema, setup eligibility, provider profile/config finalization, no-secrets posture, fail-closed persistence/reset semantics, setup/execution boundary alignment, Core/Desktop/ORIN setup completion status proof, desktop readiness display suppression continuity, future SDK/model handoff criteria, default/missing-config/invalid-config/consent-UX-required/profile-missing/profile-invalid/config-missing/config-invalid/no-secrets/validation-failed/reset/ready/future-handoff fixtures, provider-visible-data none, sentToProvider false, canAcceptPrompts false, and continued prompt-model execution disabled / downloads-network-memory-voice gated posture before any H1, PR readiness, or release-readiness claim.

### Desktop Runtime And Launcher

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_ai_provider_state_validation.py` | Helper Status: Reusable | FAM-007 provider state validator | Reuse for static FAM-007 no-provider/provider-privacy, provider-selection/consent-boundary, disabled Assisted Desktop no-provider interaction, local provider registry/configuration state, provider-visible-data disclosure, runtime-foundation provider-boundary state, provider runtime contract schema/reason/provenance/fail-closed state, provider readiness/setup eligibility contract state, provider activation foundation state/gate/adapter/execution criteria state, provider execution-readiness state/gate/provider-path/adapter-selection/prompt-model proof state, provider path/consent readiness state/config/profile/setup proof state, setup/consent-flow readiness state/schema/gate/display-suppression proof state, consent collection foundation state/schema/gate/status proof state, hardware/capability planning, capability-pack lifecycle planning, data/memory/consent/audit/secrets planning, Windows resilience planning, persona/Core/voice boundary, validation proof-gate, and no-provider fallback state checks before creating another local-AI/provider-state validator. It now covers local-only/no-provider fallback semantics, visible provider/privacy/selection/consent/registry/configuration/foundation-readiness/runtime-foundation state, provider runtime category and reason-code matrix, provider readiness state/setup eligibility/setup blocker/readiness reason/readiness provenance/readiness schema versioning, provider activation state/eligibility/blocker/reason/provenance/schema versioning, provider execution-readiness state/eligibility/blocker/reason/provenance/schema versioning, provider path readiness state/eligibility/blocker/reason/provenance/schema versioning, setup flow readiness state/eligibility/blocker/reason/provenance/schema versioning, consent flow readiness state/eligibility/blocker/reason/provenance/schema versioning, consent collection state/eligibility/blocker/reason/provenance/schema versioning, desktop AI-owned readiness display hidden/suppressed proof, provider path selection posture, adapter selection posture, null-local-adapter posture, provider profile metadata, provider configuration envelope, setup and execution approval posture, setup consent and execution consent posture, consent blocker/reason/provenance/schema versioning, provider-visible-data requirement posture, data classification posture, audit envelope posture, setup eligibility/capability/manifest/safety blocker posture, future activation/setup/SDK/consent handoff markers, prompt/model/provider execution gates, prompt send disabled posture, provider-visible execution data posture, functional-AI criteria, v1.8.0-prebeta readiness criteria, safe default config, missing/invalid config fail-closed posture, consent/config transitions, capability-pack eligibility, manifest validity, install-intent posture, provider-boundary interaction plan fields, consent-boundary copy, provider-visible-data detail, disabled provider setup next action, no configured or available providers before consent, no hidden external calls, prompt-acceptance gating, no memory persistence/indexing/retrieval/learning, no model workload execution, safe static hardware snapshot posture, unprobed RAM/disk posture, blocked capability-pack downloads/installs/updates/uninstalls, checksum/signature/compatibility markers, blocked network egress, no-secrets posture, disabled voice runtime, Core/Desktop copy-contract and golden-fixture markers, Core/Desktop/ORIN execution-readiness, provider path/consent readiness, setup/consent-flow readiness, setup foundation, and consent collection foundation copy markers, FAM-007 readiness/activation/execution/provider-path/setup-flow branch source-truth checks, and Workstream Green proof coverage through the provider setup and consent flow readiness layer plus this consent collection foundation layer. |
| `dev/orin_ai_control_center_live_resize_validation.py` | Helper Status: Workstream-scoped | FAM-007 AI Dashboard parent-only visual/function validator | Owner Workstream: FAM-007 AI Dashboard / AI Control Center runtime UI repair. Reason Reusable Helper Was Not Extended: the reusable HUD human-client validator is FAM-006-specific and proves the Dashboard launch/tray flow, while this branch needs direct FAM-007 parent-dashboard proof for disabled category doorways, clean product-facing copy, clean content-fit-or-scroll geometry, OS-mouse resize, no fake detached child-window launches, provider/model blocked truth, the clean Main-runtime old AI Control Center comparator, exhaustive element-group visual grammar audit JSON/Markdown, and focused/full-desktop evidence without mutating sibling HUD helpers or protected main. Consolidation Target: future reusable Nexus product-window live visual/function validator that can parameterize window title, launch route, proof roots, edge/corner expectations, content-fit/scroll expectations, doorway/child-window disposition, accepted comparator source, and element-group grammar audit requirements. Promotion Decision Point: before PR Readiness fold-down, decide whether this remains branch-family-specific evidence or is folded into a reusable product-window validator. |
| `dev/orin_fam007_visual_acceptance_target_packet.py` | Helper Status: Workstream-scoped | FAM-007 visual acceptance target packet generator/validator | Owner Workstream: FAM-007 AI Dashboard / AI Control Center visual target gate. Reason Reusable Helper Was Not Extended: this pass is branch-local and depends on the active FAM-007 external state, current proof roots, accepted historical packet retention, and the branch's AI Dashboard / AI Control Center surface taxonomy. Consolidation Target: future reusable visual-target packet helper after Governance/FAM-002 defines a global Visual Acceptance Target template and validator contract. Promotion Decision Point: before PR Readiness fold-down or when a second branch needs the same rendered visual-target packet contract. |
| `dev/orin_desktop_entrypoint_validation.py` | Helper Status: Reusable | desktop startup and entrypoint validator | Extend for non-live startup, tray, overlay-entry, VBS/launcher/runtime launch-chain fallback proof, plain `main.py` direct-launch handoff proof, launch-chain preflight and cleanup isolation, PATH-based Python fallback validation, runtime marker coverage, actual desktop shortcut equivalence proof, generated current-worktree shortcut proof when the real desktop shortcut points at a different governed worktree, `NEXUS_DESKTOP_VALIDATION_SHORTCUT_PATH` override handling, shortcut target/working-directory reporting, real user-operated tray action proof where feasible, tray Exit confirmation/timing proof, active-owner PID identity/stale-reused PID relaunch proof, and per-step Codex precheck manifest support. Fake/callback/offscreen proof must be labeled separately from real-client proof, and generated-equivalent shortcut proof must not be described as true real desktop shortcut proof unless the actual shortcut is aligned or USER waives the mismatch. |
| `dev/orin_desktop_launcher_healthy_validation.py` | Helper Status: Reusable | launcher healthy-path validator | Extend for launcher healthy startup regression coverage. |
| `dev/orin_desktop_launcher_regression_harness.py` | Helper Status: Reusable | launcher regression harness | Repair-gated reusable: current file still references absent legacy `nexus_*` launcher/target paths; repair to current `orin_*` paths or explicitly bypass before using it as launcher regression proof. After repair, extend for launcher scenario regression coverage before creating another launcher harness. |
| `dev/orin_launcher_live_window_audit.ps1` | Helper Status: Reusable | live window audit helper | Extend or parameterize for launcher/window live validation before creating another launcher live audit helper. |
| `dev/orin_overlay_input_capture_helper.py` | Helper Status: Reusable | overlay input helper | Reuse for overlay input-capture support instead of duplicating low-level input helpers. |

### Monitoring And HUD

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_fam006_false_accept_regression_gate.py` | Helper Status: Workstream-scoped | FAM-006 false-ACCEPT regression gate | Owner Workstream: FAM-006 Dashboard Recording / Recording Studio / minimal Log Viewer returned-UTS self-adjudication repair. Rejects known-bad Studio visual/adjudication packets before USER review, enforces substantive root-cause rows, internal visual red-team finding/disposition rows, packet-relative evidence maps, crop completeness, resize/fixed-size proof boundaries, and forbidden progress-language green claims. Consolidation Target: future reusable UI evidence false-green regression gate after Governance/global validator approval. Promotion Decision Point: before PR Readiness or final fold-down, decide whether this remains FAM-006 branch evidence or becomes reusable visual-proof enforcement infrastructure. |
| `dev/orin_fam006_unified_defect_ledger.py` | Helper Status: Workstream-scoped | FAM-006 unified defect ledger gate | Owner Workstream: FAM-006 Dashboard Recording / Recording Studio / minimal Log Viewer returned-UTS false-green elimination repair. Creates and validates the branch-local Unified Defect Ledger, false-green incident ledger, known-bad corpus coverage, packet-contained UDL artifacts, current-owned blocking-defect status, and explicit Governance-candidate disposition for defects outside the FAM-006 branch carrier. Helper PASS is evidence only; it must be paired with source-truth review and packet evidence. Consolidation Target: future reusable unified defect ledger / false-green incident gate after Governance/global validator approval. Promotion Decision Point: before PR Readiness or final fold-down, decide whether this remains FAM-006 branch evidence or becomes reusable visual-proof and packet-proof enforcement infrastructure. |
| `dev/orin_monitoring_hud_surface_validation.py` | Helper Status: Reusable | monitoring HUD surface validator | Reuse for static Monitoring/HUD visual-surface markers, desktop-mode activation, runtime telemetry-boundary markers, renderer-owned placement markers, controls-visibility markers, fail-safe/no-data/degraded-status behavior markers, slice-boundary copy, no-hardware-polling/no-settings-persistence/no-fake-hardware-values/no-recovery-claims/no-voice boundary checks, no expanded retired legacy user-facing identity, Stage 2-R13 Dashboard-first Workstream handoff source-truth markers, Stage 2-R12 Dashboard-first Interface Release Boundary source-truth markers, WS31 Dashboard-only acceptance baseline markers, WS32 Dashboard standalone movement/clipping/Core-Overlay decoupling markers, WS33 dashboard settings/control content and monitor-group clarity markers, WS34 provider/setup/no-data/degraded truth and warning posture controls markers, WS35 dashboard-specific proof refresh and Live Validation UTS boundary markers, SLC-037 Overlay Profile state-foundation markers, SLC-038 Overlay Profile visible 300px-to-450px selector plus settings-window create/rename/save/discard proof, SLC-039 Overlay Profile settings-window monitor membership mapping proof, SLC-040 Manage Monitors read-only Overlay Profile context proof, SLC-041 Overlay Profile focused validation/live-proof readiness, returned-UTS Overlay Profile selector-first/create-first settings flow, visible-monitor search/filter, max-five visible-row target, NDAI-native list scrollbar, no normal outer settings-window scrollbar marker, red danger action placement/styling, condensed Manage Monitors Overlay context, removed Manage Monitors settings-route button, selected Monitor Group detail hierarchy, follow-up returned-UTS implemented proof for NDAI filter/profile dropdown styling, max-five menu targets, disabled Edit until explicit profile selection, profile Delete danger confirmation, Dashboard Manage Monitors button parity, row-only clickable Assigned Overlay status/assignment surface below the Sensor Source card, selected-source count/settings guidance attached to the Sensor Source card, Enabled for Overlay checkbox removal, source-list sensor settings entry points, Polling Rate override disclaimer where implemented, null-state and 100+ item Overlay Profile selector stress proof with exactly five visible NDAI-scrollbar options and unclipped menu geometry, H1 Dashboard-first hardening source-truth markers, Dashboard-specific acceptance/proof separation, Stage 2-R11 dashboard-versus-minimal-HUD architecture expectations, WS18/WS30 Core desktop-layer/non-blocking ownership markers, WorkerW coordinate-rebase proof, no Core topmost foreground ownership, WS19-WS35 historical/supporting markers, and Overlay/display deferred/non-gating proof classification before creating another HUD surface validator. |
| `dev/orin_fam006_feature_studio_visual_proof.py` | Helper Status: Workstream-scoped | FAM-006 feature-studio pre-Live Validation visual proof capture | Owner Workstream: FAM-006 Dashboard Recording / Recording Studio / minimal Log Viewer returned-UTS visual repair. Captures pre-LV focused screenshots for Recording Studio and Log Viewer default, active, hover/focus, pressed, disabled/blocked, and Log Viewer width-resize states; creates focused crops, a comparator contact sheet, and row-to-evidence mapping under the USER-inspectable OneDrive screenshots root. This helper is pre-Live Validation evidence only and must not be used to clear renewed exact-desktop-shortcut Live Validation or UTS acceptance by itself. Consolidation Target: future reusable feature-studio visual proof helper after more FAMs consume the same unique-child/studio taxonomy. |
| `dev/orin_fam006_full_desktop_false_green_review.py` | Helper Status: Workstream-scoped | FAM-006 full-desktop visual false-green packet generator and gate | Owner Workstream: FAM-006 Dashboard Recording / Recording Studio / minimal Log Viewer returned-UTS visual repair. Generates the branch-local USER review packet after a focused/cropped visual packet is contradicted by full-desktop evidence; copies the rejected packet into the known-bad corpus, purges the USER hub, includes full-desktop media, rejected focused media, accepted reference media, row-specific root-cause ledgers, USER visual-defect classifications, child-window placement doctrine, and visual/placement option boards. The helper validates that future process packets do not omit full-context proof, genericize the false-green root cause, omit USER-reported defects, omit packet-contained media, or imply H1/LV/UTS/PR green. It is not a runtime UI repair helper and must not be used to claim visual acceptance by itself. Consolidation Target: future reusable full-context visual false-green packet gate after Governance/global approval. |
| `dev/orin_monitoring_hud_live_validation.ps1` | Helper Status: Reusable | monitoring HUD live desktop validator | Workstream/Hardening/Live Validation helper for FAM-006 Monitoring HUD proof. Reuse for Dashboard-first WS31+ live proof, WS32 dashboard standalone travel/clipping/decoupling proof, WS33 dashboard settings/control content and monitor-management clarity proof, WS34 provider/setup/no-data/degraded truth and warning posture controls proof, WS35 dashboard-specific full virtual-desktop screenshot proof plus Live Validation UTS boundary proof, SLC-038 Overlay Profile focused WebView proof and 300px-to-450px selector UTS checks, SLC-039 Overlay Profile focused WebView proof and settings-window membership mapping UTS checks, SLC-040 Manage Monitors read-only Overlay Profile context proof and UTS checks, SLC-041 Overlay Profile focused validation/live-proof readiness, returned-UTS Overlay Profile selector-first/create-first proof, visible monitor search/filter proof, max-five row/overflow proof, danger action state proof, condensed Manage Monitors Overlay context proof, removed Manage Monitors settings-route proof, follow-up returned-UTS proof for NDAI filter/profile dropdown styling, five-visible option menus, null-state and 100+ item Overlay Profile selector stress proof with exactly five visible NDAI-scrollbar options and unclipped menu geometry, manager Create/Edit/select flow, profile Delete, Manage Monitors button parity, row-only clickable Assigned Overlay status/assignment surface below the Sensor Source card, selected-source count/settings guidance attached to the Sensor Source card, Enabled for Overlay removal, source-list sensor settings entry points, refreshed UTS checks, H1 Dashboard-first hardening pressure-test proof, LV1 Stage 1 active-client proof plus formal UTS handoff, launched-desktop marker proof, Core desktop-layer/non-interference runtime markers, Dashboard/Overlay/Core independence evidence, raw PNG evidence copies under `C:\Users\anden\OneDrive\Pictures\Screenshots\Nexus Desktop AI\fam_006_monitoring_hud_live_validation\<stamp>\`, mandatory detailed focused per-element screenshot copies under `C:\Users\anden\OneDrive\Pictures\Screenshots\Nexus Desktop AI\fam_006_monitoring_hud_live_validation\<stamp>\focused_element_screenshots\element_<label>_<state>.png`, mandatory per-element visual inventory output that names every current user-facing window, border/frame, card, row, page break/divider, background treatment, scrollbar, button, dropdown, checkbox, input, chip, status field, confirmation, empty/error/deferred state, and USER issue-specific element, temporary issue-form-to-screenshot mapping for returned UTS or screenshot/video issues, mandatory short MP4/frame-sequence evidence generated from ordered focused screenshots for LV1 desktop UI handoff, `-RunInteractionSelfQA` live HUD control exercise, `-RecordingOptionCSelfQA` focused Recording Option C lane that validates only the current/new/affected Dashboard Recording card, Recording Studio, minimal Log Viewer shell, native/export boundary, active Overlay Profile target mirror, and issue #258 target-reliability evidence when prior HUD elements are unaffected, `-ActiveUserFacingClient` foreground/user-observable validation, interaction manifest output, no-progress watchdog, millisecond artifact-root uniqueness, cleanup verification, Dashboard-first self-QA that does not require Overlay/display unanchor/card-drag/card-resize acceptance, real-client proof-class separation, per-step Codex Precheck PASS/FAIL/NOT TESTED/WAIVED output in manifests/source truth, compact USER-questionnaire UTS generation, and future Overlay/display proof only when that interface is re-admitted or USER grants explicit interface-bundle approval. Formal `C:\Nexus USER\UTS - <worktree-label>.txt` export is exclusive to Live Validation Stage 1 and requires `-PrepareLiveValidationUserTestSummary`; `C:\Nexus USER\User Test Summary.txt` remains a template/layout example only. The UTS handoff must stay a short step-based questionnaire with closing additions, while issue ledgers, proof-class detail, helper internals, and broad traceability digests stay in manifests/source truth. Workstream and Hardening proof runs must not refresh or digest UTS. Helper success, fake/offscreen state, callback proof, screenshots, marker proof, direct-runtime active-client proof, sandbox/offscreen proof, and generated-equivalent shortcuts must not be collapsed into real user-operated tray proof or true actual-desktop-shortcut proof unless the actual shortcut targets the active worktree or USER explicitly waives the mismatch. Helper PASS, marker PASS, screenshot existence, video existence, manifest existence, or USER execution waiver cannot clear the Codex Visual Adjudication gate; LV1 must separately record artifact-by-artifact product-vision verdicts before USER handoff, must fail or repair when screenshot names do not visually prove the named state, and full-desktop screenshots are context only, not per-element acceptance proof. Remaining gaps before package-complete claims: returned LV1 UTS acceptance or waiver digestion, PR Readiness, and final package closeout. |
| `dev/orin_monitoring_hud_human_client_validation.ps1` | Helper Status: Active / Required before FAM-006 LV1 handoff | monitoring HUD human-client desktop validator | WS48/WS49+ helper for the actual USER-like path. Launches the canonical red FAM-006 desktop shortcut `C:\Users\anden\OneDrive\Desktop\FAM-006 RED - Nexus Desktop AI Launcher.lnk` by default or a governed `NEXUS_DESKTOP_VALIDATION_SHORTCUT_PATH` equivalent when USER approves an override, resolves the shortcut target and working directory before launch, records `shortcutResolution`, and requires `shortcut_targets_active_worktree=PASS` so the helper fails fast if the shortcut is not rooted in the active FAM-006 worktree at `C:\Nexus Worktrees\FAM-006`. It uses visible Windows tray/menu actions, exact native tray menu item rectangles by label, broadened tray icon resolution with timeout-safe native tray/popup probes, mouse/cursor click/drag evidence, Dashboard open/close/move/resize checks, real-shortcut first-open screenshot-sequence proof for #123 recurrence with visual-continuity analysis, Dashboard IA-card Settings open/Done close proof, Settings double-click native-maximize guard proof, window-level Dashboard Close hide/reopen proof, Dashboard rounded-corner native-mask proof by placing a white validation backdrop behind the Dashboard and sampling the native window corner exterior pixels to catch black rectangular corner bleed, rounded-mask-clipped resize hit-test proof so transparent clipped corners cannot be over-credited as usable resize rails, normal-speed Dashboard move-fluidity proof with native GetWindowRect position sampling, pre-click Windows resize-cursor discovery with user-like mouse-move stimulation before cursor sampling, central 50 percent rounded-corner diagonal resize-zone proof, native GetWindowRect high-cadence resize sampling, cursor-to-window tracking-lag thresholds, high-refresh cadence proof, NCP tray icon left-click open/close proof, tray menu Open/Close Command Overlay state proof, Command Overlay closeout before resize proof, Dashboard repositioning away from the lower-right tray menu cleanup zone after intentional resize enlargement, NCP Create Custom Task/Create Custom Group/Manage Custom Tasks/Manage Custom Groups clicks while Dashboard is visible/moved, tray Create Custom Task duplicate-dialog guard proof, visible shutdown-confirmation check, timeout/cancel/accept proof, virtual-desktop screenshot-frame artifacts, focused rect screenshots, USER-inspectable context screenshot copies under `C:\Users\anden\OneDrive\Pictures\Screenshots\Nexus Desktop AI\fam_006_human_client_validation\<stamp>\context_desktop_screenshots\`, USER-inspectable focused element screenshot copies under `C:\Users\anden\OneDrive\Pictures\Screenshots\Nexus Desktop AI\fam_006_human_client_validation\<stamp>\focused_element_screenshots\element_<label>.png`, mandatory short MP4/frame-sequence proof generated from ordered human-client screenshots before a PASS manifest, fallback cleanup for non-UIA dialogs, and a JSON manifest at `dev/logs/fam_006_human_client_validation/latest_manifest.json`. Tray menu actions must resolve inside the native tray popup or exact native menu-coordinate fallback so in-app buttons cannot be over-credited as tray proof; native tray and popup discovery must fail with bounded diagnostic evidence instead of hanging after shortcut-settled proof; issue #140 requires the helper to prove NCP tray icon toggle and tray Open/Close state before PR Readiness. This helper is Workstream/Hardening proof infrastructure only; it must not generate or digest the formal UTS. The Live Validation helper must not export a green UTS unless this human-client manifest is PASS with shortcut/worktree alignment, Settings/window-level Close/rounded-corner mask/first-open visual-continuity/normal-speed movement/high-refresh resize-fluidity proof, NCP tray toggle/state proof, USER-inspectable screenshot/video evidence under the OneDrive screenshots root, or USER explicitly waives the missing proof. |
| `dev/orin_fam006_workstream_readiness.py` | Helper Status: Workstream-scoped | FAM-006 Workstream readiness validator | Owner Workstream: FAM-006 Monitoring/HUD Option C recording runtime. Reason Reusable Helper Was Not Extended: this helper binds the accepted SLC-051 through SLC-055 route, active Dashboard Recording Start/Stop, Recording Studio focused control/status surface, minimal Log Viewer native/export folder shell, runtime-owned local file writing/readback, issue #258 Overlay Profile persistence proof, and Dashboard Recording card proof to the FAM-006 branch identity. Consolidation Target: fold durable generic Workstream readiness checks into reusable branch governance or runtime validation helpers after this branch closes. Promotion Decision Point: before PR Readiness or final package closeout, decide whether to keep as FAM-006 historical proof or extract reusable checks. |
| `dev/orin_fam006_hardening_h1.py` | Helper Status: Workstream-scoped | FAM-006 Hardening H1 validator | Owner Workstream: FAM-006 Monitoring/HUD Option C recording runtime. Reason Reusable Helper Was Not Extended: H1 pressure-tests the active FAM-006 Dashboard Recording card, active Dashboard Start/Stop, Recording Studio, minimal Log Viewer shell, native/export log boundary, runtime-owned local file writing/readback, issue #258 Overlay Profile restart persistence hydration, recording-target mirror, visual-system sampling, and future-gated tray/keybind/full-export/provider/model/Native Log Loader boundaries as one branch-specific hardening package. Consolidation Target: fold reusable visual/regression gates into `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, or the governed runtime validation suite after this branch closes. Promotion Decision Point: before PR Readiness or final package closeout, decide whether branch-specific H1 evidence remains historical or becomes reusable helper coverage. |
| `dev/orin_fam006_visual_conformance_ledger.py` | Helper Status: Workstream-scoped | FAM-006 stop-the-line visual conformance ledger validator | Owner Workstream: FAM-006 Monitoring/HUD Option C recording runtime. Reason Reusable Helper Was Not Extended: this helper records the returned-UTS stop-the-line element ledger for Recording Studio and Log Viewer, enforces the approved final-disposition vocabulary, rejects vague visual-green language, rejects stale Studio markers, and preserves the Current Branch Active Repair Ledger plus Historical / Previous Branch Issue Candidate Ledger for the active FAM-006 packet hygiene repair. Consolidation Target: fold durable generic visual-ledger, packet-hygiene, and same-class primitive checks into `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, or a reusable visual proof helper after this branch closes. Promotion Decision Point: before PR Readiness or final package closeout, decide whether this helper remains FAM-006 historical evidence or becomes reusable visual-conformance infrastructure. |
| `dev/orin_fam006_live_validation_forensics.py` | Helper Status: Workstream-scoped | FAM-006 Live Validation / UTS failure investigation packet generator | Owner Workstream: FAM-006 Monitoring/HUD Option C recording runtime. Reason Reusable Helper Was Not Extended: this helper is a bounded investigation-support tool for the post-LV1 / UTS handoff failure; it preserves raw old-tool output, stable finding IDs, evidence-path audits, runtime-proof rerun manifests, focused screenshot visual-adjudication notes, combination-matrix gaps, code-lineage summaries, phase attribution, expected-red validator-first product-state runs, validator self-check fixtures, and the USER review findings/repair packets without repairing product/runtime behavior, accepting UTS, or implementing prevention planning. It must treat a runtime proof rerun as investigation evidence only, require the approved reconciled baseline and prior investigation helper head to remain ancestors of current HEAD, keep helper-path proof separate from normal USER-path proof, and separate verified seeded/real-OS proof from still-unproved USER-created profile, restart, pre-session folder-button, window-focus sequence, and screenshot-label proof gaps. Consolidation Target: after USER reviews the findings, fold durable evidence-path, visual-adjudication, expected-red user-path, and combination-coverage checks into `dev/orin_monitoring_hud_live_validation.ps1`, `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, or `dev/orin_user_review_bundle.py` as source truth selects. Promotion Decision Point: before PR Readiness or final package closeout, decide whether this helper remains historical investigation evidence, is deleted, or is promoted into reusable Live Validation forensic tooling. |

FAM-006 Repair Implementation Approval Packet Addendum: `dev/orin_fam006_live_validation_forensics.py` also owns the timestamped `repair-implementation-approval-review` packet mode for this FAM-006 expected-red repair lane. The mode must copy the accepted validator-first packet, rerun the expected-red product-state checks, preserve the 4 FAIL / 3 BLOCKED / 5 UNPROVEN posture until runtime repair proves normal USER-path green, record accepted Start/Stop ownership direction, record accepted Recording Studio native-log / Log Viewer shell direction, and keep product/runtime fixes, Live Validation acceptance, UTS acceptance, PR Readiness, issue closeout, merge, release, Governance/FAM-007/neutral-main mutation, provider/model/private work, and future-gated full Log Viewer/export/tray/keybind/settings/Native Log Loader implementation withheld until separate USER approval.
| `dev/orin_dashboard_rounded_corner_mask_probe.py` | Helper Status: Active / Called by FAM-006 human-client validation | Dashboard rounded native-window mask probe | Subhelper for issue #137 and later Dashboard rounded-corner regressions. It receives the live Dashboard HWND from the real red-shortcut human-client validation path, places a temporary white backdrop behind the Dashboard, brings the Dashboard forward, captures a virtual-desktop bitmap artifact, samples native window corner-exterior pixels and visible Dashboard interior pixels, and fails if corner samples are dark/opaque rather than showing the white backdrop. This exists so CSS-rounded marker proof cannot over-credit a still-rectangular native window. |
| `dev/orin_monitoring_hud_internal_sandbox_validation.py` | Helper Status: Reusable | monitoring HUD internal sandbox validator | Use for FAM-006 internal product proof. Validates Dashboard-first Workstream handoff posture, WS31 Dashboard-only acceptance baseline posture, WS32 Dashboard standalone movement/clipping/Core-Overlay decoupling posture, WS33 Dashboard settings/control content and monitor-group clarity posture, WS34 provider/setup/no-data/degraded truth and warning posture controls, WS35 dashboard-specific proof refresh and Live Validation UTS boundary, SLC-037 Overlay Profile default/migration/persistence/concept-boundary proof, SLC-038 Overlay Profile 300px-to-450px selection plus settings-window create/rename/persistence proof, SLC-039 Overlay Profile membership mapping persistence/proof, SLC-040 Manage Monitors read-only Overlay Profile context proof, SLC-041 Overlay Profile focused validation/live-proof readiness, returned-UTS selector-first/create-first state proof, visible-monitor search/filter state proof, membership save/discard preservation, danger-state proof, condensed Manage Monitors context proof, removed Manage Monitors settings-route proof, follow-up returned-UTS manager state/dropdown/delete/assignment/sensor-settings planning and implementation proof, H1 Dashboard-first hardening pressure-test posture, LV1 Stage 1 handoff posture, LV2 explicit USER waiver digestion posture, the standalone HUD shell/module files, ORIN Core render-file isolation, dashboard-specific settings/control behavior, provider-truthful telemetry contracts, 1s polling floor, visual warning posture, no fake GPU/thermal values, retired-name sterilization, and Overlay/display deferred/non-gating classification without treating User Test Summary results as a Workstream or Hardening gate. Historical WS18-WS30 markers remain supporting repair evidence, not current Dashboard acceptance by themselves. Remaining gaps before package-complete claims: follow-up returned-UTS H1/refreshed LV1, PR Readiness, and final package closeout. |

FAM-006 LV1 Reviewability Addendum: `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, and `dev/orin_monitoring_hud_live_validation.ps1` now also own Overlay Profile detail-actions and delete-confirmation reviewability proof gates. A focused proof cannot pass solely because confirmation DOM is open; the action footer and confirmation block must be visible inside the Overlay Profile settings window. After the final refreshed LV1 PASS, the remaining package-complete gap is returned USER UTS acceptance or waiver digestion, followed by PR Readiness and final package closeout.`

FAM-006 HUD-Wide Visual Inspection Matrix Addendum: `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, `desktop/desktop_renderer.py`, and `nexus_visual/monitoring_hud.js` now own the returned USER visual-inspection repair gate. H1 and LV1 proof must prove `runMonitoringHudVisualInspectionMatrixProof`, `hudWideVisualInspectionMatrix`, `buttonGlowUniformity`, `defaultButtonGlowUniformity`, `semanticHoverColorPreserved`, `buttonTextDeadSpacePass`, `visualInspectionScopeCovered`, `pageBreakVisualInspection`, `backgroundBleedClippingInspection`, `sourceSettingsFocusNoGold`, `rowTitleTabsInspected`, `responsiveWindowContract`, `perElementVisualInventory`, `issueFormCoverageMatrix`, and `scope=buttons-dropdowns-rows-chips-fields-page-breaks-backgrounds-bleed-clipping-scaling` before a Codex Visual Adjudication PASS. The matrix must inspect current HUD windows, borders/frames, cards, buttons, dropdown toggles/options, rows, row-title tabs/insets, chips, fields, page breaks, background glow/graphics, scrollbars, bleed-through, clipping, scaling, source-row hover persistence, Source Settings focus/Shift posture, nested source-settings return flow, and dirty-guard coverage. Returned USER issue-form IDs, including UTS-HUD-018 through UTS-HUD-021, must map to named focused screenshots and a visual verdict. Helper PASS, marker PASS, screenshot existence, DOM presence, or USER execution waiver alone cannot clear visual acceptability. Live Validation helpers must fail closed and return `REPAIR` rather than exporting a UTS handoff when Codex-visible defects remain; when bounded continuation is approved, helpers and source truth must support the repair/rerun loop until the visual matrix is green before USER acceptance review.`

FAM-006 Returned-UTS Focused Retest Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, and `nexus_visual/monitoring_hud.js` now also own failed-issue-only UTS handoff discipline after USER returns partial PASS/REPAIR. USER-confirmed PASS issue IDs must be closed unless regression appears, active failed issue IDs must map to explicit proof gates, and the desktop UTS handoff must list a brief issue list, active issues to test, and issue-regression checks instead of replaying every historical step. The current FAM-006 follow-up gates include `checkedControlHoverAffordance`, `sameMonitorRowDirtyGuard`, `defaultProfileDeletePersists`, `defaultDeletePersistsWithoutAutoRecreate`, `overlayManagerScaling`, `windowSelectorSameRow`, `windowSelectorStandardFootprint`, `windowSelectorMenuUnclipped`, `windowSelectorResponsiveCompact`, and `dividerGlowReduced50Percent`, with proof copied to the USER-inspectable OneDrive screenshots directory and a mandatory short video before returning to USER_TEST_REQUIRED.`

FAM-006 Returned-UTS Determinism Stop-Loss Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `dev/orin_monitoring_hud_surface_validation.py`, and `dev/orin_fam006_live_validation_forensics.py` own the returned-UTS failure-review stop-loss for the Dashboard Recording / Recording Studio / minimal Log Viewer package. Before a future formal LV1 UTS export can pass, the live validator must prove or explicitly block the five returned issue classes: `RETURNED_UTS_PROFILE_LOG_CONSISTENCY_GATE`, `RETURNED_UTS_RECORDING_STUDIO_MANUAL_BUTTON_GATE`, `RETURNED_UTS_LOG_VIEWER_VISUAL_SYSTEM_GATE`, `RETURNED_UTS_USER_VISIBLE_STORAGE_MODEL_GATE`, and `RETURNED_UTS_RECORDING_STUDIO_UI_ACTIVATION_GATE`. Helper foreground proof, native direct-launch proof, seeded/sandbox proof, marker PASS, manifest PASS, screenshot existence, or generic shell presence cannot clear these gates. After the bounded FAM-006 repair, these gates require product-backed proof of normal USER paths, generated native log contents against selected profile/monitor set, visual-system adjudication, user-visible folder model, and Studio UI activation dependency; any regression must reopen the gate and block UTS handoff rather than treating prior PASS markers as durable acceptance.

FAM-006 LV1 Picture-Proof Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `desktop/desktop_renderer.py`, and `dev/orin_monitoring_hud_surface_validation.py` must treat code state, DOM values, callbacks, markers, logs, and manifest fields as supporting diagnostics only. Proof is only proof when the USER-facing claim is visible in a named focused screenshot, full-window screenshot where full-window context is the claim, or short-video/frame-sequence artifact in the USER-inspectable evidence folder. A Live Validation claim that a user-facing element exists, accepts text, changes state, opens a window, saves a profile/log, mirrors a target, or visually matches the accepted system cannot pass unless the photo/video artifact visibly proves that exact state. Any claim that cannot be proven in a photo or video must be elevated to the USER as a manual validation item instead of being accepted from code, marker, DOM, or helper output. The default Recording Option C lane must include focused screenshots for the current-branch Recording Card, Quick Access Start/Stop, Recording Studio, Log Viewer shell, native log save/readback, Log Viewer lifecycle combinations, active Overlay Profile selector, Recording target mirror, and `02_recording_option_c_focused_lane_complete`. Full Overlay Profile create/edit/save/restart proof is required only when current branch diff, source truth, or a dedicated dependency-persistence lane marks that workflow affected; in that case the dedicated lane must include the created-draft, keyboard-edited profile-name, saved profile/Recording target mirror, and restart persistence mirror screenshots before UTS export. If the picture does not show the claimed result or the visual result looks wrong, the helper must fail closed and return to repair rather than accepting the code/manifest claim.

FAM-006 LV1 Deterministic Action-Effect Matrix Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `desktop/desktop_renderer.py`, and `dev/orin_monitoring_hud_surface_validation.py` must derive a user-facing action/effect matrix from the accepted BP/Workstream scope, changed files, affected surfaces, and current source truth before claiming Live Validation green. For every admitted current/new/affected control, button, selector, input, window launcher, stateful row/card, or dependent mirror, Live Validation must prove the visible control exists, perform the normal USER-path interaction where applicable, then visibly prove the expected effect and every logical dependent UI state that should change. Examples include but are not limited to: a Start/Stop button must prove the action started or stopped recording, the button label/enabled state changed correctly, the Recording card status changed, the Recording Studio state changed when open, and the native/export log state updated; a profile selector must prove the selected profile changed, the Recording target mirror changed, and restart persistence remains correct; a window-launch button must prove the requested non-child window visibly opens and has the expected chrome/state without accidentally triggering adjacent actions. DOM/code/manifest claims are only diagnostics. The proof artifact must include named screenshots or short-video frames for the before state, direct action result, and dependent changed surfaces, or the helper must classify the gap as `NOT TESTED`, `BLOCKED`, or `REPAIR` instead of exporting a UTS handoff. Existing unaffected surfaces are retested only when changed-file or source-truth impact analysis marks them affected, but every admitted current-branch surface must be inventoried and either proven, explicitly waived by USER, or held as a blocker.

FAM-006 LV1 Returned-UTS Visual And Cleanup Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `dev/orin_monitoring_hud_surface_validation.py`, `dev/orin_monitoring_hud_internal_sandbox_validation.py`, and `desktop/desktop_renderer.py` must distinguish control/window-open proof from visual-system proof for the Dashboard Recording Card, Recording Studio, and Log Viewer. A focused screenshot proving that Recording Studio or Log Viewer opened cannot clear Live Validation unless the same evidence also shows the accepted Nexus/FAM-006 UI contract: inherited chrome, color, shape, typography, row/card treatment, button styling, spacing, hover/focus/disabled states where relevant, and compact density. If USER reports the window is visually out of family, validators must reopen the visual-system gate even if native-window creation, focus, or marker proof passed. The same lane must preserve and restore USER state after validation-created profiles, monitor groups, selections, folders, or other local objects; generated stress-test profiles or monitors are cleanup obligations, not USER data. Profile-target and Start/Stop log claims may close only when native log/readback evidence proves the selected/created Overlay Profile and active monitor data were recorded in the log contents, not merely when the UI mirror or Start/Stop marker passed. Confirmed closed items, such as Overlay Profile persistence and card-holder inset parity when USER confirms them, should not be replayed in failed-issues-only UTS handoffs unless regression appears.

FAM-006 LV1 Exact USER Desktop Launcher Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `dev/orin_monitoring_hud_human_client_validation.ps1`, `desktop/orin_desktop_main.py`, and `dev/orin_monitoring_hud_surface_validation.py` must launch Live Validation through the exact USER Desktop shortcut path `C:\Users\anden\OneDrive\Desktop\FAM-006 RED - Nexus Desktop AI Launcher.lnk`. The required proof method is visible user-level desktop control: Codex must visibly click, double-click, or keyboard-open the USER desktop shortcut from a visible selected Desktop folder item, and record photo/video proof of the launched runtime, tray/menu/window path, and each admitted action/effect check. The governed human-client helper, old-style real-cursor movement, or another visibly proven mouse/keyboard path may be the primary proof route when it preserves before/during/after evidence and exercises the actual USER-facing control. Computer Use is optional supporting tooling only, not a required method, and may be used only when it is target-safe and stable. Visible user-level desktop control may use visible mouse movement, left-click, right-click, double-click, and keyboard input only against a visible target; hidden app activation calls, direct handler calls, internal signal dispatch, synthetic tray/menu activation, launcher foreground signals, scripted shortcuts that bypass the visible control, and direct function calls are not proof that the tray icon, button, menu item, or window control surface is usable. The required escalation order is reliable visible user-level desktop control through the governed human-client or equivalent visible mouse/keyboard path first, optional Computer Use only when target-safe and stable, and single-path manual USER waiver last. Tray proof must use a deterministic visible-target acquisition loop to manually and visually locate the intended tray icon or menu item in the current screenshot before clicking: capture the current visible screen/window, inventory accessible elements and screenshot-visible controls, derive candidate targets from label/icon/proximity/state evidence, exercise only user-equivalent input against the visually tied candidate, and verify the expected visible result before reusing that target model. If the tray/taskbar target is visible but not exposed as a targetable window, the helper/Codex pass may use bounded `Visible Desktop Coordinate Control` before asking for USER waiver: slowly move the real Windows cursor to the screenshot-tied target, send real OS-level mouse down/up or drag input, preserve before/during/after photo/video or ordered-frame evidence, and verify the resulting visible menu/window/state. Tray proof must also preserve this exact blocker language: blind full-desktop, taskbar, tray, hidden-overflow, or notification-area coordinate probing is forbidden. If the Nexus tray icon or required menu item cannot be safely identified from visible evidence or visible movement cannot verify the visible result, the helper/Codex pass must stop with an exact manual USER validation blocker instead of probing nearby coordinates. USER-assisted coordinate calibration may unblock one local proof attempt, but it must be recorded as temporary environment evidence and cannot replace repeatable Codex-owned target discovery, source-truth rules, or future proof. The human-client helper may preflight or support the pass by visibly clicking, double-clicking, or keyboard-opening the USER desktop shortcut through an already-visible desktop shell icon, an already-visible Desktop folder item, or a bounded File Explorer select/open fallback and recording `visible_desktop_shortcut_double_clicked=PASS`, but it must not use desktop-wide `MinimizeAll`, show-desktop, or broad window-state disruption merely to find the shortcut. Scripted UIAutomation or SendInput output is not primary proof by itself unless it satisfies the visible desktop coordinate control evidence contract. Shell-executing the `.lnk` with `Start-Process`, launching the VBS target directly, or using a private/dev launcher is supporting diagnostics only. The helper must resolve the shortcut target and working directory, prove both are rooted in `C:\Nexus Worktrees\FAM-006`, record the shortcut resolution in the manifest, and fail closed when the exact shortcut is missing, points at another worktree, cannot carry the required LV configuration, or lacks visible-click proof. Direct `desktop/orin_desktop_main.py`, generated-equivalent shortcut, private launcher, helper-only, callback, direct-runtime launch proof, environment-injected self-QA proof, or scripted helper-click proof is supporting diagnostics only and cannot clear LV1, UTS handoff, or returned-UTS repair proof unless USER explicitly receives and accepts the manual-validation blocker. Restart-persistence proof must also relaunch through the exact USER Desktop shortcut rather than a private/direct runtime path. For the FAM-006 Option C Recording lane, `recording_studio_visible_button_opens_native_window=PASS` and `log_viewer_studio_visible_button_opens_native_window=PASS` are required before the formal UTS export; button existence, renderer code, markers, direct JavaScript requests, scripted click output, or callback output cannot prove the windows open.

FAM-006 Dropdown / Selection Volume Stress Addendum: `dev/orin_monitoring_hud_live_validation.ps1`, `desktop/desktop_renderer.py`, `nexus_visual/monitoring_hud.js`, `dev/orin_monitoring_hud_surface_validation.py`, and `dev/orin_monitoring_hud_internal_sandbox_validation.py` must fail closed when any current user-facing dropdown or multi-selection/list surface lacks both null-state proof and high-volume stress proof where that surface can contain variable user/runtime content. Required proof includes no item / no selection state where legal, 100+ item state for variable profile/group/source/monitor surfaces, max-five visible option/row policy where admitted, NDAI scrollbar styling, same-row or intended-placement geometry, menu/list clipping bounds, readable labels/options, and no full-row over-expansion unless that full-row layout is explicitly the admitted design. Fixed small option sets such as Polling Rate still require placement, open/close, clipping, and scroll/overflow proof but are exempt from fabricated 100+ options. Helper PASS, screenshot existence, or middle-volume fixtures cannot satisfy this addendum.`

FAM-006 Branch-Local Visual Acceptance Target Addendum: `dev/orin_fam006_visual_acceptance_target_packet.py` is `Helper Status: Workstream-scoped` for the active `feature/fam-006-dashboard-recording-start-stop-local-file` returned-UTS visual repair lane. It generates and validates a self-contained USER packet containing Design Candidate Render media, full desktop/context renders, actual rendered state contact sheets, element legends, a Visual Selection Ledger template, a Draft Branch Visual Acceptance Target, a Rejected Patterns Ledger, UDL / false-green status, source-truth conflict classification, validation-output evidence, and a Reusable Design Recipe template before additional visible Recording Studio / Log Viewer product UI implementation. The helper must fail the packet when state renders, full desktop/context renders, element legends, selection ledgers, rejected patterns, source-truth conflict classification, UDL / false-green status, validation outputs, packet media, or packet hygiene are missing. Final ZIP SHA proof must separate pre-ZIP packet validation from external post-ZIP manifest proof because embedding the final ZIP SHA inside the ZIP changes the ZIP. The helper must keep render authority levels distinct: Concept Render is brainstorming, Design Candidate Render is USER-selection evidence, Visual Acceptance Target is pending until USER accepts/revises it, and Implementation Match Proof must come later from actual implemented UI screenshots/video. It must not claim renewed Live Validation, UTS acceptance, PR Readiness, implementation approval, runtime UI repair, or reusable program-wide shared primitive/template implementation. This FAM-006 branch now carries the source-truth promotion of durable Visual Acceptance Target doctrine into `Docs/phase_governance.md`, `Docs/branch_plans/README.md`, and this registry under USER-approved branch/worktree-local governance repair authority; sibling worktrees still adopt that doctrine only after merge/rebaseline through their own legal carriers.

FAM-006 Full-Desktop False-Green Option Packet Addendum: `dev/orin_fam006_full_desktop_false_green_review.py` is `Helper Status: Workstream-scoped` for the active `feature/fam-006-dashboard-recording-start-stop-local-file` false-green repair lane. It generates and validates the branch-local full-desktop false-green USER review packet after rejected packets prove focused crops, text-card options, selected-direction semantic drift, fake Log Viewer doorway semantics, oversized selected A2 action wells, proof-copy leaks inside product renders, selected-render bottom dead-space, or mismatched child-window chrome/control pills were overaccepted. The helper must preserve known-bad packet evidence for `FAM-006-20260624-121535.zip`, `FAM-006-20260624-130151.zip`, `FAM-006-20260624-132551.zip`, `FAM-006-20260624-135010.zip`, `FAM-006-20260624-142638.zip`, and `FAM-006-20260624-145849.zip`, purge and regenerate `C:\Nexus USER\FAM-006`, produce exactly one timestamped ZIP, include actual packet-contained media, and fail if A1/A2/A3 nested-card inheritance renders, B1/B2/B3 full desktop placement diagrams, C1/C2/C3 Log Viewer doorway renders, corrected Log Viewer doorway shell render, selected render contract evidence, full-desktop comparison media, window-control/chrome comparison media, bottom-dead-space comparison media, selected-direction summary, UDL / false-green ledgers, source-truth context, or validation-output records are missing. Visual/spatial option packets must not be text-only; every claimed validation output in the packet must include command, cwd, timestamp, exit code, PASS/FAIL, stdout, and stderr. Once USER selects A2 revised / B2 / corrected Log Viewer doorway shell, the helper must fail if the packet regresses to open recommendation wording, omits rejected/deferred dispositions, captures dirty pre-commit Git proof without final clean post-push proof, loses `OPEN LOG VIEWER`, lets A2 render an oversized bottom action/control well, leaves visible bottom dead space beneath the final selected action row, omits accepted-reference compact window-control/chrome proof, adds A2 bottom helper copy, selects C2 inline/right-aligned row actions, omits `VIEWER - Deferred`, omits bottom `OPEN NATIVE LOGS` and `OPEN EXPORTED LOGS` actions, puts helper/proof copy inside the selected Log Viewer product surface, displays fake native/export data rows, or implies local path default display / graph / export customization / previous-log selection / native-log reading from Recording Studio / direct exported-log opening from Recording Studio / fake full-viewer workspace. The helper does not approve runtime UI repair, H1, Live Validation, UTS, PR Readiness, PR creation, merge, release, or global Governance mutation.

FAM-006 Dual Recording Candidate / Log Viewer Rename Addendum: `dev/orin_fam006_runtime_ui_immersion_options.py` is `Helper Status: Workstream-scoped` for the active `feature/fam-006-dashboard-recording-start-stop-local-file` returned-UTS visual repair lane. It generates and validates a review-only USER packet when USER asks for final candidate-selection evidence before choosing the Recording Studio implementation shape. The helper must inspect actual AI Control Center / HUD Dashboard HTML/CSS/selector/token values as branch-local comparators only, keep AI Control Center out of permanent global-law promotion, render exactly the admitted Recording Studio candidates REC-A and REC-C with START / PAUSE / STOP plus OPEN LOG VIEWER, explicitly exclude REC-B, render the shared LOG-A-based surface as `Log Viewer` with `VIEWER - Deferred` and bottom OPEN NATIVE LOGS / OPEN EXPORTED LOGS actions, and use title-first unique-studio hierarchy (`RECORDING STUDIO` / `LOG VIEWER` first, supporting description beneath) while citing Overlay Profile Settings and Manage Monitors only as compact child-window treatment references. Rendered button candidates must use the AI Control Center / HUD content-fit button contract unless the packet records a source-truth exception: 31px height, 11px label text, equal 14px left/right label buffer, matching vertical buffer, and same row label/value font, color, and underglow family as the comparator row grammar. The helper must include actual media and comparison boards, include exact A/C delta rows, include rename-to-Log-Viewer proof, include resize behavior disposition, include a future FAM-007 convergence note, and fail if the packet is text-only, lacks code-level grammar comparison, displays fake Log Viewer data rows or local paths by default, puts proof/helper copy inside product UI, omits UDL/false-green status, resurrects REC-B or generic LOGS grammar, or claims final winner selection, runtime implementation match, H1, Live Validation, UTS, PR Readiness, PR creation, merge, release, or global Governance promotion. The false-ACCEPT regression gate recognizes this packet as a candidate-selection packet so it is not forced through B2 implementation-proof schema while still preserving known-bad replay and USER packet hygiene.

FAM-006 REC-A + Log Viewer Implementation-Match Addendum: `dev/orin_fam006_reca_log_viewer_implementation_match_packet.py` is `Helper Status: Workstream-scoped` for the active `feature/fam-006-dashboard-recording-start-stop-local-file` selected-direction implementation-match repair lane. It generates and validates the USER-reviewable runtime implementation-match packet after USER selects REC-A Recording Studio and the renamed Log Viewer direction. The helper must preserve the accepted candidate-selection ZIP as decision evidence, purge and regenerate `C:\Nexus USER\FAM-006`, copy actual runtime proof media from `dev/orin_fam006_feature_studio_visual_proof.py`, embed source-truth context, include START / PAUSE / STOP proof, include `OPEN LOG VIEWER` route proof that opens Log Viewer rather than native/export folders directly, include `VIEWER - Deferred` and bottom `OPEN NATIVE LOGS` / `OPEN EXPORTED LOGS` proof, include B2 placement proof, include target-vs-actual and implementation-match defect ledgers, include UDL / false-green artifacts, run the visual conformance ledger against the packet-contained row map, run false-ACCEPT known-bad replay, and generate exactly one new timestamped implementation-match ZIP while not deleting the accepted candidate-selection ZIP. It must fail if required proof is local-path-only, cropped-only, target-only, missing full-desktop context, missing route proof, missing B2 proof, stale `Log Viewer Studio` runtime naming appears where the current surface must read `Log Viewer`, generic `LOGS` routing appears, fake data rows/local path display/full viewer/export customization/previous-log selection appears, validation outputs are missing command/cwd/timestamp/exit/stdout/stderr evidence, or the packet claims H1, Live Validation, UTS, PR Readiness, PR creation, merge, release, cleanup, or global Governance acceptance.

### Shared Action, Interaction, And Saved Actions

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_shared_action_baseline_validation.py` | Helper Status: Reusable | shared action baseline validator | Extend for shared action catalog and resolution baseline checks. |
| `dev/orin_interaction_baseline_validation.py` | Helper Status: Reusable | interaction baseline validator | Extend for typed-first overlay, confirm/result, and command interaction baseline checks. |
| `dev/orin_saved_action_source_validation.py` | Helper Status: Reusable | saved-action source validator | Extend for saved-action source loading, corruption, and source safety checks. |
| `dev/orin_saved_action_authoring_validation.py` | Helper Status: Reusable | saved-action authoring validator | Extend for non-UI saved-action authoring and validation behavior. |
| `dev/orin_saved_action_authoring_ui_validation.py` | Helper Status: Reusable | saved-action authoring UI validator | Extend for Qt/dialog authoring behavior that can be proven without a full live desktop session. |
| `dev/orin_saved_action_authoring_live_validation.py` | Helper Status: Reusable | saved-action authoring live validator | Extend for saved-action authoring live proof that does not require the larger interactive PowerShell suite. |
| `dev/orin_saved_action_authoring_interactive_validation.ps1` | Helper Status: Reusable | saved-action authoring interactive suite | Extend for full saved-action authoring interactive desktop coverage before creating another authoring live suite. |
| `dev/orin_saved_action_authoring_interactive_runtime.py` | Helper Status: Reusable | saved-action authoring interactive runtime support | Reuse as support for the interactive authoring suite. |
| `dev/orin_callable_group_execution_validation.py` | Helper Status: Reusable | callable-group validator | Extend for deterministic callable-group execution checks. |
| `dev/orin_callable_group_execution_interactive_validation.ps1` | Helper Status: Reusable | callable-group interactive suite | Extend for callable-group interactive proof before creating another callable-group live helper. |

### External Trigger Intake

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_external_trigger_intake_validation.py` | Helper Status: Reusable | external trigger intake validator | Extend for FB-039 trigger intake, registration, lifecycle state transitions, decision evidence snapshots, boundary state snapshots, readiness inspections, registry readiness sweeps, registry readiness summaries, registry readiness detail snapshots, bounded invocation follow-through, malformed-input pressure tests, blocked-category precedence, duplicate non-mutation, result immutability, and negative-path checks before creating another external-trigger helper. |

### Toolkits, Boot, Diagnostics, Support, And Voice

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_boot_monitor_preflight.py` | Helper Status: Reusable | boot preflight | Extend for boot monitor preflight checks. |
| `dev/orin_boot_toolkit_validation.py` | Helper Status: Reusable | boot toolkit validator | Extend for boot toolkit validation. |
| `dev/orin_boot_transition_capture.py` | Helper Status: Reusable | boot transition capture | Extend for boot transition capture rather than creating duplicate capture helpers. |
| `dev/orin_boot_transition_verification.py` | Helper Status: Reusable | boot transition verification | Extend for boot transition verification. |
| `dev/orin_desktop_toolkit_validation.py` | Helper Status: Reusable | desktop toolkit validator | Extend for desktop toolkit validation. |
| `dev/orin_dev_toolkit_smoke_validation.py` | Helper Status: Reusable | dev toolkit smoke validator | Extend for dev toolkit smoke checks. |
| `dev/orin_diagnostics_report_issue_validation.py` | Helper Status: Reusable | diagnostics issue validator | Repair-gated reusable: current file still references legacy `nexus_diagnostics.pyw`; repair to current `orin_diagnostics.pyw` or explicitly bypass before using it as diagnostics issue/report proof. After repair, extend for diagnostics issue/report validation. |
| `dev/orin_recoverable_launch_failed_validation.py` | Helper Status: Reusable | recoverable diagnostics validator | Extend for bounded recoverable launch-failed diagnostics. |
| `dev/orin_startup_snapshot_harness_validation.py` | Helper Status: Reusable | startup snapshot validator | Extend for startup snapshot harness validation. |
| `dev/orin_support_bundle_triage_toolkit_validation.py` | Helper Status: Reusable | support triage toolkit validator | Extend for support bundle triage toolkit validation. |
| `dev/orin_support_bundle_triage.py` | Helper Status: Reusable | support triage helper | Reuse for support bundle triage logic. |
| `dev/orin_support_bundle_triage_harness.py` | Helper Status: Reusable | support triage harness | Extend for support bundle triage harness coverage. |
| `dev/orin_voice_regression_harness.py` | Helper Status: Reusable | voice regression harness | Extend for voice-path regression coverage. |

### Utilities And Manual Support

| Path | Helper Status | Family | Reuse / Consolidation |
| --- | --- | --- | --- |
| `dev/orin_seeded_saved_action_review.ps1` | Helper Status: Reusable | saved-action seeded review | Reuse for seeded saved-action review support. |

### Active Workstream-Scoped Helpers

| Path | Helper Status | Owner Workstream | Reason Reusable Helper Was Not Extended | Consolidation Target | Promotion Decision Point |
| --- | --- | --- | --- | --- | --- |
| `dev/orin_fb038_seam2_validation.ps1` | Helper Status: Workstream-scoped | FB-038 | Created as a deterministic seam runner while Python/PySide6 path availability was failing; it coordinates existing reusable validators rather than replacing them. | Retained as FB-038 release/PR evidence only. If a later branch needs deterministic validator orchestration, fold the runner semantics into documented validator invocation guidance or a reusable validation runner before creating another seam runner. | PR Readiness decision: keep Workstream-scoped after merge because it preserves FB-038 evidence without becoming the default reusable validation entrypoint. |
| `dev/orin_fb038_seam2_live_validation.ps1` | Helper Status: Workstream-scoped | FB-038 | Created to prove tray-origin Create Custom Task dialog-open/no-write behavior without overloading the broader FB-036 interactive suite mid-Workstream. | Retained as FB-038 tray/Create Custom Task evidence. Future tray-origin authoring work must first consolidate this flow into a reusable tray live validation helper or into `dev/orin_saved_action_authoring_interactive_validation.ps1`. | PR Readiness decision: keep Workstream-scoped after merge; do not promote until a second branch needs the same tray-origin authoring proof. |
| `dev/orin_fb038_seam3_live_validation.ps1` | Helper Status: Workstream-scoped | FB-038 | Created to prove tray-origin create completion, catalog reload, exact-match re-resolution, and cleanup while keeping Seam 3 separate from Seam 2 no-write proof. | Retained as FB-038 tray-origin create-completion evidence. Future create-completion work must consolidate with the Seam 2 helper into a reusable tray/Create Custom Task live helper or fold the scenario into the saved-action interactive suite. | PR Readiness decision: keep Workstream-scoped after merge; do not promote until future branches prove the flow is reusable beyond FB-038. |
| `dev/orin_fam003_resident_access_validation.py` | Helper Status: Workstream-scoped | FAM-003 | Created to prove the resident tray identity, immutable entries, quick-slot budget, owner-bounded AI/privacy routes, Windows tray visibility wording, and no-second-tray contract without overloading the generic desktop entrypoint validator mid-Workstream. | Fold the static contract checks into a future reusable tray/resident-access helper if another branch extends the same doorway. | PR Readiness decision: keep Workstream-scoped unless a second branch needs reusable resident doorway coverage. |
| `dev/orin_fam003_settings_repair_visual_validation.py` | Helper Status: Workstream-scoped | FAM-003 | Created to prove the bounded Global Settings / Tray / Quick Access USER visual-fail repair, including v10 settings IA / UI Exposure Contract proof where Tray is the real category and Quick Access is the only visible active subpage, future-gated inactive routes are hidden from active choices, the window is compact and settings-specific, Delete is labeled and destructive, Up/Down are grouped in a compact reorder pill, Restore Defaults is staged, Save/Revert/Done dirty-state behavior is deterministic, UIREF-001/002/003 top-level chrome/control-cluster/control-state adoption is proven, accepted AI Control Center family-grammar comparator screenshots are available, side-by-side contact-sheet output is written, focused window-control/dropdown/row-action/dirty/default/save proof artifacts are captured, artifact-to-surface ledger output exists, fail-capable defect ledger output separates structure, screenshot, reference, and actual visual/product conformance evidence, row-level fail-capable element-group conformance ledger output exists, scope coverage manifest output exists, and stale sparse utility geometry, fake future settings/categories, inactive/future-gated active controls, decorative one-item nav placeholders, glyph-only delete actions, weak left organizer, internal metadata chrome/table vocabulary, marker-only PASS, screenshot-existence PASS, overclaimed PASS ledgers, or shared-primitive claims without a promoted shared primitive are rejected. | Fold the visual/behavior checks into a future reusable settings-window or resident-access visual validation helper if another branch needs the same settings proof class. | PR Readiness decision: keep Workstream-scoped unless a second branch needs reusable settings repair proof coverage. |
| `dev/orin_fam003_visual_acceptance_target_packet.py` | Helper Status: Workstream-scoped | FAM-003 | Created to generate the branch-local Visual Acceptance Target USER packet in the standard `C:\Nexus USER\FAM-003` hub, actual Design Candidate Render PNG media, clean and annotated focused renders, color-plus-ID legend/callout traceability, element/state review aids, draft target template, rejected-pattern ledger, reusable design recipe template, source-truth conflict classification, branch-local versus durable-governance disposition, external-state receipt pointers, and standard timestamped `C:\Nexus USER\FAM-003-YYYYMMDD-HHMMSS.zip` without mutating product/runtime UI or claiming LV/UTS/PR green. It must clean stale same-label ZIPs and retired nonstandard visual-acceptance packet artifacts before reporting green, preserve that USER/ChatGPT findings are seed defects rather than the ceiling, define accepted visual targets as guides/templates rather than guaranteed literal final screenshots, and route durable repo-wide enforcement or shared primitives/templates to future Governance/FAM-002 approval. | Fold into a future reusable visual-target packet generator only after Governance approves a repo-wide Visual Acceptance Target gate and fixture coverage. | PR Readiness decision: keep Workstream-scoped; this is branch-local process support only. |
| `dev/orin_fam003_visual_acceptance_target_validation.py` | Helper Status: Workstream-scoped | FAM-003 | Created to fail the FAM-003 visual-target packet when required render media, annotated callout media, element legends, state coverage, guide/template wording, Visual Selection Ledger template, Draft Branch Visual Acceptance Target, rejected-pattern ledger, reusable design recipe template, source-truth conflict classification, branch-local governance hardening markers, UDL/false-green integration, external-state files, validation receipt final-pass proof, or folder/ZIP parity are missing. It rejects active final validation receipts with `FAIL`. It does not prove USER acceptance, Live Validation green, UTS completion, PR readiness, or implementation match; it only proves that the branch-local visual-target packet is reviewable and that Design Candidate Render evidence remains distinct from USER_ACCEPTED target and Implementation Match Proof. | Fold into future reusable packet validation only after a Governance-approved visual-target packet schema exists. | PR Readiness decision: keep Workstream-scoped; this helper is not a global visual gate. |
| `dev/orin_fam003_same_defect_recurrence_validation.py` | Helper Status: Workstream-scoped | FAM-003 | Created to prove the branch-local LV1 same-defect false-closure loop breaker after repeated FAM-003 Global Settings / Tray / Quick Access retest-candidate rejects. It validates the external same-defect recurrence ledger, reopened recurring rows, `F3-LV1-PROOF-003` gate-enforcement row, active external state blocked-gate receipts, UTS blocked posture, packet semantic validator hardening, and prior false-packet blockability. It does not prove visual conformance, USER acceptance, LV green, UTS completion, PR readiness, merge readiness, release readiness, or cleanup readiness. | Fold into a future reusable Live Validation false-closure recurrence helper only after another branch needs the same same-defect recurrence gate and Governance approves reusable fixture coverage. | PR Readiness decision: keep Workstream-scoped; this is branch-local loop-breaker proof only. |

## Admitted Future Helper Families

These reservations do not register files that do not yet exist. They record the reuse order and expected names for already-admitted future work so a later implementation branch does not invent helper families ad hoc.

### Sensor HUD Beta

- Reuse order first: extend `dev/orin_desktop_entrypoint_validation.py` for tray and overlay entry coverage before creating a new Sensor HUD entry validator.
- Reuse order second: extend `dev/orin_interaction_baseline_validation.py` for command-overlay Sensor HUD intents before creating a new interaction-path helper.
- If a future implementation branch still needs new durable helpers after that reuse check, use `dev/orin_sensor_provider_validation.py` for provider contract, merge and dedup, stale-state, fallback, pause and resume, polling, and effective-rate proof.
- If a future implementation branch still needs new durable helpers after that reuse check, use `dev/orin_sensor_hud_layout_validation.py` for layout persistence, rematch, unresolved-placeholder behavior, search, pinning, categorization, and serialized card state proof.
- If a future implementation branch still needs new durable helpers after that reuse check, use `dev/orin_sensor_hud_live_validation.ps1` for tray actions, Sensor HUD open and edit behavior, pause and resume, lock and edit interaction, and live desktop proof.
- Provider-specific probes remain `Temporary probe` assets unless and until they are promoted through this registry with an explicit status and reuse story.

## Release / PR Standard

Before a branch reports PR Readiness green:

- all new root `dev/` validation helpers must be registered here
- workstream-scoped helpers must carry the required markers above
- temporary probes must be deleted or promoted
- any helper that is kept after merge must have a clear status and reuse/consolidation story
- release notes or closeout text must reference reusable helper families by capability, not by ad hoc seam-only names, unless the seam helper is intentionally preserved as workstream-scoped evidence
- `dev/orin_branch_governance_validation.py --release-readiness-health-gate` must prove post-merge source truth through the `Release Readiness Health Pass` before PR creation or merge approval; the owning record must include `Post-Merge Branch Authority Projection:`, `Stale Active Branch Wording Scan:`, `Stale PR Creation / PR Readiness Pending Wording Scan:`, `Merged-Unreleased Scope Posture:`, `Release Execution Gate:`, `Watcher / Live PR State Projection:`, `Branch Cleanup Plan:`, `Branch Cleanup Execution Gate:`, `FAM Overlap Routing:`, `Release Candidate Anchor Projection:`, `Release Window Contributor Inventory:`, `Governance Intake Routing:`, and `Projected Post-Merge Validation:` after any Stage 2 or bot-review source-truth repair. A post-merge projection receipt cannot clear the health pass while the same branch still has a record under `Active Branch Authority Records`; `Merge-Stable Projection Shadowed By Active Authority` blocks that projection-beside-active-authority shape before PR green. PR Readiness Stage 1 must also fail with `PR Readiness Stage 1 branch-authority fold-down required` when the active record's health pass or `Post-Merge State` says the branch must become historical/no-active or historical merged-unreleased while the actual record still remains in `Active Branch Authority Records`; projection-only text is not a substitute for the real fold-down. `Branch Cleanup Execution Gate:` keeps stale/old branch cleanup, worktree removal, branch switching, and GitHub Desktop-bound worktree cleanup out of Release Readiness; cleanup belongs to `Branch Readiness Stage 2 - Execution Gate` after `Stale Branch Cleanup Plan:`, branch/worktree creation or validation, `git worktree list`, and replacement target proof show no repository will lose its valid branch target. Release Readiness itself must declare `Release Candidate Anchor:`, `Release Candidate Anchor Source:`, `Target Commit:`, `Historical Endpoint Handling:`, `Candidate Includes Later Governance Repairs:`, `Release Ownership Model:`, `Release Window Contributors:`, `Merged-Unreleased Scope Inventory:`, `Last Runtime PR:`, `Post-Runtime Governance Repairs:`, and `FAM Contributor Routing:` and must stop on `Release Candidate Anchor Missing` or `Release Window Contributor Inventory Missing` when those values are absent or ambiguous. Pre-merge standing-Governance repair receipts may use `Target Commit:` as an external-owner pointer only when the active release-window external state owns the candidate posture; final Release Execution operator copy blocks still require a concrete commit SHA only.
- `dev/orin_branch_governance_validation.py --release-readiness-health-gate` must distinguish active PR operator state from projected post-merge source truth. The stale-post-merge scan checks the `## Post-Merge State` projection, while live PR URL, watcher, mergeability, comment, review, reaction, and current active-branch facts remain operator evidence outside that projection until they are folded down after merge.
- The release-readiness health gate also owns `Merge-Stable Source Truth Projection Missing`: it must scan merged-main current-state owners, compact pointer rows, worktree slot receipts, branch plans that have not been retired, and canonical detail branch records for stale pre-PR, open-PR, active-branch, active-successor, or Stage 2-pending wording after a PR has merged. Pre-PR phrases such as `No live PR`, `PR Creation Approval: Pending`, `Stage 2 PR Creation: Pending`, `PR creation pending`, and `PR Readiness Stage 1 Ready For Stage 2`, plus open/merge-pending phrases such as `PR #<n> is open`, `PR #<n> is live`, `Live PR #<n> exists`, or `Approve merge of PR #<n>`, may remain only in explicitly labeled historical snapshot fields; summary/current-state fields must project historical merged-unreleased, released/closed, or no-active-branch posture. It also owns `Post-Release Canon Closure Drift`: after a public prerelease tag exists, branch records, branch plans, backlog, roadmap, worktree slots, family visions, and compact source-truth pointers must not keep included scope as current `merged-unreleased` posture for that published release window. The validator must derive the window from Git/GitHub/tag truth and first-parent PR/merge commits, resolve prerelease tags to explicit commit IDs or paginated GitHub compare truth instead of relying on local tag refs, and use paginated compare rather than a full-history scan when the previous prerelease commit is unavailable, then scan included receipt files by release tag, PR number, merge commit, and branch identity; stale `merged-unreleased` lines may remain only when explicitly labeled as historical pre-release snapshot evidence or as no-current-release-debt markers.
- `dev/orin_branch_governance_validation.py --standing-governance-intake-gate` must prove the single `Standing Governance Intake Branch` is on the correct branch/worktree contract, has clean tracked state, uses a `Release Readiness digest` intake for release-blocker repair plus USER-approved `automation/worktree governance intake` for non-runtime multi-worktree safety repair and USER-approved `phase-gate governance intake` for repeatable non-runtime phase-gate misses, carries at most `One Active Cycle` with `RRI-YYYYMMDD-NNN` in external operational state, keeps the repo `Active RRI Cycle:` marker as an external-owner pointer rather than an in-repo current value, satisfies the pre-intake `Sync Rule`, preserves `Waiting For Governance Intake` / `Waiting For Updated Main`, restricts changed files to governance/source-truth/validator/registered-helper scope, permits registered source-truth validator updates and their fixture directories when the accepted intake changes historical/current branch-record posture, permits the standing authority record as the only active-record exception under merged-main `No Active Branch`, requires the post-merge `Return Digest`, requires `Neutral Main Workspace Rebaseline` proof that `C:\Nexus Desktop AI` is clean on `main` and equal to `origin/main`, and enforces `Return Digest Identity Guard` / `Return Digest Origin Identity Missing` so the returned originating-lane prompt cannot default to the wrong worktree or branch
- `dev/automation_observability_report.py` must classify multi-worktree automation findings using configured cwd/worktree/branch/`HEAD`/`origin/main` evidence. `Automation CWD Worktree Mismatch` blocks lane-sensitive automation reports from stale neutral main or wrong assigned worktrees, and only `BLOCKER_CANDIDATE` or `REVIEW_REQUIRED` findings may enter a bounded repair seam.
- `dev/orin_branch_governance_validation.py --pr-readiness-gate` must treat the reusable `Standing Governance Intake Branch` as a non-runtime PR carrier: it may use `Pre-PR Live State: No live PR` before USER-approved Stage 2 PR creation, must treat prior closed governance PRs as historical merge proof only, and must not require selected-next runtime workstream or package admission for the governance PR itself.
- `dev/orin_branch_governance_validation.py --pr-readiness-gate` must also distinguish normal Stage 1 pre-PR posture from a true PR-state blocker: `Pre-PR Live State: No live PR`, `PR Creation Approval: Pending USER approval`, and `Stage 2 PR Creation: Pending USER approval` are lawful before Stage 2, but PR-ready green still requires a live GitHub PR. Source-only, docs-only, governance, validator, or repo-wide support branches may clear selected-next inertia only with `No Successor Runtime Branch By Inertia: USER-waived`, `Selected-Next Defer User Waiver: Granted`, and post-merge `No Active Branch` projection.
- `dev/orin_branch_governance_validation.py --pr-readiness-gate` must ignore historical branch-authority receipts when enforcing selected-next successor lock. Historical local/remote branch refs are release/evidence trace, not proof that the selected-next implementation branch already exists; active current-branch refs may be ignored only when source truth explicitly records `Selected Next Implementation Branch: Deferred to Branch Readiness` or equivalent no-branch-created wording.
- `dev/orin_branch_governance_validation.py --worktree-confinement-gate` must prove `Assigned Worktree Confinement` for the active branch record by requiring `Active Thread Owner:`, `Thread Assignment Status:`, `Worktree Ownership Ledger:`, `Intended Write Set:`, `Same Worktree / Same Branch Collision Check:`, `Dirty Worktree Collision Check:`, `Dirty Worktree Recovery Packet:`, `Off-Worktree Work Routing:`, `Governance Routing Barrier:`, `New Worktree Decision Gate:`, comparing `Expected Worktree Root:` to `Actual Worktree Root:`, preserving `No Cross-Worktree Mutation`, proving the `GitHub Desktop-bound worktree`, and failing on `Worktree Escape User Waiver Missing` unless USER has explicitly recorded `Worktree Escape User Waiver: Granted` with scope, expiration, validation, and return path. Failed collision checks, past-tense or otherwise non-current active-owner claims, unidentified owners, invalid/void, nominal, or paper-only thread assignments, disabled/optional/bypass-permitted Governance Routing Barriers, duplicate `## Assigned Worktree Confinement` sections, forged/fabricated/invalid operational truth, USER approval scheduled after worktree creation in verb or noun form, forged/invalidated approval evidence, destructive or unilateral dirty-worktree recovery, conditional cross-worktree exceptions, receipt-derived live authority, and duplicate durable-receipt or branch-record authority pointers fail closed. Live external authority must apply the same owner-preserving dirty-recovery contract whenever tracked changes exist. When an exact USER-approved one-time bootstrap freezes standing Governance and prohibits external-state mutation, the gate may use a `Durable Carrier Admission Receipt` from `Docs/worktree_slots.md` only when that receipt proves branch/worktree identity, an affirmative whole-value non-live-state boundary, owner-preserving freeze-and-reconcile recovery, an active governance barrier, the complete confinement markers, a closed affirmative Git/helper truth-source contract, and a mandatory whole-value pre-PR fold-down obligation. Durable fallback expires when a live PR or canonical `Status:`, `Current Phase:`, or `Phase:` marker enters PR Readiness/PR Review. After bounded completion, the gate may recognize the matching exact `Historical Carrier Admission Receipt` only when it contains an affirmative bounded non-revoked USER admission decision, a whole-value non-authoritative repo live-state boundary, closed collision-clear evidence, a whole-value expired-or-absent escape-waiver receipt with no renewal or reactivation suffix, contains no active `Worktree:`, `Slot ID:`, `Assigned Branch:`, task-owner, assignment, write-authority, or escape-waiver authority marker, and its historical worktree receipt matches the current carrier. The gate must enforce exact active `Worktree:` marker parsing and must not treat historical worktree evidence as active worktree assignment, active confinement authority, or Release Readiness blocker.
- Dirty-worktree confinement regression fixtures must supply deterministic tracked-change status instead of inheriting the validator checkout's clean/dirty state; a clean checkout must not bypass negative dirty-collision or destructive-recovery mutations.
- Modern committed-journal recovery-payload detection must normalize camelCase and PascalCase field boundaries, and malformed-audit scanning must resume when a missing comma is followed directly by a plausible root object member; neither spelling nor malformed delimiter recovery may hide target-set evidence.
- Historical carrier classification must also require whole-value affirmative `Admission Confinement Receipt`, closed no-active `Fold-Down Result`, and mandatory later-USER-decision `Future Gate Boundary Receipt` values. Failure, escape, retained-active-authority, negated, optional, or conditional wording fails closed even when expected substrings are present.
- Historical carrier classification must require a concrete bounded `Historical Write Set Receipt`, a whole-value `Slot Reuse Posture` that leaves the recorded slot unreserved, and a whole-value external Git/helper `Operational Truth Source` that grants the receipt no live-state exception. Repository-wide or unrestricted write sets, retained or conditional slot assignment, and receipt-owned current authority fail closed.
- An external Live Branch Projection that points to an active `Durable Carrier Admission Receipt` must run the referenced receipt through the complete durable confinement validator, including owner, assignment, recovery, write-set, collision, routing, worktree-root, waiver, upstream, and pre-PR checks. Receipt shape alone is not authority.
- `dev/orin_external_state_validation.py` must enumerate a snapshot's complete physical inventory again after final copied-file and manifest hashes, compare that inventory with the first pass and the manifest, and reject files created, removed, or substituted during validation.
- `dev/orin_external_state_validation.py` must reject every nested directory or filesystem alias beneath the flat `audit_log` transaction namespace, re-enumerate the full typed audit inventory after final file hashes, bind every modern `Committed` live target to its confined `After SHA256`, and revalidate the complete snapshot manifest, physical inventory, copied-file hashes, and released-lock contract at the final journal-validation boundary after later lock, live-target, audit, and snapshot checks. It must fail on late audit entries, late snapshot entries or mutation, lock replacement or reacquisition, or missing, retained-before, unrelated, changed-during-validation, unreadable, or unconfined target bytes. Immutable legacy compatibility receipts must carry canonical transaction timestamps, their snapshots must not postdate the receipt, and their released locks must not predate it. Target-scoped `Record Role` values must reject nominal, paper-only, forged, fabricated, invalid, unverified, simulated, placeholder, or non-operational live-authority claims.
- Durable and historical `Non-Includes` validation must reject a closed exclusion inventory followed by full, broad, global, unbounded, unrestricted, repository-wide, retained, or ongoing write/mutation/repository authority or access. Generic `suffix` wording outside USER-packet path context must not enter the RAR path-suffix review family.
- Durable intended write-set validation must reject concrete-path or named-scope values followed by broad, global, repository-wide, arbitrary, or open-ended write/mutation authority, access, permission, or scope. Generic `transaction state`, `record role`, `Before SHA256`, `After SHA256`, and `modern target` wording must remain unknown to the external transaction review family unless journal, target-set, target-currentness, audit, external-state, released-lock, or snapshot-manifest context is present.
- Future Codex App hook enforcement should extend the existing worktree-confinement gate before adding a new root validator. The validation expectation is policy/source-checking only unless USER separately approves local hook installation: report assigned root, actual root, target root, read/write mode, waiver state, and whether the requested action is read-only analysis or mutation. It must reject live hook state in repo docs, governance-owned mutation from a non-Governance root without waiver, sibling-worktree packet generation, sibling-worktree Git mutation, external-state writes without approval, and branch-specific locks that would block legal branch movement inside the assigned root. Installed hook config, per-thread lock files, waiver files, and audit logs remain USER-local operational state under Codex home or another USER-approved external owner.
- `dev/orin_branch_governance_validation.py --pr-readiness-gate` must require PR Readiness Stage 1 `Origin/Main Freshness Check` source truth with `Branch Creation Base:`, `Current origin/main:`, `Origin/Main Advanced Since Branch Creation:`, `Origin/Main Changed Files:`, `Branch Changed Files:`, `Reconciliation Required:`, `Reconciliation File List:`, `Reconciliation Recommendation:`, and `Reconciliation Mutation Status:`; when upstream changes require review, `Origin Main Reconciliation Packet Required` blocks Stage 2 and PR creation until Stage 1 outputs the complete reconciliation data and recommendation with no file fixes during Stage 1.
- `dev/orin_branch_governance_validation.py` must source-check `Current-Main Reconciliation Identity Guard` governance and must fail active implementation branches on `Worktree Branch Identity Drift` when `Docs/feature_backlog.md` or `Docs/prebeta_roadmap.md` stop reasserting the current branch and active authority record after current-main reconciliation. The guard phrase is origin/main is context, not identity, and the required markers are `Assigned Worktree Branch Identity:`, `Branch-Local Authority Reassertion:`, `Incoming Main Active-Branch Blocks Accepted: NO`, and `Sibling Worktree Identity Preservation:`.
- `dev/orin_branch_governance_validation.py` source-checks the Branch Readiness `Carrier Lifecycle Decision` contract across governance docs. The packet must include `Carrier Lifecycle Classification:`, `Remote Branch State:`, `Unique Branch Diff:`, `Origin/Main Ancestry:`, `Origin/Main Advanced Since Branch Creation:`, `Open PR State:`, `Worktree Checkout State:`, `Recommended Stage 2 Carrier Action:`, `Stale Branch Cleanup Plan:`, `Branch Cleanup Execution Gate:`, `Recreate From Current origin/main:`, and `No Unique Commit Loss Proof:`, and the classification vocabulary is limited to `Fresh current branch`, `Stale empty local branch`, `Stale branch with unique commits`, `Historical merged branch`, `Wrong carrier/worktree`, and `Active remote/open PR branch`.
- Future helper/validator hardening should enforce `Future-Proof Implementation Review` where machine-checkable before PR Readiness green. Expected checks include required field presence in BP2/BP3/RAR/LV packets, derivation-rule presence for layout and runtime state, magic-value justification, default-only or one-state proof rejection, future-scope-implemented-by-inference detection, template/shared-primitive/reference/source-truth gap routing, issue-candidate disposition for out-of-scope defects, and `Reference Effectiveness Warning` after repeated same-class repairs. Helper implementation remains future-gated until separately approved; Codex must manually apply the rule in phase digests until then.
- Future helper/validator hardening should enforce `Reference Candidate Synchronization Gate` where machine-checkable before PR Readiness green. Expected report-only helper behavior includes reading promoted repo references, reading admitted branch-owned external candidate records when the local workflow has access, generating an aggregate candidate sync report, generating a collision review report by conflict key, and remaining clean-clone safe when `C:\Nexus Governance State` is unavailable. Expected checks include `Reference Candidate Sync Review:` field presence when a branch proposes, consumes, varies, supersedes, or depends on same-class standards; candidate table and collision table shape; conflict key presence; same-class candidate collision detection; candidate treated as canon before promotion; external candidate overriding promoted repo reference; promoted reference moved external; branch invents candidate while same-class candidate exists without merge/split/variant/collision disposition; promoted reference contradicted without `Reference Gap` or collision row; aggregate report generated from branch-owned proposals rather than hand-authored repo canon; and no external candidate treated as source truth. Helper implementation remains future-gated until separately approved; Codex must manually apply the rule in phase digests until then.
